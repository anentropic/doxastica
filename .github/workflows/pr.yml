
name: PR Coverage Report

on:
  pull_request:
concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  coverage:
    name: Report test coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Sync dependencies (dev + ladybug extra)
        # D-03: coverage must exercise BOTH backends, so the ladybug reference
        # backend extra is synced here (unlike the isolation job in quality.yml).
        run: uv sync --locked --dev --extra ladybug

      - name: Run tests with coverage
        run: uv run pytest --cov=src/doxastica --cov-report=xml --cov-report=term

      - name: Post coverage comment
        uses: MishaKav/pytest-coverage-comment@v1.1.51
        with:
          pytest-coverage-path: coverage.xml
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache pruning
        if: always()
        run: uv cache prune --ci
