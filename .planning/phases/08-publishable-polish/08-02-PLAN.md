---
phase: 08-publishable-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/src/index.md
autonomous: true
requirements: [PKG-03]
must_haves:
  truths:
    - "README.md leads with the Kumiho reference-implementation framing (arXiv 2603.17244, multi-scope extension, no recovery)"
    - "An MIT LICENSE file exists at the repo root"
    - "docs/src/index.md has a runnable Quick Start (no TODO placeholder)"
  artifacts:
    - path: "README.md"
      provides: "PKG-03 project framing: standalone reference implementation of Kumiho, multi-scope extension, no recovery"
      contains: "reference implementation of Kumiho"
    - path: "docs/src/index.md"
      provides: "Real Quick Start using the public API (MemoryCore.in_memory / revise / query_scope)"
      contains: "MemoryCore"
  key_links:
    - from: "docs/src/index.md Quick Start"
      to: "src/doxastica/__init__.py public exports"
      via: "import doxastica ... MemoryCore.in_memory()"
      pattern: "MemoryCore|query_scope|revise"
---

<objective>
Fill the two content gaps that satisfy PKG-03's framing requirement and remove the docs
TODO. The `README.md` is a 2-line stub and `docs/src/index.md` has "TODO: Add usage examples
here". The MIT `LICENSE` file already exists (verify only).

Purpose: a reader (or citation) immediately sees doxastica is a faithful Kumiho reference
implementation with the deliberate multi-scope extension and recovery exclusion, and a new
user can copy-paste a working first program.
Output: a framed `README.md`; a runnable Quick Start in `docs/src/index.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-publishable-polish/08-RESEARCH.md
@.planning/phases/08-publishable-polish/08-VALIDATION.md
@./CLAUDE.md
@src/doxastica/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with the PKG-03 Kumiho framing (verify MIT LICENSE)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 2-line stub — keep the arXiv link/attribution, expand the framing)
    - .planning/phases/08-publishable-polish/08-RESEARCH.md ("README target lead (PKG-03 framing)" code-example block is the canonical lead wording)
    - ./CLAUDE.md (the Project block: "faithful ... implementation of the Kumiho architecture (arXiv 2603.17244) plus one deliberate extension (multi-scope) and one deliberate exclusion (AGM recovery, replaced by superseded-chain semantics)")
  </read_first>
  <action>
    Replace the README body so it LEADS with the PKG-03 framing. The opening paragraph MUST
    contain the literal phrases "reference implementation of Kumiho", "multi-scope" (the
    deliberate extension; Kumiho is single-agent), and "no recovery" (the deliberate exclusion,
    replaced by superseded-chain semantics). Keep the arXiv 2603.17244 reference and Young Bin
    Park attribution.

    Structure the README as: (1) the one-paragraph framing lead; (2) a short "What it is" note
    that it is a standalone, zero-LLM, zero-NVM library whose correctness is provable (AGM/Hansson
    postulates verified mechanically as a backend conformance suite); (3) an Installation section
    reflecting the D-03 split — `pip install doxastica` ships a working in-memory AGM core with
    pydantic only, and `pip install doxastica[ladybug]` adds the LadybugDB reference backend;
    (4) a "Quick Start" pointer linking to the docs site Quick Start (do NOT duplicate the full
    example here — keep README lean, link to https://anentropic.github.io/doxastica/);
    (5) a License line stating MIT.

    Do NOT introduce any narrative/game/LLM concept. Use `ladybug` (never `ladybugdb`) as the
    installable/extra name.
  </action>
  <verify>
    <automated>grep -q "reference implementation of Kumiho" README.md && grep -q "multi-scope" README.md && grep -qi "no recovery" README.md && head -1 LICENSE | grep -q "MIT License"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "reference implementation of Kumiho" README.md` matches.
    - `grep -q "multi-scope" README.md` matches.
    - `grep -qi "no recovery" README.md` matches.
    - `grep -q "doxastica\[ladybug\]" README.md` matches (the D-03 extra is documented).
    - `grep -c "ladybugdb" README.md` returns 0 (no slopsquat token).
    - `head -1 LICENSE | grep -q "MIT License"` matches (LICENSE present — verify only, no edit).
  </acceptance_criteria>
  <done>README leads with the Kumiho reference-implementation framing (multi-scope extension, no recovery), documents the D-03 install split, states MIT; the MIT LICENSE file is confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the index.md TODO with a runnable Quick Start (PKG-03)</name>
  <files>docs/src/index.md</files>
  <read_first>
    - docs/src/index.md (the "## Quick Start" section currently reads "TODO: Add usage examples here.")
    - src/doxastica/__init__.py (the exact public exports: `MemoryCore`, `BeliefFilter`, `WORLD_SCOPE_ID`, `Status`, etc.)
    - src/doxastica/core.py lines 158-210 (the `MemoryCore.__init__` / `in_memory()` factory) and 390-420, 594-600 (the `revise` and `query_scope` signatures used in the example)
  </read_first>
  <action>
    Replace the "TODO: Add usage examples here." line under `## Quick Start` in
    `docs/src/index.md` with a real, runnable example using only the public API surface
    exported from `doxastica.__init__`. The example MUST:
    - import from `doxastica` (e.g. `MemoryCore`, `BeliefFilter`) and `uuid`/`uuid7`;
    - build the zero-dependency core via the `MemoryCore.in_memory()` factory (NOT a raw
      backend or connection — the in-memory backend needs no ladybug install, matching the
      `pip install doxastica` story);
    - call `revise(scope_id, belief_id, value, source_event_id)` where `source_event_id` is a
      `uuid.uuid7()` (native on the locked 3.14 floor — no extra dependency);
    - read the current base back with `query_scope(scope_id, BeliefFilter())` and show the
      revised value;
    - demonstrate one further `revise` of the same `belief_id` to a new value and show
      `query_scope` returns exactly the new current tail (append-only supersession, no
      duplicate) — this is the load-bearing illustration of the core's behavior.

    Keep it concise (a single fenced ```python block plus one or two sentences of prose). Do
    NOT reference any narrative/LLM concept, any ladybug-only call, or `MemoryCore.open()`
    (the Quick Start must run with `pip install doxastica` alone).
  </action>
  <verify>
    <automated>! grep -qi "TODO" docs/src/index.md && grep -q "MemoryCore" docs/src/index.md && grep -q "query_scope" docs/src/index.md && grep -q "uuid7" docs/src/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "TODO" docs/src/index.md` returns 0.
    - `docs/src/index.md` contains a ```python fenced block referencing `MemoryCore`, `revise`, and `query_scope`.
    - The example uses `MemoryCore.in_memory()` (not `.open(` and not a raw `InMemoryBackend()` construction is acceptable but the zero-dep factory is preferred) and `uuid7` for `source_event_id`.
    - `grep -c "ladybugdb" docs/src/index.md` returns 0.
    - The Quick Start does not import or call any ladybug-specific symbol (runs under `pip install doxastica` alone).
  </acceptance_criteria>
  <done>docs/src/index.md Quick Start is a runnable in-memory example (MemoryCore.in_memory → revise → query_scope, uuid7 event ids) with no TODO placeholder and no ladybug dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/docs prose → reader/citation | the published framing represents the library's provenance and scope; misframing misrepresents an academic reference impl |
| documented install command → user's environment | a wrong install string (e.g. `ladybugdb`) would send users to a non-existent/typosquat package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | install command in README / index.md | mitigate | Both tasks assert `grep -c ladybugdb == 0`; the install string is the verified `doxastica` / `doxastica[ladybug]` (CLAUDE.md "What NOT to Use": `ladybugdb` is a typosquat/404) |
| T-08-05 | Tampering | Quick Start example correctness | mitigate | Example uses only public `doxastica.__init__` exports against the read signatures; `mkdocs build --strict` (Plan 03 / wave gate) will fail on a broken code fence reference |
| T-08-06 | Repudiation | provenance framing (arXiv attribution) | accept | README retains the arXiv 2603.17244 + Young Bin Park attribution; correct attribution is the desired state, not a risk to mitigate |
</threat_model>

<verification>
- `grep -q "reference implementation of Kumiho" README.md` + `multi-scope` + `no recovery` all match.
- `head -1 LICENSE | grep -q "MIT License"`.
- `docs/src/index.md` has no `TODO` and references `MemoryCore` / `query_scope` / `uuid7`.
- No `ladybugdb` token in either file.
</verification>

<success_criteria>
README leads with the PKG-03 Kumiho framing (multi-scope extension, no recovery) and
documents the D-03 install split; docs/src/index.md ships a runnable zero-dependency Quick
Start; the MIT LICENSE is confirmed present.
</success_criteria>

<artifacts_produced>
NEW content this plan creates (no new files):
- `README.md`: rewritten body — framing lead, "what it is" note, D-03 install section, docs Quick Start link, MIT license line.
- `docs/src/index.md`: a runnable Quick Start `python` code block replacing the TODO.
No new modules, symbols, or dependencies; LICENSE is verified, not created.
</artifacts_produced>

<output>
Create `.planning/phases/08-publishable-polish/08-02-SUMMARY.md` when done.
</output>
