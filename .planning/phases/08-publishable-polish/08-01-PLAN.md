---
phase: 08-publishable-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PKG-02, PKG-04]
must_haves:
  truths:
    - "A fresh wheel METADATA declares pydantic as the sole required dep and ladybug only under the [ladybug] extra (D-03)"
    - "A fresh wheel METADATA carries License, Classifier, and Project-URL fields (PyPI-ready)"
    - "Every .github/workflows file uses only python-version 3.14 (no 3.11)"
    - "REQUIREMENTS PKG-02 and ROADMAP Phase-8 SC1 read the decided bar (pydantic sole required; 3.14-only), not the stale prose"
  artifacts:
    - path: "pyproject.toml"
      provides: "PyPI-ready [project] metadata (license, classifiers, keywords, [project.urls]) atop the existing D-03 dep split"
      contains: "[project.urls]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected PKG-02 line on the decided bar"
      contains: "PKG-02"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase-8 SC1 on the decided bar"
      contains: "Phase 8: Publishable Polish"
  key_links:
    - from: "pyproject.toml [project]"
      to: "wheel METADATA"
      via: "uv_build PEP 621/639 emission"
      pattern: "license|classifiers|project.urls"
---

<objective>
Make the package PyPI-ready at the metadata layer and reconcile the phase's own acceptance
prose with the two locked decisions that already superseded it. Two concerns, one plan,
because they share the same source-of-truth (the decided dependency/version bar) and touch
disjoint files from the other Phase-8 plans.

Concern A (PKG-04 metadata slice + PKG-02 verify): add `license`, `license-files`,
`keywords`, `classifiers`, and `[project.urls]` to `pyproject.toml [project]` so the wheel
METADATA renders a license, classifiers, and project links on PyPI — and verify (do not
rebuild) that the existing D-03 dep split and 3.14-only CI matrix already satisfy PKG-02.

Concern B (D-02): correct the STALE acceptance text in `.planning/REQUIREMENTS.md` (PKG-02)
and `.planning/ROADMAP.md` (Phase-8 SC1) from "exactly ladybug + pydantic" / "3.11 floor +
3.14" to the decided reality (pydantic SOLE required; ladybug the `[ladybug]` extra; CI
3.14-only). These are planning-doc text fixes, NOT code.

Purpose: a PyPI listing that shows license/classifiers/links, and a phase whose success
criteria are verifiable against the code that already exists.
Output: PyPI-ready `pyproject.toml`; corrected REQUIREMENTS/ROADMAP prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-publishable-polish/08-RESEARCH.md
@.planning/phases/08-publishable-polish/08-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PyPI-ready metadata to pyproject.toml; verify the D-03/3.14 bar holds (PKG-04 metadata, PKG-02)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the `[project]` table — already encodes the D-03 dep split and `requires-python = ">=3.14"`; do NOT touch `dependencies`, `optional-dependencies`, or `requires-python`)
    - .planning/phases/08-publishable-polish/08-RESEARCH.md (Pattern 3 + Pitfall 5: the exact fields the fresh wheel METADATA is missing — license/classifiers/urls/keywords)
  </read_first>
  <action>
    Add the following PyPI-readiness keys to the existing `[project]` table in pyproject.toml
    (additive only — leave `name`, `version`, `description`, `readme`, `authors`,
    `requires-python`, `dependencies`, and `[project.optional-dependencies]` exactly as they
    are; the D-03 split and 3.14 floor are LOCKED and already correct).

    Add to `[project]`:
    - `license = "MIT"` (PEP 639 SPDX expression — uv_build supports it)
    - `license-files = ["LICENSE"]`
    - `keywords = ["belief-revision", "agm", "kumiho", "graph", "epistemic", "memory"]`
    - `classifiers = [ "Development Status :: 4 - Beta", "License :: OSI Approved :: MIT License", "Programming Language :: Python :: 3.14", "Typing :: Typed", "Topic :: Scientific/Engineering :: Artificial Intelligence" ]`

    Add a new `[project.urls]` table with keys:
    - `Homepage = "https://github.com/anentropic/doxastica"`
    - `Documentation = "https://anentropic.github.io/doxastica/"`
    - `Repository = "https://github.com/anentropic/doxastica"`

    Do NOT add any runtime dependency. Per CLAUDE.md the ONLY required runtime dep is
    `pydantic`; `ladybug` stays an extra. These are metadata-only fields.
  </action>
  <verify>
    <automated>uv build --out-dir /tmp/dx-dist-01 && unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep -E 'License-Expression: MIT' && unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep -E 'Classifier: ' && unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep -E 'Project-URL: ' && unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep -E 'Requires-Dist: pydantic'</automated>
  </verify>
  <acceptance_criteria>
    - `uv build` succeeds and the fresh wheel METADATA contains a `License-Expression: MIT` line, at least one `Classifier:` line, and at least one `Project-URL:` line.
    - `unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep -E 'Requires-Dist: pydantic'` matches (pydantic is required); ladybug appears ONLY under `extra == 'ladybug'` (run `unzip -p /tmp/dx-dist-01/*.whl '*/METADATA' | grep ladybug` and confirm every match is gated by `extra == 'ladybug'`).
    - `grep -rh 'python-version' .github/workflows` shows only `["3.14"]` (no `3.11`) — the PKG-02 CI-matrix bar is already met (verify-only; no workflow edit in this task).
    - `requires-python = ">=3.14"` is unchanged in pyproject.toml.
    - Note: `uv build`/`uv` may panic under the local macOS sandbox (MEMORY: uv sandbox panic). If so, re-run the verify command with the sandbox disabled; CI (ubuntu) is unaffected.
  </acceptance_criteria>
  <done>Fresh wheel METADATA carries License-Expression/Classifier/Project-URL plus the unchanged pydantic-required + ladybug-extra split; CI matrix confirmed 3.14-only.</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale PKG-02 / SC1 prose to the decided bar (D-02)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the PKG-02 bullet, currently "Runtime deps `ladybug` + `pydantic` v2 only ... CI matrix Python 3.11 (floor) and 3.14")
    - .planning/ROADMAP.md (Phase 8 "Success Criteria" item 1, currently "Runtime dependencies are exactly `ladybug` + `pydantic` v2 ... a Python 3.11 (floor) and 3.14 matrix")
    - .planning/phases/08-publishable-polish/08-RESEARCH.md ("State of the Art" table: the old-bar→decided-bar mapping is the authoritative correction source)
    - ./CLAUDE.md (the D-03 reversal block + the 3.14-floor lock — the canonical decided wording)
  </read_first>
  <action>
    Edit the PKG-02 requirement bullet in `.planning/REQUIREMENTS.md` so it states the DECIDED
    bar instead of the stale one: `pydantic` v2 is the SOLE required runtime dependency and
    `ladybug` is the optional `[ladybug]` reference-backend extra (NOT "exactly ladybug +
    pydantic"); zero NVM imports; `hypothesis` in the dev group; the CI matrix is Python
    **3.14-only** (NOT "3.11 (floor) and 3.14"). Reference the governing decisions inline:
    Phase-2 D-03 (packaging reversal) and CONTEXT #2 (the 3.14-floor lock).

    Edit Phase 8 Success Criterion 1 in `.planning/ROADMAP.md` (the line beginning "Runtime
    dependencies are exactly `ladybug` + `pydantic`...") to the SAME decided bar: pydantic the
    sole required dep; ladybug the `[ladybug]` extra; the in-memory backend adds no dependency;
    hypothesis in the dev group; CI runs the conformance suite on Python 3.14-only. Cite D-03
    and CONTEXT #2.

    Do NOT change PKG-03 or PKG-04 text, the requirement IDs, the checkbox states, or the
    Traceability table. This is a wording correction of two specific lines only.
  </action>
  <verify>
    <automated>grep -q "sole" .planning/REQUIREMENTS.md && ! grep -qi "3.11 (floor)" .planning/REQUIREMENTS.md && ! grep -qi "exactly .ladybug. . .pydantic" .planning/ROADMAP.md && grep -q "3.14" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE 'exactly .ladybug. \+ .pydantic|3\.11 \(floor\)' .planning/REQUIREMENTS.md` returns no matches (stale wording gone).
    - The PKG-02 bullet in `.planning/REQUIREMENTS.md` mentions `pydantic` as the sole required dep, `ladybug` as the `[ladybug]` extra, and a 3.14-only CI matrix, and cites D-03.
    - `grep -niE 'exactly .ladybug. \+ .pydantic|3\.11 \(floor\)' .planning/ROADMAP.md` returns no matches.
    - Phase 8 SC1 in `.planning/ROADMAP.md` states pydantic-sole-required + ladybug-extra + 3.14-only and cites the governing decision.
    - The PKG-02 / PKG-03 / PKG-04 requirement IDs and the Traceability table are unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS PKG-02 and ROADMAP Phase-8 SC1 read the decided bar (pydantic sole required, ladybug extra, 3.14-only) with D-03 / CONTEXT-#2 citations; no stale "exactly" / "3.11 floor" strings remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject `[project]` → wheel METADATA → PyPI listing | declared metadata becomes the public package face; wrong license/classifier misrepresents the artifact |
| dependency declaration → installer resolution | a stray required dep would silently expand the install footprint past the D-03 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `pyproject.toml [project.dependencies]` | mitigate | Task 1 is metadata-only; acceptance asserts `Requires-Dist: pydantic` and ladybug-only-under-extra — a slipped required dep fails the gate |
| T-08-02 | Spoofing | `[project.urls]` Homepage/Repository | mitigate | URLs hardcoded to the canonical `github.com/anentropic/doxastica` + `anentropic.github.io/doxastica`; reviewed against repo_url in mkdocs.yml |
| T-08-03 | Information disclosure | wheel METADATA / classifiers | accept | metadata is intentionally public; no secret/credential is added (only license, classifiers, public URLs) |
| T-08-SC | Tampering | `ladybugdb` vs `ladybug` slopsquat | mitigate | No package install in this plan; `requires-python`/deps untouched; the `ladybug` (not `ladybugdb`) name is preserved and asserted in the METADATA grep |
</threat_model>

<verification>
- Fresh `uv build` wheel METADATA: `License-Expression: MIT`, ≥1 `Classifier:`, ≥1 `Project-URL:`, `Requires-Dist: pydantic`, ladybug only under `extra == 'ladybug'`.
- `grep -rh 'python-version' .github/workflows` → only `["3.14"]`.
- No `exactly ladybug + pydantic` / `3.11 (floor)` strings remain in REQUIREMENTS.md or ROADMAP.md.
</verification>

<success_criteria>
PyPI-ready metadata is present in the built wheel and the phase's own acceptance prose
(REQUIREMENTS PKG-02 + ROADMAP SC1) matches the decided D-03 / 3.14 bar, while the runtime
dependency contract (pydantic sole required, ladybug extra) is provably intact.
</success_criteria>

<artifacts_produced>
NEW symbols/keys this plan creates:
- `pyproject.toml`: new `license`, `license-files`, `keywords`, `classifiers` keys in `[project]`; new `[project.urls]` table (`Homepage`, `Documentation`, `Repository`).
- `.planning/REQUIREMENTS.md`: corrected PKG-02 bullet text (no new IDs).
- `.planning/ROADMAP.md`: corrected Phase-8 Success-Criterion-1 text.
No new source files, modules, or runtime dependencies.
</artifacts_produced>

<output>
Create `.planning/phases/08-publishable-polish/08-01-SUMMARY.md` when done.
</output>
