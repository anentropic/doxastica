---
phase: 08-publishable-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/backend-contract.md
  - docs/backend-contract.md
  - mkdocs.yml
  - .github/workflows/docs.yml
  - .github/workflows/release.yml
  - docs/changelog.md
autonomous: true
requirements: [PKG-04]
must_haves:
  truths:
    - "mkdocs build --strict is green AND the backend-contract page exists in the built site"
    - "The CI changelog invocations pass --config .cliff.toml so the repo config (group headers + docs-include marker) is honored"
    - "A fresh wheel imports and ships py.typed (the pipeline-authoritative build, not the stale dist)"
    - "The orphaned sphinx/myst docs/changelog.md stub is removed"
  artifacts:
    - path: "docs/src/backend-contract.md"
      provides: "The backend port contract published INSIDE docs_dir so the site renders it (PKG-04)"
      contains: "Backend Port Contract"
    - path: "mkdocs.yml"
      provides: "nav entry pointing the site at backend-contract.md"
      contains: "backend-contract.md"
    - path: ".github/workflows/docs.yml"
      provides: "git-cliff invocation with --config .cliff.toml"
      contains: ".cliff.toml"
    - path: ".github/workflows/release.yml"
      provides: "git-cliff-action honoring .cliff.toml"
      contains: ".cliff.toml"
  key_links:
    - from: "mkdocs.yml nav"
      to: "docs/src/backend-contract.md"
      via: "nav: Backend Contract entry inside docs_dir"
      pattern: "backend-contract.md"
    - from: ".github/workflows git-cliff calls"
      to: ".cliff.toml"
      via: "--config flag / GIT_CLIFF_CONFIG env"
      pattern: "\\.cliff\\.toml"
---

<objective>
Close the three PKG-04 wiring defects that a green build silently hides, plus one hygiene
delete. (1) The backend port contract lives OUTSIDE `docs_dir` (`docs/backend-contract.md`)
so the published site never renders it — relocate it into `docs/src/` and add a nav entry
(PKG-04's "publishes the port contract" is currently UNMET despite a green build). (2) The
`docs.yml` and `release.yml` git-cliff invocations omit `--config .cliff.toml`, so the repo's
dot-prefixed config (group headers + the `docs-include-start` marker) is silently ignored —
add the config to both. (3) Delete the orphaned `docs/changelog.md` sphinx/myst stub.

Purpose: the published docs site actually serves the backend contract (so third parties can
write backends), and the changelog renders with the repo's configured format end-to-end.
Output: contract page in the site nav; both workflows passing `--config .cliff.toml`; the
orphan stub removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-publishable-polish/08-RESEARCH.md
@.planning/phases/08-publishable-polish/08-VALIDATION.md
@./CLAUDE.md
@mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the backend contract into docs_dir and add it to the mkdocs nav (PKG-04)</name>
  <files>docs/src/backend-contract.md, docs/backend-contract.md, mkdocs.yml</files>
  <read_first>
    - docs/backend-contract.md (the existing Phase-1 BACK-04 contract — it has 0 markdown links per RESEARCH.md A1, so relocating cannot break `--strict`)
    - mkdocs.yml (`docs_dir: docs/src`; the `nav:` block currently has Home / API Reference / Changelog and no contract entry)
    - .planning/phases/08-publishable-polish/08-RESEARCH.md (Pattern 1 + Pitfall 3: relocate-into-docs_dir is the recommended Option A; the target nav shape is given)
  </read_first>
  <action>
    Move the contract file INTO `docs_dir` using `git mv docs/backend-contract.md
    docs/src/backend-contract.md` (Option A — relocate, the recommended fix; the file has no
    internal markdown links so `--strict` link checking is unaffected). Preserve the file's
    content exactly.

    Then add a nav entry to `mkdocs.yml` so the site exposes it. Insert `Backend Contract:
    backend-contract.md` into the `nav:` list between `Home: index.md` and `API Reference:
    reference/`. The resulting nav order is: Home, Backend Contract, API Reference, Changelog.
    (Paths in `nav:` are relative to `docs_dir`, so the entry is `backend-contract.md`, NOT
    `src/backend-contract.md`.)

    Do NOT use the snippet-include alternative (Option B) and do NOT leave a copy at the old
    `docs/backend-contract.md` path — relocation, not duplication.
  </action>
  <verify>
    <automated>test -f docs/src/backend-contract.md && ! test -f docs/backend-contract.md && grep -q "backend-contract.md" mkdocs.yml && git-cliff --config .cliff.toml -o CHANGELOG.md && uv run --no-sync mkdocs build --strict && test -f site/backend-contract/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/backend-contract.md` succeeds and `test -f docs/backend-contract.md` fails (relocated, not duplicated).
    - `grep -q "Backend Contract: backend-contract.md" mkdocs.yml` matches (nav entry added inside docs_dir-relative form).
    - After `git-cliff --config .cliff.toml -o CHANGELOG.md` (required first so the changelog snippet resolves under `check_paths: true`), `uv run --no-sync mkdocs build --strict` exits 0 and `test -f site/backend-contract/index.html` succeeds (the page is in the built site).
    - Note: the only expected build output is the benign mkdocs-material v2 vendor warning (RESEARCH "State of the Art") — it is NOT a `--strict` failure.
  </acceptance_criteria>
  <done>The backend contract lives at docs/src/backend-contract.md, appears in the mkdocs nav, and renders into site/backend-contract/index.html under a green --strict build.</done>
</task>

<task type="auto">
  <name>Task 2: Pass --config .cliff.toml to git-cliff in both CI workflows; delete the orphan changelog stub (PKG-04)</name>
  <files>.github/workflows/docs.yml, .github/workflows/release.yml, docs/changelog.md</files>
  <read_first>
    - .github/workflows/docs.yml (the "Generate changelog" step: `run: uvx git-cliff -o CHANGELOG.md` — missing `--config`)
    - .github/workflows/release.yml (the `changelog` job using `orhun/git-cliff-action@v4` with `args: --latest` and an `OUTPUT`/`GITHUB_TOKEN` env block — missing the config)
    - .cliff.toml (line 6 carries the `<!-- docs-include-start -->` marker the default config drops)
    - .planning/phases/08-publishable-polish/08-RESEARCH.md (Pattern 2 + Pitfall 2: git-cliff's default search path is `cliff.toml`, NOT the dot-file; the fix is `--config .cliff.toml` or `GIT_CLIFF_CONFIG`)
    - docs/changelog.md (the orphaned sphinx/myst `{include}` stub OUTSIDE docs_dir — anti-pattern, delete it; the live page is docs/src/changelog.md)
  </read_first>
  <action>
    In `.github/workflows/docs.yml`, change the "Generate changelog" step command from
    `uvx git-cliff -o CHANGELOG.md` to `uvx git-cliff --config .cliff.toml -o CHANGELOG.md`
    so the repo's dot-prefixed config (commit-parser groups + the `docs-include-start` marker)
    is used instead of git-cliff's silent default.

    In `.github/workflows/release.yml`, make the `changelog` job's `orhun/git-cliff-action@v4`
    step honor the same config. Add the action's config input — set `with.config: .cliff.toml`
    alongside the existing `args: --latest` (the git-cliff-action exposes a `config` input that
    maps to `--config`); equivalently add `GIT_CLIFF_CONFIG: .cliff.toml` to that step's `env`
    block. Prefer the explicit `config: .cliff.toml` input. Leave `args: --latest`, `OUTPUT`,
    and `GITHUB_TOKEN` unchanged.

    Delete the orphaned `docs/changelog.md` (the sphinx/myst `{include}` stub). It is outside
    `docs_dir`, unused by mkdocs, and its myst syntax is wrong for mkdocs-material; the live
    changelog page is `docs/src/changelog.md`. Do NOT touch `docs/src/changelog.md`.
  </action>
  <verify>
    <automated>grep -q -- "--config .cliff.toml" .github/workflows/docs.yml && grep -q ".cliff.toml" .github/workflows/release.yml && ! test -f docs/changelog.md && git-cliff --config .cliff.toml -o CHANGELOG.md && grep -q "docs-include-start" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q -- "--config .cliff.toml" .github/workflows/docs.yml` matches.
    - `grep -q "\.cliff\.toml" .github/workflows/release.yml` matches (via `config: .cliff.toml` input or `GIT_CLIFF_CONFIG` env).
    - `test -f docs/changelog.md` fails (orphan deleted); `test -f docs/src/changelog.md` still succeeds (live page untouched).
    - `git-cliff --config .cliff.toml -o CHANGELOG.md` then `grep -q 'docs-include-start' CHANGELOG.md` matches (the configured render produces the marker; the default config would not).
    - Note: `CHANGELOG.md` is generated and gitignored — do NOT commit it; generating it locally for the gate is expected.
  </acceptance_criteria>
  <done>Both workflows invoke git-cliff with .cliff.toml; the configured changelog renders the docs-include marker; the orphaned docs/changelog.md stub is removed and docs/src/changelog.md is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove a fresh wheel imports and ships py.typed (PKG-04 distribution gate)</name>
  <files>docs/src/backend-contract.md</files>
  <read_first>
    - .planning/phases/08-publishable-polish/08-RESEARCH.md (Pitfall 4 + "Code Examples": the committed dist/ is a stale 71-byte Phase-1 scaffold; a fresh `uv build` packages all 9 modules — never trust the committed dist; dist/ is already gitignored and untracked)
    - .github/workflows/release.yml (the `build` + `validate` jobs already rebuild via `uv build` on tag and assert py.typed — this task is the local mirror of that gate, no workflow edit)
  </read_first>
  <action>
    This is a verification-only task — no production-code edit. Build a fresh distribution into
    a temp dir (NOT the gitignored committed `dist/`, which is the stale Phase-1 scaffold per
    Pitfall 4) and prove it is a real, importable, typed package: `uv build --out-dir
    /tmp/dx-dist-03`, confirm the wheel ships `doxastica/py.typed`, and confirm the package
    imports and exposes its public surface.

    Do NOT push a tag, do NOT run the PyPI publish step, and do NOT re-commit `dist/` (release.yml
    is the authoritative build path; actually publishing is OUT of scope per D-05). The
    `docs/src/backend-contract.md` listing in `files_modified` is a no-op anchor for plan-level
    file ownership in this wave — make no content change to it here (Task 1 owns its relocation).
  </action>
  <verify>
    <automated>uv build --out-dir /tmp/dx-dist-03 && unzip -l /tmp/dx-dist-03/*.whl | grep -q "doxastica/py.typed" && uv run --no-sync python -c "import doxastica; assert 'MemoryCore' in doxastica.__all__ and 'BeliefStore' in doxastica.__all__"</automated>
  </verify>
  <acceptance_criteria>
    - `uv build --out-dir /tmp/dx-dist-03` succeeds.
    - `unzip -l /tmp/dx-dist-03/*.whl | grep -q "doxastica/py.typed"` matches (typed marker shipped).
    - `python -c "import doxastica; assert 'MemoryCore' in doxastica.__all__"` exits 0 (real package, not the 71-byte scaffold).
    - No tag is pushed and no PyPI publish is invoked (D-05 scope boundary).
    - Note: `uv build` may panic under the local macOS sandbox (MEMORY: uv sandbox panic); if so, re-run with the sandbox disabled. CI (ubuntu) is unaffected.
  </acceptance_criteria>
  <done>A fresh wheel built outside the stale committed dist/ ships doxastica/py.typed and imports with MemoryCore/BeliefStore on its public surface; no publish performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo doc file → published docs site (`docs_dir`) | a file outside docs_dir is silently absent from the site — a green build masks an unmet "publish the contract" requirement |
| git history + `.cliff.toml` → CI git-cliff → CHANGELOG / release notes | a wrong/ignored config produces a misformatted public changelog and release body |
| committed `dist/` → release artifact | trusting the stale committed wheel would publish an empty package |
| CI workflow (`uvx`, git-cliff-action) → supply chain | pinned action versions; OIDC trusted publishing in release.yml (no long-lived PyPI token) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information disclosure | contract page absent from site | mitigate | Task 1 asserts `site/backend-contract/index.html` exists under `--strict` — the "publishes the contract" gate cannot pass on a green-but-empty build |
| T-08-08 | Tampering | CI git-cliff using wrong config | mitigate | Tasks 2 pins `--config .cliff.toml` in both workflows; acceptance greps the `docs-include-start` marker that only the repo config emits |
| T-08-09 | Tampering | stale committed `dist/` shipped | mitigate | Task 3 builds fresh into `/tmp` and asserts real modules + py.typed; release.yml rebuilds on tag; committed `dist/` is gitignored/untracked (verified) |
| T-08-10 | Elevation of privilege | PyPI publish credentials | accept/transfer | release.yml uses `pypa/gh-action-pypi-publish` OIDC trusted publishing (no token); actually publishing is OUT of scope (D-05) — pipeline-ready only |
| T-08-SC | Tampering | `ladybugdb` slopsquat in docs/workflows | mitigate | No package install added; the contract page and workflows reference `ladybug` only; CLAUDE.md guard preserved |
</threat_model>

<verification>
- `git-cliff --config .cliff.toml -o CHANGELOG.md && uv run --no-sync mkdocs build --strict && test -f site/backend-contract/index.html` (the PKG-04 publish gate).
- `grep -q -- "--config .cliff.toml" .github/workflows/docs.yml` and `grep -q ".cliff.toml" .github/workflows/release.yml`.
- `grep -q 'docs-include-start' CHANGELOG.md` after a configured render.
- `uv build --out-dir /tmp/dx-dist-03 && unzip -l /tmp/dx-dist-03/*.whl | grep doxastica/py.typed`.
- `! test -f docs/changelog.md` and `! test -f docs/backend-contract.md`.
- Wave gate: `uv run pytest` (Phase-7 conformance MUST stay green — frozen).
</verification>

<success_criteria>
The published docs site serves the backend port contract (page in nav + in `site/`), both CI
workflows render the changelog through the repo's `.cliff.toml` (group headers + docs-include
marker), a fresh wheel imports and ships py.typed, and the orphaned changelog stub is gone —
with the Phase-7 conformance suite still green.
</success_criteria>

<artifacts_produced>
NEW files/symbols this plan creates or relocates:
- `docs/src/backend-contract.md` — NEW location of the backend port contract (relocated from `docs/backend-contract.md`, which is removed).
- `mkdocs.yml` — NEW `nav` entry `Backend Contract: backend-contract.md`.
- `.github/workflows/docs.yml` — git-cliff step gains `--config .cliff.toml`.
- `.github/workflows/release.yml` — git-cliff-action gains `config: .cliff.toml` (or `GIT_CLIFF_CONFIG` env).
- `docs/changelog.md` — DELETED (orphaned sphinx/myst stub).
- (Generated, not committed) `CHANGELOG.md` at repo root — produced for the local gate, gitignored.
No new source modules or runtime dependencies.
</artifacts_produced>

<output>
Create `.planning/phases/08-publishable-polish/08-03-SUMMARY.md` when done.
</output>
