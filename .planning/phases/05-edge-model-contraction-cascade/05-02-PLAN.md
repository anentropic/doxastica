---
phase: 05-edge-model-contraction-cascade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/doxastica/core.py
  - tests/test_cascade.py
autonomous: true
requirements: [EDGE-01]
must_haves:
  truths:
    - "MemoryCore.add_edge(from_state_id, to_state_id, edge_type) lays a generic typed edge between two BeliefStates"
    - "add_edge takes the closed EdgeType enum — only SUPERSEDES/DEPENDS_ON/DERIVED_FROM are layable via the public seam"
    - "Calling add_edge twice with the same args yields exactly one edge (idempotent — guaranteed by the backend, the core adds nothing)"
    - "add_edge to a non-existent endpoint creates no edge and raises nothing (D-07 silent no-op is INTENDED mechanism, pinned by test)"
    - "The whole add_edge write happens inside exactly ONE unit_of_work (D-06)"
  artifacts:
    - path: "src/doxastica/core.py"
      provides: "MemoryCore.add_edge body — passthrough to backend.add_edge inside one unit_of_work"
      contains: "def add_edge"
    - path: "tests/test_cascade.py"
      provides: "add_edge mechanism tests (idempotency, closed-enum, silent-no-op D-07 pin) across both backends"
      contains: "add_edge"
  key_links:
    - from: "src/doxastica/core.py MemoryCore.add_edge"
      to: "self._backend.add_edge(edge_type, str(from_id), str(to_id))"
      via: "passthrough inside self._backend.unit_of_work()"
      pattern: "unit_of_work"
---

<objective>
Implement the public `MemoryCore.add_edge(from_state_id, to_state_id, edge_type)` method (EDGE-01) as
a near-passthrough to the backend's idempotent `add_edge` primitive, wrapped in exactly one
`unit_of_work` (D-06). The public signature takes the closed `EdgeType` enum so only the three generic
edge types (`SUPERSEDES`/`DEPENDS_ON`/`DERIVED_FROM`) are layable via the public seam — the structural
`HAS_REVISION`/internal-`SUPERSEDES` wiring stays inside `_append_state`.

Purpose: complete the edge-creation half of the edge/cascade surface. This is independent of the
`direction` port extension (Plan 01) — it composes only on existing primitives — so it runs in parallel
in Wave 1.

Output: `MemoryCore.add_edge` body; `tests/test_cascade.py` (new) covering idempotency, the closed-enum
seam, and the D-07 silent-no-op-on-missing-endpoint pin, across both backends.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-edge-model-contraction-cascade/05-CONTEXT.md
@.planning/phases/05-edge-model-contraction-cascade/05-RESEARCH.md
@.planning/phases/05-edge-model-contraction-cascade/05-PATTERNS.md

@src/doxastica/core.py
@src/doxastica/protocol.py
@src/doxastica/ports.py
@tests/test_backend_parity.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — failing add_edge mechanism tests (tests/test_cascade.py)</name>
  <behavior>
    - add_edge lays a typed edge: after `core.add_edge(b.state_id, a.state_id, EdgeType.DEPENDS_ON)`, the edge is observable (the dependent relationship exists — verifiable via the backend `traverse` or, once Plan 03 lands, `get_impact`; for THIS plan assert via `backend.traverse(str(a.state_id), {"DEPENDS_ON"}, None, direction="in")` reaching b, since Plan 01 lands the direction param in the same wave — gate ordering note below).
    - Idempotency: calling `add_edge` twice with identical args leaves exactly one edge (no duplicate reached node, no raise).
    - Closed enum: the public signature accepts only `EdgeType` members — passing a raw string is a type error (basedpyright catches it; a runtime test is not required, but a positive test for each of the three generic types is).
    - D-07 silent no-op: `core.add_edge(real.state_id, ghost_uuid, EdgeType.DEPENDS_ON)` where `ghost_uuid` was never created MUST NOT raise and MUST lay no usable edge.
  </behavior>
  <read_first>
    - tests/test_backend_parity.py:240-262 — `_both_backends()` loop helper + byte-identical idiom; :74-81 — `_reached_ids`/`_frontier_ids`.
    - tests/conftest.py:34-45 — `backend` fixture (params=["memory","ladybug"] + importorskip).
    - src/doxastica/core.py:89-94 — `MemoryCore.in_memory()` for zero-dep construction; the public `revise(scope, belief_id, value, source_event_id)` signature for building real BeliefStates.
    - RESEARCH "Code Examples" — `test_add_edge_missing_endpoint_is_silent_no_op` (D-07 pin) gives a ready body.
    - IMPORTANT GATE-ORDERING NOTE: this plan's observation assertions use `traverse(..., direction="in")`, which is delivered by Plan 01 (same wave). When executing Wave 1, complete Plan 01 (or at least its Task 2-4 `direction` param) before this plan's RED tests can transition to GREEN. If executing this plan first, the `direction="in"` assertions will fail on the missing kwarg — that is expected until Plan 01 lands. To keep THIS plan's add_edge logic independently verifiable, ALSO include an idempotency/no-op assertion that does NOT depend on direction (e.g. assert no exception is raised and, on the in-memory backend, inspect that the adjacency contains a single edge entry via a backend-agnostic `traverse(..., direction="out")` from the dependent node, which exists pre-Plan-01).
  </read_first>
  <files>tests/test_cascade.py</files>
  <action>
    Create `tests/test_cascade.py`. Reuse the existing `backend` fixture and the `_both_backends`/`_reached_ids` idioms from `test_backend_parity.py` (import or mirror them — do not re-scaffold conftest). Write FAILING tests (RED until Task 2) for:
    (a) `add_edge` lays a DEPENDS_ON edge — verify the dependent edge is observable. Use a `direction="out"` traverse from the dependent node (`traverse(str(b.state_id), {"DEPENDS_ON"}, None)` reaches a) so the assertion does NOT depend on Plan 01; optionally add a `direction="in"` cross-check guarded to run once Plan 01 lands.
    (b) idempotency: add the same edge twice; assert the reachable set is unchanged (one edge) and no raise.
    (c) one positive test per generic `EdgeType` (SUPERSEDES, DEPENDS_ON, DERIVED_FROM) — each lays and is observable.
    (d) D-07 PIN: `add_edge(real.state_id, ghost_uuid, EdgeType.DEPENDS_ON)` with a never-created `ghost_uuid` raises nothing and lays no usable edge (the canonical D-07 silent-no-op test from RESEARCH Code Examples).
    Parametrize every case across both backends via the `backend` fixture or `_both_backends` loop.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cascade.py -q -k add_edge 2>&1 | tail -20  # RED until Task 2 adds MemoryCore.add_edge</automated>
  </verify>
  <acceptance_criteria>
    `tests/test_cascade.py` exists with add_edge tests that FAIL because `MemoryCore.add_edge` does not yet exist (AttributeError). Tests cover idempotency, all three generic EdgeTypes, and the D-07 silent-no-op pin, across both backends.
  </acceptance_criteria>
  <done>add_edge mechanism tests written and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MemoryCore.add_edge (passthrough inside one unit_of_work)</name>
  <behavior>
    - `add_edge(from_state_id, to_state_id, edge_type)` calls `self._backend.add_edge(edge_type, str(from_state_id), str(to_state_id))` inside exactly one `with self._backend.unit_of_work():` and returns `None`.
    - No endpoint-existence validation (D-07) — the body adds NO raise; the backend MERGE silently no-ops on a missing endpoint.
    - All add_edge tests from Task 1 go GREEN (the direction="in" cross-checks go green only once Plan 01 lands).
  </behavior>
  <read_first>
    - src/doxastica/protocol.py:101-108 — the EXACT public signature to match: `(self, from_state_id: UUID, to_state_id: UUID, edge_type: EdgeType) -> None`. Do NOT change it.
    - src/doxastica/core.py:286-314 — `_append` (the uow + backend-write convention to mirror); :247-284 — `_append_state` showing UUID-stringification at every backend call (`str(state_id)`, and `self._backend.add_edge("SUPERSEDES", str(state_id), prior["state_id"])` — note arg order `(edge_type, from_id, to_id)`).
    - src/doxastica/ports.py:78-86 — backend `add_edge(edge_type, from_id, to_id, props=None)` arg order.
    - src/doxastica/core.py:18-22 — driver-blind discipline (no `ladybug` import, no Cypher). src/doxastica/core.py:52 — model imports (ensure `EdgeType` is importable; add if absent).
  </read_first>
  <files>src/doxastica/core.py</files>
  <action>
    Implement `MemoryCore.add_edge` with the signature matching `protocol.py:101-108` EXACTLY. Body (PATTERNS Pattern 1, D-06/D-07):
    open exactly one `with self._backend.unit_of_work():` and inside it call `self._backend.add_edge(edge_type, str(from_state_id), str(to_state_id))`. Stringify both UUIDs so they match the stored STRING PKs on the ladybug side (core.py:273-283 precedent). Add NO endpoint-existence check — the silent no-op on a missing endpoint is the INTENDED mechanism (D-07), not a bug to fix. Pass the `EdgeType` enum straight through (the backend coerces via `str(...)`). Ensure `EdgeType` is imported in `core.py` (add to the `from doxastica.models import ...` line if not already present). Keep the method driver-blind — no Cypher, no `ladybug` import.
  </action>
  <verify>
    <automated>uv run basedpyright src/doxastica/core.py 2>&1 | tail -5 && uv run pytest tests/test_cascade.py -q -k add_edge 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    `MemoryCore.add_edge` exists with the exact protocol signature; all Task-1 add_edge tests pass on both backends (direction-independent assertions green now; any direction="in" cross-checks green once Plan 01 lands); idempotency and D-07 silent-no-op pinned green; basedpyright strict-clean; `tests/test_import_purity.py` still green (no driver leak).
  </acceptance_criteria>
  <done>Public add_edge is a one-call passthrough inside one unit_of_work; EDGE-01 mechanism green on both backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → `MemoryCore.add_edge` | Input is constrained to the closed `EdgeType` enum + two `UUID`s; no free string reaches storage. |
| core → backend `add_edge` | UUIDs stringified to match STRING PKs; the backend MERGE is idempotent and append-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | `add_edge` writing to an unintended endpoint | accept | D-07: the silent no-op on a missing endpoint is the INTENDED mechanism (mechanism, not policy) — the edge simply is not laid; pinned by a test. Not a vulnerability for an embedded LLM-free core (RESEARCH Security Domain). |
| T-05-04 | Spoofing/Tampering | edge-type seam | mitigate | The public signature takes the closed `EdgeType` enum, so only the three generic types are layable via the public seam; structural `HAS_REVISION`/internal `SUPERSEDES` stays inside `_append_state`. basedpyright-strict rejects a raw string at the type boundary. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — deps pinned/audited in Phases 1–2. |

No new authentication, session, access-control, network, or PII surface (embedded, LLM-free core).
</threat_model>

<verification>
- `uv run pytest tests/test_cascade.py -q -k add_edge` — add_edge mechanism green on both backends.
- `uv run basedpyright src/doxastica/core.py` — strict-clean.
- `uv run pytest tests/test_import_purity.py -q` — driver-blindness preserved.
- `uv run ruff check src/doxastica tests` — clean.
</verification>

<success_criteria>
- `MemoryCore.add_edge` matches the `protocol.py` signature exactly and is a single passthrough inside one `unit_of_work` (D-06).
- Idempotency (double-add ⇒ one edge) verified on both backends; the core adds nothing beyond the passthrough.
- The D-07 silent-no-op-on-missing-endpoint behaviour is pinned by a passing test (not left incidental).
- The public seam takes only the closed `EdgeType` enum.
</success_criteria>

<output>
Create `.planning/phases/05-edge-model-contraction-cascade/05-02-SUMMARY.md` when done.
</output>
