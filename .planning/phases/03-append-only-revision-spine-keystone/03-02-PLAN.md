---
phase: 03-append-only-revision-spine-keystone
plan: 02
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - src/doxastica/core.py
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, CHAIN-01, CHAIN-02, CHAIN-03, OPS-01, OPS-02, OPS-03, HIST-02]
must_haves:
  truths:
    - "get_or_create_scope creates/returns a Scope; '__world__' yields is_world=True; every other id is a peer (is_world=False)"
    - "revise and expand are mechanically identical: append an active BeliefState, link HAS_REVISION, lay SUPERSEDES new->prior when a prior current exists"
    - "contract raises WorldScopeContractionError for '__world__' before any backend access; is a no-op returning None on a belief with no active current; else appends exactly one retracted state copying the prior value verbatim"
    - "current(scope, belief) is DERIVED as the ordering-max over active states; no CURRENT_STATE pointer is stored"
    - "opaque value is json.dumps-encoded at the port boundary and json.loads-decoded on hydrate, so brace-shaped values round-trip byte-identically (DEF-02-01)"
    - "get_revision_chain(belief_id) returns the full immutable chain ordered by (source_event_id, state_id)"
    - "each public write opens exactly one unit_of_work"
  artifacts:
    - path: "src/doxastica/core.py"
      provides: "get_or_create_scope, _current, _append, _ensure_scope, _hydrate, revise, expand, contract, get_revision_chain bodies"
      contains: "def revise"
  key_links:
    - from: "src/doxastica/core.py revise/expand/contract"
      to: "self._backend (BackendPort primitives)"
      via: "upsert_node + add_edge + match_nodes inside unit_of_work"
      pattern: "unit_of_work"
    - from: "src/doxastica/core.py _append"
      to: "json.dumps(value) / _hydrate json.loads"
      via: "value-encoding contract at the core-port boundary"
      pattern: "json\\.(dumps|loads)"
    - from: "src/doxastica/core.py contract"
      to: "WORLD_SCOPE_ID guard"
      via: "structural pre-backend comparison"
      pattern: "WORLD_SCOPE_ID"
---

<objective>
Fill the unwritten AGM operation bodies on the existing `MemoryCore` (Phase 2 left them
unwritten) by composing the five already-shipped LPG primitives, implementing the locked
keystone decisions D-01 (derived current, no stored pointer), D-04 (revise ≡ expand),
D-05 (contract vacuity + retracted-copy), D-06 (auto-create scope+Belief, permissive), and the
DEF-02-01 value-encoding contract (`json.dumps`/`json.loads` at the core↔port boundary).

Purpose: this is the keystone the entire postulate suite stands on — the three write operations,
the derived-current selection (the one place the UUID7 ordering contract lives), and the
append-only HAS_REVISION + SUPERSEDES edge plumbing, all backend-agnostic against the port so they
run identically on both backends. Tests land in 03-03 / 03-04; this plan delivers the behavior they
verify.

Output: `get_or_create_scope`, private `_current` / `_append` / `_ensure_scope` / `_hydrate`
helpers, `revise` / `expand` / `contract`, and `get_revision_chain` bodies on `MemoryCore`.
Driver-blind (no `import ladybug`); adds stdlib `import json` / `import uuid` at module top.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md
@.planning/phases/03-append-only-revision-spine-keystone/03-RESEARCH.md
@.planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md
@src/doxastica/core.py
@src/doxastica/ports.py
@src/doxastica/models.py
@src/doxastica/errors.py
</context>

<artifacts_this_phase_produces>
This plan produces:
- `MemoryCore.get_or_create_scope(scope_id) -> Scope` (SCOPE-01/02/D-02 reserved-id rule).
- `MemoryCore._ensure_scope(scope_id)` — the create-if-absent step reused inside every write (D-06).
- `MemoryCore._current(scope_id, belief_id) -> dict | None` — DERIVED current, the ONE place the
  ordering contract `(source_event_id, state_id)` lives (D-01/DATA-03).
- `MemoryCore._append(scope_id, belief_id, value, source_event_id, status) -> BeliefState` — the
  shared revise/expand body (D-04).
- `MemoryCore._hydrate(props) -> BeliefState` — the value-decode boundary (json.loads).
- `MemoryCore.revise` / `MemoryCore.expand` (D-04 identical) / `MemoryCore.contract` (D-05/D-03).
- `MemoryCore.get_revision_chain(belief_id) -> list[BeliefState]` (HIST-02).
- The DEF-02-01 value-encoding contract (json.dumps on write, json.loads on read; contract copies
  the already-encoded prior value verbatim — no double-encode).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_or_create_scope + _ensure_scope + _current + _hydrate helpers (SCOPE-01/02/03, CHAIN-01, D-01/D-02/D-06)</name>
  <files>src/doxastica/core.py</files>
  <behavior>
    - get_or_create_scope("__world__") returns Scope(scope_id="__world__", is_world=True)
    - get_or_create_scope("alice") returns Scope(scope_id="alice", is_world=False); calling twice is idempotent (one Scope node)
    - _current(scope, belief) returns None when no active state exists for that (scope, belief)
    - _current returns the state with the maximal (str(source_event_id), str(state_id)) among active states for that exact (scope, belief) — never a state from another scope (SCOPE-03 divergence)
    - _hydrate turns a raw props dict (with json-encoded value string) into a frozen BeliefState whose .value is the decoded opaque value and whose state_id/source_event_id are UUIDs
  </behavior>
  <read_first>
    - src/doxastica/core.py (the file being modified — class structure, driver-blind TYPE_CHECKING import block lines 29-39, `self._backend` line 52, the existing `unit_of_work` passthrough lines 97-99; the module docstring line 24 saying op bodies are unwritten — update it)
    - src/doxastica/ports.py (BackendPort primitive signatures — match_nodes(label, where) -> list[dict]; upsert_node(label, node_id, props); the port has NO ORDER-BY/aggregate primitive, so the sort is core-side)
    - src/doxastica/models.py (Scope/Belief/BeliefState/Status frozen models + the WORLD_SCOPE_ID constant added in 03-01; BeliefState closed six-field set; pydantic coerces id strings to UUID at the seam)
    - .planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md (core.py section: get_or_create_scope, _current, _hydrate patterns with exact composition; Pitfall 4 — derive is_world in the core from scope_id == WORLD_SCOPE_ID, do not trust the stored column for the return value)
    - .planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md (D-01 derived current per (scope, belief); D-02 reserved-id rule; D-06 auto-create + permissive)
    - .planning/phases/03-append-only-revision-spine-keystone/03-RESEARCH.md (Pattern 1, Pattern 2; the verified ordering-max key (str(source_event_id), str(state_id)); Pitfall 4 is_world round-trip)
  </read_first>
  <action>
    Add `import json` and `import uuid` at the top of `src/doxastica/core.py` (stdlib, NOT drivers —
    `test_import_purity.py` only forbids `ladybug`). Add runtime imports
    `from doxastica.models import BeliefState, Scope, Status` and `from doxastica.models import WORLD_SCOPE_ID`
    (WORLD_SCOPE_ID is a plain `str`, function-blind, allowed at module top); keep the `BackendPort`
    import type-only under TYPE_CHECKING; NEVER `import ladybug`. Update the module docstring to note
    the Phase-3 op bodies now land here. Then implement on `MemoryCore`:
    (1) `get_or_create_scope(self, scope_id: str) -> Scope`: compute `is_world = scope_id == WORLD_SCOPE_ID`
    (D-02 reserved-id rule); `match_nodes("Scope", {"scope_id": scope_id})`; if absent,
    `upsert_node("Scope", scope_id, {"scope_id": scope_id, "is_world": is_world})`; return
    `Scope(scope_id=scope_id, is_world=is_world)` — DERIVE `is_world` in the core, do NOT trust the
    stored column for the return value (Pitfall 4). Signature unchanged from protocol.py.
    (2) `_ensure_scope(self, scope_id: str) -> None`: the create-if-absent half of get_or_create_scope
    (same reserved-id rule), called INSIDE each write's unit_of_work (D-06); never flips an existing
    scope's is_world (a pure function of scope_id).
    (3) `_current(self, scope_id: str, belief_id: str) -> dict[str, Any] | None`:
    `match_nodes("BeliefState", {"scope_id": scope_id, "belief_id": belief_id, "status": "active"})`;
    return `None` if empty else `max(states, key=lambda s: (str(s["source_event_id"]), str(s["state_id"])))`.
    This is the ONE place the ordering contract lives (DATA-03); document the key. Do NOT sort/aggregate
    in Cypher (no port primitive; breaks parity).
    (4) `_hydrate(self, props: dict[str, Any]) -> BeliefState`: construct a frozen `BeliefState` from
    props with `value=json.loads(props["value"])` (decode boundary) and the other five fields passed
    through (pydantic coerces state_id/source_event_id strings to UUID; status via Status(props["status"])).
    Add `from typing import Any` to the runtime imports if needed for the helper annotations.
    Do NOT write revise/expand/contract/get_revision_chain in this task (Task 2). Do NOT add a
    CURRENT_STATE pointer anywhere (D-01).
  </action>
  <verify>
    <automated>UV_NO_SYNC=1 uv run --extra ladybug pytest tests/test_revision_spine.py::test_get_or_create_scope tests/test_revision_spine.py::test_cross_scope_divergence -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_revision_spine.py::test_get_or_create_scope -x` exits 0 (depends on 03-03 Wave-0 scaffold existing; if scaffold absent at exec time, the verify command surfaces the MISSING test which 03-03 creates first)
    - `src/doxastica/core.py` contains `def get_or_create_scope`, `def _current`, `def _ensure_scope`, `def _hydrate`
    - `src/doxastica/core.py` contains `json.loads` and the ordering key `(str(s["source_event_id"]), str(s["state_id"]))`
    - `src/doxastica/core.py` does NOT contain `import ladybug` (grep: `grep -c 'import ladybug' src/doxastica/core.py` returns 0)
    - `UV_NO_SYNC=1 uv run --extra ladybug basedpyright src/doxastica/core.py` reports no errors
  </acceptance_criteria>
  <done>get_or_create_scope, _ensure_scope, _current, _hydrate are implemented; scope creation applies the reserved-id rule and derives is_world in the core; _current returns the derived ordering-max active state (or None) scoped to the exact (scope, belief); _hydrate decodes the json value; core.py stays driver-blind and basedpyright-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: revise / expand / contract + get_revision_chain bodies (OPS-01/02/03, CHAIN-02/03, HIST-02, D-03/D-04/D-05/D-07)</name>
  <files>src/doxastica/core.py</files>
  <behavior>
    - revise(scope, belief, value, event_id) appends an active BeliefState, links HAS_REVISION, and (when a prior current existed) lays SUPERSEDES new->prior; returns the new BeliefState with the opaque value intact
    - revise on a novel belief_id lays no SUPERSEDES edge (nothing to supersede); revise on an existing belief_id supersedes the prior current
    - expand produces byte-identical behavior to revise for the same inputs (D-04)
    - a brace-shaped value like {"x": 2} round-trips: revise then get_revision_chain returns .value == {"x": 2} on BOTH backends (DEF-02-01)
    - contract("__world__", belief, event_id) raises WorldScopeContractionError and performs NO backend write (even if "__world__" was never created)
    - contract on a belief with no active current returns None and appends no state (vacuity)
    - contract on a belief with an active current appends exactly one status=retracted BeliefState whose stored value is byte-identical to the superseded state's stored value (no double-encode), links HAS_REVISION + SUPERSEDES new(retracted)->prior, returns None
    - get_revision_chain(belief_id) returns every BeliefState for that belief_id across scopes, ordered by (source_event_id, state_id)
  </behavior>
  <read_first>
    - src/doxastica/core.py (the file being modified — now holds the Task-1 helpers _current/_ensure_scope/_hydrate; reuse them)
    - src/doxastica/protocol.py (the locked public signatures to honour exactly: revise/expand/contract/get_revision_chain, lines 71-98, 146-148; the UUID7 ordering contract docstring lines 48-63)
    - src/doxastica/errors.py (WorldScopeContractionError line 17 — the structural guard raises this)
    - .planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md (core.py section: Pattern 3 revise/_append, Pattern 4 contract, Pattern 6 get_revision_chain; the structural-edges-as-raw-strings rule (HAS_REVISION + SUPERSEDES via add_edge as strings); Planner-Grade Flag 4: prefer `expand = revise` class-body alias, fall back to a one-line delegate if basedpyright-strict rejects the alias)
    - .planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md (D-04 revise≡expand five steps; D-05 contract vacuity + retracted-copy + guard-fires-first; D-03 structural pre-backend guard; D-07 HAS_REVISION hub + SUPERSEDES laid on every displacement, passed as raw strings, never EdgeType members)
    - .planning/phases/03-append-only-revision-spine-keystone/03-RESEARCH.md (Pitfall 2 double-encode the contracted value; Pitfall 3 compute prior BEFORE the append; Pitfall 5 single shared _append; Pitfall 6 tiebreak)
  </read_first>
  <action>
    Implement on `MemoryCore`, all composing the port primitives, each public write opening EXACTLY
    ONE `with self._backend.unit_of_work():` (CHAIN-03; never nested, never multiple — Pitfall):
    (1) `_append(self, scope_id, belief_id, value, source_event_id, status) -> BeliefState`: inside
    one unit_of_work — `self._ensure_scope(scope_id)` (D-06); `upsert_node("Belief", belief_id, {"belief_id": belief_id})`
    (D-06 auto-create Belief, idempotent); `prior = self._current(scope_id, belief_id)` computed BEFORE
    the new append (Pitfall 3); `state_id = uuid.uuid7()`; build props
    `{"state_id": str(state_id), "belief_id": belief_id, "scope_id": scope_id, "source_event_id": str(source_event_id), "value": json.dumps(value), "status": status.value}`
    (json.dumps resolves DEF-02-01); `upsert_node("BeliefState", state_id, props)`;
    `add_edge("HAS_REVISION", belief_id, str(state_id))` (hub form, RAW STRING edge type — D-07);
    if `prior is not None`: `add_edge("SUPERSEDES", str(state_id), prior["state_id"])` (raw string);
    `return self._hydrate(props)`.
    (2) `revise(self, scope_id, belief_id, value, source_event_id) -> BeliefState`:
    `return self._append(scope_id, belief_id, value, source_event_id, Status.active)`.
    (3) `expand`: make it mechanically identical to revise (D-04). Prefer the class-body alias
    `expand = revise`; if basedpyright-strict rejects the bare alias (loses the bound-method type),
    define `expand` as an explicit one-line delegate calling `self._append(..., Status.active)` with
    the same signature as protocol.py. Both names MUST stay on the public surface (protocol.py
    requires both; Phase 7 exercises both AGM families).
    (4) `contract(self, scope_id, belief_id, source_event_id) -> None`: FIRST statement (before
    unit_of_work, before any backend access) — `if scope_id == WORLD_SCOPE_ID: raise WorldScopeContractionError(...)`
    (D-03 structural guard fires first, D-05). Then inside one unit_of_work: `self._ensure_scope(scope_id)`;
    `prior = self._current(scope_id, belief_id)`; if `prior is None: return None` (D-05 vacuity, silent
    no-op); else `state_id = uuid.uuid7()`; build props copying `prior["value"]` VERBATIM as the value
    (it is ALREADY the encoded stored string — do NOT json.dumps again, Pitfall 2) with
    `status=Status.retracted.value`; `upsert_node("BeliefState", state_id, props)`;
    `add_edge("HAS_REVISION", belief_id, str(state_id))`; `add_edge("SUPERSEDES", str(state_id), prior["state_id"])`;
    `return None`.
    (5) `get_revision_chain(self, belief_id: str) -> list[BeliefState]`:
    `states = self._backend.match_nodes("BeliefState", {"belief_id": belief_id})` (cross-scope per
    Open Q2 A2 — the signature takes only belief_id); `states.sort(key=lambda s: (str(s["source_event_id"]), str(s["state_id"])))`;
    `return [self._hydrate(s) for s in states]`. Never add HAS_REVISION to the EdgeType enum (D-07).
  </action>
  <verify>
    <automated>UV_NO_SYNC=1 uv run --extra ladybug pytest tests/test_revision_spine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_revision_spine.py -x` exits 0 (all spine behavior tests from 03-03 pass on both backends)
    - `src/doxastica/core.py` contains `def revise`, `def contract`, `def get_revision_chain`, and `def _append`
    - `src/doxastica/core.py` contains `raise WorldScopeContractionError` and `if scope_id == WORLD_SCOPE_ID`
    - `src/doxastica/core.py` contains `json.dumps(value)` and exactly the HAS_REVISION + SUPERSEDES `add_edge` calls (grep: `grep -c 'add_edge' src/doxastica/core.py` >= 3)
    - The contract value-copy does NOT re-encode: `src/doxastica/core.py` uses `prior["value"]` for the retracted props value (not `json.dumps(prior...)`)
    - `UV_NO_SYNC=1 uv run --extra ladybug basedpyright src/doxastica/core.py` reports no errors; `UV_NO_SYNC=1 uv run ruff check src/doxastica/core.py` clean
  </acceptance_criteria>
  <done>revise/expand (identical, D-04), contract (guard-first, vacuity, retracted-copy verbatim), and get_revision_chain are implemented composing the port primitives; each write opens exactly one unit_of_work; HAS_REVISION + SUPERSEDES are laid as raw strings; value is json-encoded once on write and copied verbatim on contract; core.py is driver-blind, basedpyright-strict-clean, ruff-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → MemoryCore | scope_id / belief_id / value / source_event_id cross the public seam |
| MemoryCore → BackendPort | values pass to upsert_node/add_edge as bind data (never Cypher text) |
| value encode/decode | opaque `value` crosses the json.dumps/json.loads integrity boundary (DEF-02-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | opaque `value` round-trip integrity (DEF-02-01) | mitigate | core json.dumps on write / json.loads on read, applied identically on both backends; contract copies the already-encoded stored form verbatim (no double-encode); brace-shaped values verified to round-trip byte-identically. Closes the inherited ladybug brace-coercion corruption |
| T-03-04 | Tampering / Elevation | `value` as an execution surface | mitigate | the core NEVER eval's/exec's the value and NEVER builds a query from it — it is `json.dumps`-encoded and handed to the port as a STRING bind only (BACK-04 §6); pure data |
| T-03-05 | Tampering | scope_id / belief_id injection | accept | these are passed to upsert_node/add_edge as `$param` bind values (Phase-2 invariant); the core writes NO Cypher; a scope named `"; DROP …"` is inert data |
| T-03-06 | Tampering (append-only bypass) | revise/expand/contract | mitigate | every op is a pure append (upsert_node on a never-reused uuid7 state_id PK + add_edge MERGE); the port exposes no delete/replace primitive, so no op can rewrite a BeliefState or HAS_REVISION edge (CHAIN-02); world-scope contract is structurally refused before any write (D-03) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH Package Legitimacy Audit: zero new deps; stdlib json/uuid only) |
</threat_model>

<verification>
- `UV_NO_SYNC=1 uv run --extra ladybug pytest tests/test_revision_spine.py` — spine behavior green on both backends.
- `UV_NO_SYNC=1 uv run --extra ladybug pytest` — full suite green (no regression).
- `UV_NO_SYNC=1 uv run --extra ladybug basedpyright src` + `uv run ruff check src` — strict + lint clean.
- `UV_NO_SYNC=1 uv run python -c "from doxastica import MemoryCore; c=MemoryCore.in_memory(); s=c.revise('a','b',{'x':2}, __import__('uuid').uuid7()); assert s.value=={'x':2}"` — brace value round-trips on the base (pydantic-only) install.
</verification>

<success_criteria>
- All three write ops behave per D-04/D-05; current is derived per D-01 (no stored pointer).
- World-scope contract raises before any write; vacuous contract is a no-op returning None.
- Brace-shaped values round-trip byte-identically (DEF-02-01 closed) on both backends.
- get_revision_chain returns the full ordered immutable chain.
- core.py is driver-blind, basedpyright-strict-clean, ruff-clean.
</success_criteria>

<output>
Create `.planning/phases/03-append-only-revision-spine-keystone/03-02-SUMMARY.md` when done.
</output>
