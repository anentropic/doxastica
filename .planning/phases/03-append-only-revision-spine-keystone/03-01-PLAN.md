---
phase: 03-append-only-revision-spine-keystone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/doxastica/models.py
  - src/doxastica/__init__.py
  - src/doxastica/backends/ladybug.py
autonomous: true
requirements: [SCOPE-02, CHAIN-02, HIST-02]
must_haves:
  truths:
    - "WORLD_SCOPE_ID is importable as `from doxastica import WORLD_SCOPE_ID` and equals the literal string '__world__'"
    - "The ladybug backend bootstraps a HAS_REVISION REL table (hub form Belief -> BeliefState) idempotently"
    - "ladybug add_edge can lay a HAS_REVISION edge whose FROM endpoint is a Belief (keyed belief_id), not a BeliefState"
    - "No CURRENT_STATE REL table is created (derived current, D-01)"
  artifacts:
    - path: "src/doxastica/models.py"
      provides: "WORLD_SCOPE_ID = '__world__' module constant"
      contains: "WORLD_SCOPE_ID"
    - path: "src/doxastica/__init__.py"
      provides: "WORLD_SCOPE_ID re-export in __all__"
      contains: "WORLD_SCOPE_ID"
    - path: "src/doxastica/backends/ladybug.py"
      provides: "HAS_REVISION REL table in _bootstrap_schema + endpoint-label-aware add_edge"
      contains: "HAS_REVISION"
  key_links:
    - from: "src/doxastica/backends/ladybug.py add_edge"
      to: "_EDGE_ENDPOINTS / _PK_BY_LABEL"
      via: "per-edge-type endpoint label + PK lookup"
      pattern: "HAS_REVISION"
---

<objective>
Lay the structural foundation the Phase-3 op bodies (plan 03-02) compose against, with zero
AGM logic: (1) the reserved world-scope constant `WORLD_SCOPE_ID = "__world__"` (D-02), exported
from the package barrel because NVM imports it; (2) the `HAS_REVISION` REL table on the ladybug
backend in hub form `Belief -> BeliefState` (D-07); and (3) the load-bearing generalization of the
ladybug `add_edge` so it can lay an edge whose FROM endpoint is a `Belief` (keyed `belief_id`) and
not the hardcoded `BeliefState` (keyed `state_id`) — the PATTERNS.md flag-1 source edit.

Purpose: this is interface-first ordering — the constant and the schema/edge plumbing must exist
before the op bodies in 03-02 reference them, so the executor of 03-02 receives working contracts
rather than discovering them. No `CURRENT_STATE` table is created — current is derived (D-01).

Output: `WORLD_SCOPE_ID` constant + barrel export; `HAS_REVISION` REL table DDL; an
endpoint-label-aware `add_edge` on the ladybug adapter. No `core.py` op bodies (that is 03-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md
@.planning/phases/03-append-only-revision-spine-keystone/03-RESEARCH.md
@.planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (the rest land in 03-02 / 03-03 / 03-04):
- `WORLD_SCOPE_ID = "__world__"` module constant (in `models.py`) + `__init__.py` re-export.
- `HAS_REVISION` ladybug REL table (hub shape `FROM {ns}_Belief TO {ns}_BeliefState`).
- An endpoint-label-aware ladybug `add_edge` (a `_EDGE_ENDPOINTS` map, parallel to `_PK_BY_LABEL`)
  so `HAS_REVISION` (Belief->BeliefState) and the structural-edge family (BeliefState->BeliefState)
  both resolve correct FROM/TO labels + PK columns.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Define and export WORLD_SCOPE_ID constant (D-02)</name>
  <files>src/doxastica/models.py, src/doxastica/__init__.py</files>
  <read_first>
    - src/doxastica/models.py (the file being modified — StrEnum/constant declaration style, lines 24-55; the constant lives here alongside the closed taxonomy per PATTERNS flag 2)
    - src/doxastica/__init__.py (the file being modified — existing import block + `__all__` re-export pattern, lines 3-35)
    - .planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md (D-02: reserved constant; value is dunder-wrapped "__world__"; NVM imports it; `get_or_create_scope` signature unchanged)
    - .planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md (Planner-Grade Flag 2: constant lives in models.py vs constants.py — decision: models.py; barrel re-export shape)
  </read_first>
  <action>
    Add the module-level constant `WORLD_SCOPE_ID: str = "__world__"` to `src/doxastica/models.py`
    (place it near the top after the imports / before or after the `Status` enum, matching the
    file's existing declaration style). The value MUST be exactly the dunder-wrapped string
    `"__world__"` per D-02 (deliberately chosen over bare `"world"` to avoid collision with a
    caller-chosen scope literally named "world"). Add a one-line docstring/comment citing D-02 and
    that it is the reserved id whose `get_or_create_scope` yields `Scope(is_world=True)`. Then in
    `src/doxastica/__init__.py`, import `WORLD_SCOPE_ID` from `doxastica.models` (add it to the
    existing `from doxastica.models import (...)` block) and add the string literal `"WORLD_SCOPE_ID"`
    to `__all__`, keeping the existing alphabetical ordering. Do NOT change the
    `get_or_create_scope` signature anywhere (it stays `(scope_id: str) -> Scope`, D-02). Do NOT
    create a separate `constants.py` (decision: models.py is the home, PATTERNS flag 2).
  </action>
  <verify>
    <automated>UV_NO_SYNC=1 uv run python -c "from doxastica import WORLD_SCOPE_ID; assert WORLD_SCOPE_ID == '__world__', WORLD_SCOPE_ID; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from doxastica import WORLD_SCOPE_ID; assert WORLD_SCOPE_ID == '__world__'"` exits 0
    - `src/doxastica/models.py` contains the token `WORLD_SCOPE_ID`
    - `src/doxastica/__init__.py` contains `"WORLD_SCOPE_ID"` inside `__all__`
    - The `get_or_create_scope` signature in `src/doxastica/protocol.py` is unchanged (no `is_world` parameter added)
  </acceptance_criteria>
  <done>`WORLD_SCOPE_ID` is defined in models.py, re-exported via __init__ `__all__`, equals "__world__", and is importable as `from doxastica import WORLD_SCOPE_ID`.</done>
</task>

<task type="auto">
  <name>Task 2: Add HAS_REVISION REL table + generalize ladybug add_edge endpoints (D-07, PATTERNS flag 1)</name>
  <files>src/doxastica/backends/ladybug.py</files>
  <read_first>
    - src/doxastica/backends/ladybug.py (the file being modified — `_bootstrap_schema` lines 144-176 with the existing edge-table loop; `add_edge` lines 212-232 which hardcodes both endpoints to `{ns}_BeliefState` keyed `state_id`; `_PK_BY_LABEL` line 86)
    - .planning/phases/03-append-only-revision-spine-keystone/03-PATTERNS.md (the ladybug.py section + Planner-Grade Flag 1: hub-form HAS_REVISION FROM-endpoint is a Belief keyed belief_id; add_edge must generalize endpoint labels/PKs OR pick chain-link — CONTEXT default is hub)
    - .planning/phases/03-append-only-revision-spine-keystone/03-CONTEXT.md (D-07: add HAS_REVISION REL table IF NOT EXISTS; SUPERSEDES/DEPENDS_ON/DERIVED_FROM tables already exist; CURRENT_STATE is NOT created; HAS_REVISION passed to add_edge as a RAW STRING, never an EdgeType enum member)
    - .planning/phases/03-append-only-revision-spine-keystone/03-RESEARCH.md (Open Q1 recommendation: HUB form `FROM Belief TO BeliefState`; Code Examples — the HAS_REVISION DDL snippet)
    - src/doxastica/backends/memory.py (the in-memory add_edge is already label-agnostic — keys on raw str(from_id) — so it needs NO change; confirm only)
  </read_first>
  <action>
    In `src/doxastica/backends/ladybug.py`, two changes, both preserving the existing
    `IF NOT EXISTS` idempotency and the rule that `{ns}` is the ONLY interpolated identifier
    (validated by `_NS_RE` in `__init__`). (1) In `_bootstrap_schema`, add a new `self._exec`
    creating the hub-form HAS_REVISION table:
    `CREATE REL TABLE IF NOT EXISTS {ns}_HAS_REVISION(FROM {ns}_Belief TO {ns}_BeliefState)`.
    Add it as its own `_exec` (NOT inside the `SUPERSEDES/DEPENDS_ON/DERIVED_FROM` loop, whose
    FROM/TO is BeliefState->BeliefState). Place it after the BeliefState node table and the edge
    loop. Do NOT add a `CURRENT_STATE` table (D-01). Update the method docstring line ~152
    ("HAS_REVISION / CURRENT_STATE arrive in Phase 3") to state that only HAS_REVISION lands.
    (2) Generalize `add_edge` so its endpoint labels + PK columns are looked up per edge_type
    instead of hardcoded to `{ns}_BeliefState`/`state_id`. Introduce a module-level constant
    `_EDGE_ENDPOINTS` (parallel to `_PK_BY_LABEL`) mapping each edge-type STRING to a
    `(from_label, to_label)` pair: `"HAS_REVISION" -> ("Belief", "BeliefState")` and the three
    EdgeType members `"SUPERSEDES"/"DEPENDS_ON"/"DERIVED_FROM" -> ("BeliefState", "BeliefState")`.
    In `add_edge`, resolve `from_label, to_label` from `_EDGE_ENDPOINTS[str(edge_type)]`, then build
    the MATCH using each endpoint's own label and PK column from `_PK_BY_LABEL` (so the FROM clause
    matches `(a:{ns}_Belief {belief_id: $from})` for HAS_REVISION and `(a:{ns}_BeliefState {state_id: $from})`
    for the structural edges). Keep both endpoint ids as `$param` binds; keep `MERGE` idempotency
    (double-MERGE = 1 edge). Accept `edge_type` as `EdgeType | str` unchanged (HAS_REVISION arrives
    as a raw string — never an enum member, D-07). Do NOT edit `memory.py` (its add_edge is already
    label-agnostic) unless a parity check in plan 03-03 reveals a gap.
  </action>
  <verify>
    <automated>UV_NO_SYNC=1 uv run --extra ladybug python -c "
import ladybug as lb
from doxastica.backends.ladybug import LadybugBackend
be = LadybugBackend(lb.Connection(lb.Database()), namespace='dx', owns_conn=True)
be.upsert_node('Belief', 'b1', {'belief_id': 'b1'})
be.upsert_node('BeliefState', 's1', {'state_id': 's1', 'belief_id': 'b1', 'scope_id': 'sc', 'source_event_id': 'e1', 'value': '1', 'status': 'active'})
be.add_edge('HAS_REVISION', 'b1', 's1')
print('hub edge ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - The probe command above exits 0 and prints `hub edge ok` (a Belief->BeliefState HAS_REVISION edge is laid without error)
    - `src/doxastica/backends/ladybug.py` contains `HAS_REVISION` and `_EDGE_ENDPOINTS`
    - `src/doxastica/backends/ladybug.py` does NOT contain the token `CURRENT_STATE` in a `CREATE REL TABLE` statement (grep: `grep -v '^[[:space:]]*#' src/doxastica/backends/ladybug.py | grep 'CREATE REL TABLE' | grep -c CURRENT_STATE` returns 0)
    - `UV_NO_SYNC=1 uv run --extra ladybug basedpyright src/doxastica/backends/ladybug.py` reports no new errors
  </acceptance_criteria>
  <done>The ladybug backend bootstraps a hub-form HAS_REVISION REL table idempotently, its add_edge resolves per-edge-type endpoint labels/PKs (Belief->BeliefState for HAS_REVISION; BeliefState->BeliefState for the structural family), no CURRENT_STATE table is created, and basedpyright stays clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core/caller → ladybug DDL | namespace string is interpolated into `CREATE REL TABLE` / `MATCH` Cypher |
| caller belief/scope ids → ladybug binds | `from_id` / `to_id` cross into Cypher as edge endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ladybug `_bootstrap_schema` HAS_REVISION DDL | mitigate | `{ns}` is the ONLY interpolated identifier and is `_NS_RE`-validated in `__init__` (Phase-2 control, unchanged); the rest of the new DDL is fixed structural text — no caller data interpolated |
| T-03-02 | Tampering | ladybug `add_edge` generalized endpoints | mitigate | `from_id`/`to_id` remain `$param` binds; only the validated namespace + fixed endpoint labels (from the closed `_EDGE_ENDPOINTS` map) + edge-type label are interpolated — an edge endpoint id of `"; DROP …"` is inert bind data |
| T-03-AO | Tampering (append-only bypass) | new REL table + add_edge | accept | No edge-delete/replace primitive is added; HAS_REVISION is MERGE-append-only by construction (the no-edge-delete invariant that drives D-01 holds) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH Package Legitimacy Audit: zero new deps); nothing to gate |
</threat_model>

<verification>
- `UV_NO_SYNC=1 uv run --extra ladybug pytest` — the existing suite (parity + bootstrap idempotency) stays green after the schema + add_edge changes (no regression to SUPERSEDES/DEPENDS_ON/DERIVED_FROM edges).
- `UV_NO_SYNC=1 uv run --extra ladybug basedpyright src` — strict typing clean.
- `UV_NO_SYNC=1 uv run ruff check src` — lint clean.
</verification>

<success_criteria>
- `from doxastica import WORLD_SCOPE_ID` works and equals `"__world__"`.
- The ladybug backend creates a hub-form HAS_REVISION REL table idempotently and lays a
  Belief->BeliefState edge via `add_edge("HAS_REVISION", belief_id, state_id)`.
- No `CURRENT_STATE` REL table is created.
- Existing tests + basedpyright strict + ruff stay green.
</success_criteria>

<output>
Create `.planning/phases/03-append-only-revision-spine-keystone/03-01-SUMMARY.md` when done.
</output>
