---
phase: 01-protocol-backend-port-data-model-decisions
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/doxastica/ports.py
  - docs/backend-contract.md
  - tests/test_port_distinct.py
autonomous: true
requirements: [BACK-01, BACK-04]
user_setup: []

must_haves:
  truths:
    - "the internal backend port is a distinct typing.Protocol, separate from BeliefStore"
    - "the port exposes only LPG primitives — no run/query/execute method taking a query string"
    - "traverse is the single generic graph-walk primitive returning (reached, frontier)"
    - "docs/backend-contract.md enumerates the constraints a third-party LPG backend must meet"
  artifacts:
    - path: "src/doxastica/ports.py"
      provides: "Internal LPG-primitive BackendPort Protocol (BACK-01 decided granularity)"
      contains: "class BackendPort(Protocol)"
      min_lines: 20
    - path: "docs/backend-contract.md"
      provides: "BACK-04 drafted port-contract spec — the 7 constraints a backend must satisfy"
      contains: "## "
      min_lines: 30
    - path: "tests/test_port_distinct.py"
      provides: "BACK-01 guard — port is a separate Protocol with no query-string method"
      contains: "BackendPort"
  key_links:
    - from: "src/doxastica/ports.py"
      to: "traverse primitive"
      via: "single generic graph-walk method"
      pattern: "def traverse"
---

<objective>
Author the internal **LPG-primitive backend port** `Protocol` (BACK-01 — the decided
granularity, recorded) and the drafted backend-port contract spec (BACK-04). This is the
second, distinct seam below the public `BeliefStore`: graph primitives (`upsert_node` /
`add_edge` / `match_nodes` / one generic `traverse` / `unit_of_work`), never Cypher, never a
query string. The named `get_impact`/`get_scope_at` round-trip tension is recorded inline and
flagged for the Phase 2 spike.

Purpose: Two seams must be explicit and distinct in code — the public Protocol (NVM-core) and
this internal port the backend-agnostic core writes against. Choosing LPG-primitive over
Cypher-level is a decision whose reversal is a rewrite; this plan makes it real in types and in
a written contract. No adapters, no behavior (adapters are Phase 2).
Output: `src/doxastica/ports.py`, `docs/backend-contract.md`, `tests/test_port_distinct.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-CONTEXT.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md
@src/doxastica/models.py
</context>

<artifacts_this_phase_produces>
This plan produces these symbols/docs:
- `src/doxastica/ports.py`:
  - `BackendPort(Protocol)` with methods: `upsert_node(label: str, node_id: UUID | str, props: dict[str, Any]) -> None`;
    `add_edge(edge_type: EdgeType | str, from_id: UUID | str, to_id: UUID | str, props: dict[str, Any] | None = None) -> None`;
    `match_nodes(label: str, where: dict[str, Any]) -> list[dict[str, Any]]`;
    `traverse(start: UUID | str, edge_types: frozenset[EdgeType | str], max_depth: int | None) -> tuple[list[dict[str, Any]], frozenset[UUID]]`;
    `unit_of_work() -> AbstractContextManager[None]`
  - An inline note recording the LPG-primitive decision (BACK-01) and the
    `get_impact`/`get_scope_at` round-trip tension flagged for the Phase 2 spike
- `docs/backend-contract.md`: the BACK-04 drafted spec — the 7 constraints (data model;
  primitive op semantics; PK uniqueness; append-only safety; ordering left to core; value
  opacity; conformance via the Phase 7 suite)
- `tests/test_port_distinct.py`: asserts `BackendPort` is a distinct `Protocol` from
  `BeliefStore` and exposes no `run`/`query`/`execute` method
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the LPG-primitive BackendPort Protocol</name>
  <files>src/doxastica/ports.py</files>
  <read_first>
    - src/doxastica/models.py (the `EdgeType` enum the port references)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("ports.py" section — the verbatim BackendPort block + anti-patterns)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md (lines 402-427 BackendPort code example; Pattern 2 + Pitfall 3)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-CONTEXT.md (decision #1 LPG-primitive granularity + named tension)
  </read_first>
  <action>
    Create `src/doxastica/ports.py` with a module docstring. Import from `contextlib`
    (`AbstractContextManager`), `typing` (`Any`, `Protocol`), `uuid` (`UUID`), and
    `doxastica.models` (`EdgeType`). Define `class BackendPort(Protocol):` with EXACTLY these five
    primitive methods (ellipsis bodies, no implementation): `upsert_node(label, node_id, props)`,
    `add_edge(edge_type, from_id, to_id, props=None)`, `match_nodes(label, where) -> list[dict]`,
    `traverse(start, edge_types: frozenset[EdgeType | str], max_depth: int | None) ->
    tuple[list[dict[str, Any]], frozenset[UUID]]` (the SINGLE generic graph-walk primitive —
    returns `(reached, frontier)`; `get_impact` and `get_scope_at` will compose from it in
    Phases 3+), and `unit_of_work() -> AbstractContextManager[None]` (atomic write transaction).
    Do NOT add any `run`/`query`/`execute` method, any method taking a Cypher/query string, or any
    arbitrary-property-predicate method — those are the declined Repository/generic-GraphStore
    anti-pattern (Pitfall 3). Record, as a module-level docstring/comment, the LPG-primitive
    decision (BACK-01) and the named tension: under LPG-primitive, `get_impact`/`get_scope_at` may
    cost more round-trips than one hand-written Cypher query — flagged for confirmation in the
    Phase 2 spike (SC4); the port adjusts then if the spike demands it. The signatures use only
    generic LPG vocabulary (label/node_id/props/edge_types) — no `ladybug`/Cypher import.
  </action>
  <verify>
    <automated>uv run basedpyright src/doxastica/ports.py && uv run ruff check src/doxastica/ports.py && grep -q 'class BackendPort(Protocol)' src/doxastica/ports.py && grep -q 'def traverse' src/doxastica/ports.py && ! grep -Eq 'def (run|query|execute)\b' src/doxastica/ports.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run basedpyright` passes strict on `ports.py`
    - `uv run ruff check src/doxastica/ports.py` is clean
    - `ports.py` defines `class BackendPort(Protocol)` with exactly the five primitive methods
    - `traverse` is present and returns the `(reached, frontier)` tuple shape
    - No `run`/`query`/`execute` method and no method taking a query string exists
    - The LPG-primitive decision and the Phase-2-spike round-trip tension are recorded in the source
    - No `import ladybug` anywhere in the file
  </acceptance_criteria>
  <done>The internal LPG-primitive `BackendPort` Protocol exists as a distinct seam with only graph primitives, `traverse` as the single graph-walk, and the decision + tension recorded (BACK-01).</done>
</task>

<task type="auto">
  <name>Task 2: Draft the backend-port contract spec and the distinctness guard</name>
  <files>docs/backend-contract.md, tests/test_port_distinct.py</files>
  <read_first>
    - src/doxastica/ports.py (the just-authored port — keys the contract to its methods)
    - src/doxastica/protocol.py (to assert distinctness from BeliefStore)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md (lines 488-503 "Backend Port Contract" — the 7 constraints; Open Q3 format recommendation)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("docs/backend-contract.md" section)
  </read_first>
  <action>
    Create `docs/backend-contract.md` as prose Markdown keyed to the `BackendPort` methods,
    enumerating the SEVEN constraints a third-party LPG backend must satisfy (each under its own
    `##` heading or numbered list item): (1) Data model — a labelled property graph (nodes with a
    label + id-keyed property map; typed directed edges with optional property maps); (2) Primitive
    op semantics — `upsert_node` idempotent on node id (never duplicates), `add_edge` append-only
    in practice (core never deletes edges), `match_nodes` exact-match AND-combined predicate (no
    query language exposed), `traverse(start, edge_types, max_depth)` returns the de-duplicated
    cycle-safe reachable-node SET plus the frontier of nodes left unexpanded at `max_depth` with
    `max_depth=None` meaning full transitive closure and empty frontier (MUST terminate on cyclic
    graphs), `unit_of_work()` an atomic all-or-nothing write transaction; (3) Uniqueness — backend
    enforces it on node primary id (LadybugDB via PRIMARY KEY; no UNIQUE constraint needed);
    (4) Append-only safety — the core never deletes; contraction marks `status='retracted'`;
    (5) Ordering — the core orders results itself by `(source_event_id byte-order, state_id
    tiebreak)`, the backend need not order but must return all matches; (6) Opacity — node `value`
    properties are opaque blobs stored/returned verbatim, never interpreted; (7) Conformance — the
    backend passes the parameterised AGM/Hansson + structural-invariant suite (BACK-05, Phase 7),
    the executable form of this contract. Note the doc is authored here and published consumer-facing
    in Phase 8 (PKG-04). Then create `tests/test_port_distinct.py` (template test style): assert
    `BackendPort` and `BeliefStore` are distinct objects/Protocols (not the same class), and assert
    `BackendPort` has none of the attribute names `run`, `query`, `execute` (the declined-anti-pattern
    guard for BACK-01).
  </action>
  <verify>
    <automated>test -f docs/backend-contract.md && grep -c '^## \|^[0-9]\.' docs/backend-contract.md && uv run pytest tests/test_port_distinct.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/backend-contract.md` exists and enumerates all seven constraints (data model, primitive op semantics, uniqueness, append-only safety, ordering, opacity, conformance)
    - The doc references the `traverse` cycle-safe reachable-set + `max_depth=None` full-closure semantics
    - `uv run pytest tests/test_port_distinct.py` passes
    - The test asserts `BackendPort` is distinct from `BeliefStore` and has no `run`/`query`/`execute` attribute
    - `uv run ruff check tests/test_port_distinct.py` is clean
  </acceptance_criteria>
  <done>The BACK-04 contract spec is drafted (7 constraints keyed to the port methods) and the BACK-01 distinctness/primitives-only guard passes mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core logic → backend port | The internal seam the backend-agnostic core writes against; a backend dialect (Cypher) must NOT cross upward into the core logic layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-DIALECT | Tampering | `BackendPort` method surface | mitigate (by design) | BACK-01: the port exposes only LPG primitives — no `run`/`query`/`execute(str)` method. A query-string passthrough would re-open the Cypher-injection surface DATA-02 designed out and couple the "backend-agnostic" core to a dialect. The `! grep run|query|execute` gate + the distinctness test enforce primitives-only. |
| T-01-OPAQUE2 | Tampering | port `props`/`value` blobs | accept (by design) | The contract mandates node `value` blobs are stored/returned verbatim and never interpreted by the backend — opacity is the control; no eval, no query-construction from value content. |

Note: zero-network, zero-auth, no adapter/storage code (adapters are Phase 2). The port shape
itself is the mitigation — it makes a Cypher passthrough unrepresentable at the seam.
</threat_model>

<verification>
- `uv run basedpyright` passes strict on `ports.py`
- `uv run ruff check .` clean
- `uv run pytest tests/test_port_distinct.py -q` passes (BACK-01 guard)
- `docs/backend-contract.md` enumerates the 7 constraints (BACK-04)
- No `run`/`query`/`execute` method on the port; no `import ladybug`
</verification>

<success_criteria>
Two seams are explicit and distinct in code: the public `BeliefStore` Protocol and a separate
internal `BackendPort` (BACK-01); the port granularity is DECIDED and recorded as LPG-primitive
with the `get_impact`/`get_scope_at` round-trip tension flagged for the Phase 2 spike; and the
backend port contract is drafted as a written spec enumerating the constraints a third-party LPG
backend must meet (BACK-04, published Phase 8 via PKG-04).
</success_criteria>

<output>
Create `.planning/phases/01-protocol-backend-port-data-model-decisions/01-04-SUMMARY.md` when done
</output>
