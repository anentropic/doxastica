---
phase: 01-protocol-backend-port-data-model-decisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .github/workflows/quality.yml
  - .github/workflows/release.yml
  - .github/workflows/weekly.yml
  - src/doxastica/__init__.py
  - src/doxastica/py.typed
  - tests/test_doxastica.py
  - .pre-commit-config.yaml
  - mkdocs.yml
  - uv.lock
autonomous: false
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "uv sync resolves and the doxastica package imports under Python 3.14"
    - "uv build produces a wheel/sdist for import name doxastica"
    - "basedpyright strict and ruff check both pass on the empty scaffold"
    - "runtime dependencies are exactly ladybug + pydantic; the token ladybugdb appears nowhere"
    - "requires-python is >=3.14 and CI matrices target 3.14 only"
    - "hypothesis is present in the dev dependency group"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata, exact runtime deps, dev group with hypothesis, requires-python >=3.14, basedpyright strict + ruff config"
      contains: "requires-python = \">=3.14\""
    - path: "src/doxastica/__init__.py"
      provides: "Package init with module docstring and __all__ barrel"
      contains: "__all__"
    - path: "src/doxastica/py.typed"
      provides: "PEP 561 type marker so strict consumers see doxastica as typed"
  key_links:
    - from: "pyproject.toml"
      to: "ladybug + pydantic"
      via: "dependencies array"
      pattern: "ladybug>=0.17.*pydantic|pydantic.*ladybug>=0.17"
---

<objective>
Run the `cookiecutter-python-uv-library` scaffold under import name `doxastica` (PKG-01)
and patch the rendered output to the locked CONTEXT decisions: runtime deps EXACTLY
`pydantic` + `ladybug` (never `ladybugdb`), Python floor raised to `>=3.14`, `hypothesis`
added to the dev group, and CI matrices reduced to 3.14-only. This is the foundation every
other Phase 1 plan depends on — a type-checkable, ruff-clean, buildable empty package.

Purpose: Nothing else in Phase 1 can exist until a basedpyright-strict-checkable package
tree exists. This plan writes ZERO belief/storage code — only scaffold + config.
Output: A green empty scaffold: `src/doxastica/` (init + py.typed), `tests/`,
`pyproject.toml`, CI workflows, pre-commit, docs config, `uv.lock`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-CONTEXT.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan produces (no symbols — scaffold/config only):
- File: `pyproject.toml` (patched: `requires-python>=3.14`, `dependencies=["pydantic>=2.11,<3","ladybug>=0.17,<0.18"]`, dev-group `hypothesis>=6.155`, `[tool.pyright]` strict, ruff `py314`)
- File: `src/doxastica/__init__.py` (docstring + empty `__all__`)
- File: `src/doxastica/py.typed` (PEP 561 marker, copied verbatim by scaffold)
- File: `tests/test_doxastica.py` (template smoke test)
- Files: `.github/workflows/{quality,release,weekly}.yml` (3.14-only matrix)
- Files: `.python-version`, `.pre-commit-config.yaml`, `mkdocs.yml`, `uv.lock`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Render the cookiecutter scaffold under import name doxastica</name>
  <files>pyproject.toml, .python-version, src/doxastica/__init__.py, src/doxastica/py.typed, tests/test_doxastica.py, .pre-commit-config.yaml, mkdocs.yml, .github/workflows/quality.yml, .github/workflows/release.yml, .github/workflows/weekly.yml</files>
  <read_first>
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("Scaffold prompt answers" + "Scaffold Patch List" sections)
    - CLAUDE.md (tech-stack pins, the ladybug-not-ladybugdb warning)
    - /Users/paul/Documents/Dev/Personal/cookiecutter-python-uv-library/cookiecutter.json
  </read_first>
  <action>
    The doxastica repo already exists with `.planning/`, `CLAUDE.md`, `LICENSE`, `README.md`
    and git history — do NOT let cookiecutter create a nested subdirectory or overwrite these.
    Render the template at /Users/paul/Documents/Dev/Personal/cookiecutter-python-uv-library
    into a temporary location with these prompt answers: `project_name`="doxastica" (yields
    `project_slug`=`doxastica`, `package_name`=`doxastica`), `minimum_python_version`="3.14"
    (OVERRIDE the template default of 3.11 per CONTEXT decision #2 — this makes
    `requires-python`, both CI matrices, weekly.yml, ruff `target-version`, and `.python-version`
    render correctly), `python_version`="3.14", `license`="MIT", `docs_framework`="mkdocs-material",
    `project_short_description`="Graph-native AGM belief-revision core (Kumiho M0)". Then copy the
    rendered tree into the repo root WITHOUT clobbering the existing `LICENSE`, `README.md`, or
    `.planning/`. Use `uvx cookiecutter` (or `uv tool run cookiecutter`) with `--no-input` and
    `--output-dir` pointing at a temp dir, then move files in. Run `uv sync` afterward to
    generate `uv.lock`. Do NOT yet edit dependencies — that is Task 2.
  </action>
  <verify>
    <automated>test -f src/doxastica/__init__.py && test -f src/doxastica/py.typed && test -f pyproject.toml && grep -q 'requires-python = ">=3.14"' pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/doxastica/__init__.py`, `src/doxastica/py.typed`, `pyproject.toml`, `tests/` all exist
    - `pyproject.toml` contains `requires-python = ">=3.14"` (NOT 3.11)
    - The existing `LICENSE`, `README.md`, and `.planning/` directory are untouched
    - `.github/workflows/quality.yml` Python matrix is `["3.14"]` (no 3.11/3.13)
    - No nested `doxastica/doxastica/` directory was created
  </acceptance_criteria>
  <done>The scaffold tree is rendered into the repo root under import name `doxastica` with the 3.14 floor baked in by the cookiecutter prompt answer; pre-existing files preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Pin runtime deps to ladybug + pydantic and add hypothesis to dev</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the just-rendered file — read its current `dependencies`, `[dependency-groups]`, `[tool.ruff]`, `[tool.pyright]` tables)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md ("Standard Stack" table + "Package Legitimacy Audit")
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("Scaffold Patch List")
  </read_first>
  <action>
    Edit `pyproject.toml`: set `dependencies = ["pydantic>=2.11,<3", "ladybug>=0.17,<0.18"]`
    (the ONLY two runtime deps — verify the literal token `ladybugdb` appears NOWHERE; the PyPI
    installable is `ladybug`, a Kùzu fork — `ladybugdb` is a 404/SLOP package). Add
    `hypothesis>=6.155` to the `[dependency-groups]` `dev` array (declared now, exercised in
    Phase 7). Confirm `[tool.pyright]` has `typeCheckingMode = "strict"`, `pythonVersion = "3.14"`,
    `include = ["src","tests"]`; confirm `[tool.ruff]` `target-version = "py314"`. Do NOT add a
    `[[tool.basedpyright.executionEnvironments]]` block — that is a Phase 2 concern (the ladybug
    adapter boundary); Phase 1 imports no ladybug so strict passes clean. Run `uv sync` to refresh
    `uv.lock` with the new deps. If PyPI resolution fails on the version pins, that is the
    network-verification gate handled in Task 3's checkpoint — leave the pins as specified.
  </action>
  <verify>
    <automated>grep -q 'pydantic>=2.11' pyproject.toml && grep -q 'ladybug>=0.17' pyproject.toml && grep -q 'hypothesis>=6.155' pyproject.toml && ! grep -q 'ladybugdb' pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dependencies` in `pyproject.toml` is exactly the two entries `pydantic>=2.11,<3` and `ladybug>=0.17,<0.18`
    - The token `ladybugdb` does not appear anywhere in `pyproject.toml`
    - `hypothesis>=6.155` is in the dev dependency group
    - `[tool.pyright]` retains `typeCheckingMode = "strict"` and `pythonVersion = "3.14"`
    - No `[[tool.basedpyright.executionEnvironments]]` block was added
  </acceptance_criteria>
  <done>Runtime deps are pinned to exactly `ladybug` + `pydantic`, `hypothesis` is in the dev group, and `uv.lock` reflects them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm dependency pins resolve and the 3.14 floor holds</name>
  <files>pyproject.toml</files>
  <action>
    PAUSE for human verification. This is a blocking checkpoint AFTER Tasks 1-2 have rendered the
    scaffold and pinned deps. Present the two items below to the user and wait for explicit
    approval before the phase proceeds; the executor automates everything it can first (runs
    `uv sync`, `uv run basedpyright`, `uv run ruff check .`, `uv build`) and reports the results,
    then asks the human only to confirm what Claude cannot self-verify.
  </action>
  <what-built>
    The scaffold is rendered and deps are pinned. Two things need human confirmation that
    Claude cannot fully self-verify: (1) the `ladybug` + `pydantic` version pins actually
    resolve on PyPI (RESEARCH.md A2 + Environment Availability: PyPI re-verification was
    proxy-blocked during research, so the pins are CITED-from-CLAUDE.md, not freshly verified
    this session), and (2) Assumption A1 — that NVM, the primary consumer, targets Python 3.14,
    which is the basis for raising the floor from 3.11 to 3.14 (the user's call per CONTEXT #2).
  </what-built>
  <how-to-verify>
    1. Confirm `uv sync` completed without resolution errors (deps resolved on PyPI). If it
       failed, the version pins in `pyproject.toml` need adjusting to currently-published versions.
    2. Confirm that NVM (the primary consumer) targets Python 3.14, validating the raised floor.
       If NVM is pinned below 3.14, this floor decision must be revisited before proceeding
       (it would re-introduce the avoided UUID7 third-dependency tension).
    3. Run `uv run basedpyright && uv run ruff check . && uv build` and confirm all three pass
       on the empty scaffold.
  </how-to-verify>
  <verify>
    <human-check>User confirms uv sync resolved the pins on PyPI and that NVM targets Python 3.14; the three automated checks (basedpyright, ruff, uv build) pass on the empty scaffold.</human-check>
  </verify>
  <resume-signal>Type "approved" to confirm pins resolve and NVM is on 3.14, or describe what to adjust.</resume-signal>
  <done>Human has confirmed the dependency pins resolve on PyPI and NVM targets Python 3.14, or has supplied corrected pins; the empty scaffold passes basedpyright/ruff/build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata → PyPI resolver | Declared dependency names/versions are fetched and installed from a public registry — a supply-chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Spoofing (supply chain) | `pyproject.toml` dependency names | mitigate | Pin/import the verified `ladybug` (Kùzu fork, MIT, PyPI 0.17.1); `ladybugdb` is a non-existent slopsquat name (404 on PyPI) and is gated out by Task 2's `! grep ladybugdb` assertion. Both runtime deps are pre-decided project constraints, not freshly-discovered packages. |
| T-01-02 | Tampering | scaffold render overwriting existing repo files | accept | Task 1 renders to a temp dir and preserves `LICENSE`/`README.md`/`.planning/`; verified by acceptance criteria. Local-only operation, no external input. |

Note (per phase security constraints): this is a zero-network, zero-auth, decision-grade
scaffold phase. The only genuine threat surface is the supply-chain dependency-name pin,
handled above. No runtime threats apply — this plan writes no storage code.
</threat_model>

<verification>
- `uv run basedpyright` passes (strict) on the empty scaffold
- `uv run ruff check .` is clean
- `uv build` produces a wheel/sdist
- `uv run pytest -q` passes the template smoke test
- `! grep -rq ladybugdb pyproject.toml` (the slopsquat name is absent)
</verification>

<success_criteria>
The package builds, type-checks (basedpyright strict), and lints clean from the
`cookiecutter-python-uv-library` scaffold under import name `doxastica`, with runtime deps
exactly `ladybug` + `pydantic`, `hypothesis` in the dev group, `requires-python >=3.14`, and
CI matrices reduced to 3.14-only (PKG-01).
</success_criteria>

<output>
Create `.planning/phases/01-protocol-backend-port-data-model-decisions/01-01-SUMMARY.md` when done
</output>
