---
phase: 01-protocol-backend-port-data-model-decisions
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/doxastica/protocol.py
  - src/doxastica/__init__.py
  - tests/test_import_purity.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
user_setup: []

must_haves:
  truths:
    - "protocol.py imports only pydantic/typing/uuid and doxastica.models — never ladybug"
    - "BeliefStore is a typing.Protocol whose query_scope takes a BeliefFilter, never a free str"
    - "get_impact returns ImpactResult with depth: int | None = None (unbounded default)"
    - "the UUID7 ordering contract for source_event_id is written into the source as a docstring/contract note"
    - "the public surface is re-exported from doxastica/__init__.py __all__"
  artifacts:
    - path: "src/doxastica/protocol.py"
      provides: "Public BeliefStore typing.Protocol — the NVM-core seam, ladybug-free"
      contains: "class BeliefStore(Protocol)"
      min_lines: 25
    - path: "tests/test_import_purity.py"
      provides: "DATA-01 guard — AST scan asserting protocol.py imports no ladybug"
      contains: "ladybug"
    - path: "src/doxastica/__init__.py"
      provides: "Public re-export barrel (BeliefStore, models, enums, errors)"
      contains: "__all__"
  key_links:
    - from: "src/doxastica/protocol.py"
      to: "doxastica.models"
      via: "import of BeliefFilter / ImpactResult / Scope / BeliefState / EdgeType"
      pattern: "from doxastica.models import"
    - from: "src/doxastica/protocol.py"
      to: "BeliefFilter (not str)"
      via: "query_scope signature"
      pattern: "query_scope.*filter: BeliefFilter"
---

<objective>
Author the public `BeliefStore` `typing.Protocol` — the unchanged NVM-core seam — with the
Phase-1 refinements applied (DATA-01 import purity, DATA-02 `BeliefFilter` replacing the free
string, DATA-04 `ImpactResult`/optional unbounded `depth`), write the DATA-03 UUID7 ordering
contract into the source as a contract note, and wire the public re-export barrel. Enforce
import purity as an executable AST test.

Purpose: This is the public contract consumers code against — the seam that must never leak a
`ladybug` import (DATA-01) and must never accept a free query string (DATA-02). Reversing
either is the #1 rewrite risk the phase exists to close. Pure interface; no behavior.
Output: `src/doxastica/protocol.py`, `tests/test_import_purity.py`, populated
`src/doxastica/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-CONTEXT.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md
@.planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md
@src/doxastica/models.py
@src/doxastica/errors.py
</context>

<artifacts_this_phase_produces>
This plan produces these symbols:
- `src/doxastica/protocol.py`:
  - `BeliefStore(Protocol)` with methods: `get_or_create_scope(scope_id: str) -> Scope`;
    `revise(scope_id, belief_id, value: Any, source_event_id: UUID) -> BeliefState`;
    `expand(...) -> BeliefState`; `contract(scope_id, belief_id, source_event_id: UUID) -> None`;
    `add_edge(from_state_id: UUID, to_state_id: UUID, edge_type: EdgeType) -> None`;
    `query_scope(scope_id: str, filter: BeliefFilter, include_deprecated: bool = False) -> list[BeliefState]`;
    `get_impact(belief_state_id: UUID, depth: int | None = None) -> ImpactResult`;
    `get_revision_chain(belief_id: str) -> list[BeliefState]`;
    `get_scope_at(scope_id: str, as_of_event_id: UUID) -> list[BeliefState]`
  - The DATA-03 UUID7 ordering contract as a written docstring/contract note
    (`(source_event_id byte-order, state_id tiebreak)` total order; intra-ms monotonicity NOT
    demanded of the caller)
- `src/doxastica/__init__.py`: populated `__all__` re-exporting `BeliefStore`, `Scope`,
  `Belief`, `BeliefState`, `BeliefFilter`, `ImpactResult`, `Status`, `EdgeType`,
  `DoxasticaError`, `WorldScopeContractionError`
- `tests/test_import_purity.py`: `test_protocol_does_not_import_ladybug` (AST scan)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the public BeliefStore Protocol with the UUID7 ordering contract</name>
  <files>src/doxastica/protocol.py</files>
  <read_first>
    - src/doxastica/models.py (the exact model/enum names this Protocol references)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("protocol.py" section — the verbatim signature block + "Import-purity pattern")
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md (lines 315-354 BeliefStore code example; line 142 ordering-contract docstring note)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-CONTEXT.md (decision #2 ordering contract, decision #4 query_scope, decision #5 get_impact)
  </read_first>
  <action>
    Create `src/doxastica/protocol.py` with a module docstring. Import ONLY from `typing`
    (`Any`, `Protocol`), `uuid` (`UUID`), and `doxastica.models` (`Scope`, `BeliefState`,
    `EdgeType`, `BeliefFilter`, `ImpactResult`) — NEVER `import ladybug` (DATA-01; this is
    guarded by Task 2's AST test). Define `class BeliefStore(Protocol):` with the nine methods
    exactly as listed in this plan's artifacts section, each with an ellipsis body, no
    implementation. Apply the locked refinements (do NOT regress to the recovered sketch):
    `query_scope` takes `filter: BeliefFilter` and `include_deprecated: bool = False` — NEVER a
    free string (DATA-02); `get_impact` returns `ImpactResult` with `depth: int | None = None`
    default unbounded (DATA-04) — NOT a bare `list` and NOT `depth=5`. Write the DATA-03 UUID7
    ordering contract as a docstring on `get_scope_at` (or a module-level contract note): states
    are ordered by `(source_event_id byte-order, state_id tiebreak)` giving a total deterministic
    order even when caller `source_event_id`s collide intra-ms; intra-ms monotonicity is NOT
    demanded of the caller (RFC 9562 section 5.7); core-minted `state_id` is write-monotonic so
    the tiebreak reflects true insertion order. The contract text must be present in the source so
    the DATA-03 doc-assertion can find it. Note that `include_deprecated` is ergonomic sugar over
    `status` (`False` is `{active}`, `True` is `{active, retracted}`; explicit `status` governs) —
    capture this in the `query_scope` docstring.
  </action>
  <verify>
    <automated>uv run basedpyright src/doxastica/protocol.py && uv run ruff check src/doxastica/protocol.py && grep -q 'class BeliefStore(Protocol)' src/doxastica/protocol.py && grep -q 'filter: BeliefFilter' src/doxastica/protocol.py && grep -q 'depth: int | None = None' src/doxastica/protocol.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run basedpyright` passes strict on `protocol.py`
    - `uv run ruff check src/doxastica/protocol.py` is clean
    - `protocol.py` defines `class BeliefStore(Protocol)` with all nine methods
    - `query_scope` signature contains `filter: BeliefFilter` (no free `str` parameter)
    - `get_impact` returns `ImpactResult` with `depth: int | None = None`
    - The UUID7 ordering-contract text (`source_event_id byte-order`, `state_id` tiebreak) is present in the source
    - No `import ladybug` anywhere in the file
  </acceptance_criteria>
  <done>The public `BeliefStore` Protocol exists with the locked DATA-02/03/04 refinements and the written UUID7 ordering contract; it type-checks strict and imports no ladybug.</done>
</task>

<task type="auto">
  <name>Task 2: Author the import-purity AST guard and populate the re-export barrel</name>
  <files>tests/test_import_purity.py, src/doxastica/__init__.py</files>
  <read_first>
    - src/doxastica/protocol.py (the just-authored Protocol)
    - src/doxastica/models.py, src/doxastica/errors.py (the symbols to re-export)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-PATTERNS.md ("test_import_purity.py" section + "__init__.py" section)
    - .planning/phases/01-protocol-backend-port-data-model-decisions/01-RESEARCH.md (lines 429-443 import-purity AST example)
  </read_first>
  <action>
    Create `tests/test_import_purity.py` in the template test style (module docstring, top-level
    `def test_*()`, bare `assert`, no classes). Implement `test_protocol_does_not_import_ladybug`:
    read `src/doxastica/protocol.py`, parse with `ast`, walk collecting `ast.Import` names and
    `ast.ImportFrom` modules, and assert none have a first dotted component equal to `ladybug`
    (include the offending names in the assert message). This makes the DATA-01 "no leak" rule a
    build error, not a review note. Then populate `src/doxastica/__init__.py`: keep its module
    docstring and set `__all__` to re-export the public surface — `BeliefStore` (from
    `.protocol`), `Scope`, `Belief`, `BeliefState`, `BeliefFilter`, `ImpactResult`, `Status`,
    `EdgeType` (from `.models`), `DoxasticaError`, `WorldScopeContractionError` (from `.errors`) —
    with matching `from .module import ...` statements so `__all__` entries actually resolve.
  </action>
  <verify>
    <automated>uv run pytest tests/test_import_purity.py -x -q && uv run basedpyright src/doxastica/__init__.py && python -c "import doxastica; assert set(['BeliefStore','BeliefFilter','ImpactResult','EdgeType']) <= set(doxastica.__all__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_import_purity.py` passes (protocol.py imports no ladybug)
    - `import doxastica` succeeds and `doxastica.__all__` includes `BeliefStore`, `BeliefFilter`, `ImpactResult`, `EdgeType`, `Status`, the models, and the errors
    - Every `__all__` entry is importable from the package (no dangling names)
    - `uv run ruff check tests/test_import_purity.py src/doxastica/__init__.py` is clean
  </acceptance_criteria>
  <done>The DATA-01 import-purity guard passes mechanically and the public surface is re-exported from the package root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → BeliefStore Protocol | The public NVM-core seam; consumers code against this interface. Untrusted query intent crosses here via `query_scope`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-INJ | Tampering / Info disclosure | `query_scope` parameter | mitigate (by design) | DATA-02: `query_scope` takes a closed `BeliefFilter`, never a free `str` — the original `query_scope(query: str)` was the #1 injection/triple-leak risk. The typed signature makes a free query string unrepresentable at the seam; verified by the `grep filter: BeliefFilter` gate. |
| T-01-LEAK | Information disclosure | `protocol.py` imports | mitigate | DATA-01: a `ladybug` import in the public seam would leak the backend dialect/topology across the contract boundary. The AST import-purity test makes any such import a build failure. |

Note: zero-network, zero-auth, no storage code, no behavior. The two threats above are both
designed-out at the type level — the Protocol signature IS the mitigation.
</threat_model>

<verification>
- `uv run basedpyright` passes strict on `protocol.py` + `__init__.py`
- `uv run ruff check .` clean
- `uv run pytest tests/test_import_purity.py -q` passes (DATA-01 guard)
- `query_scope` takes `BeliefFilter`; `get_impact` returns `ImpactResult` with `depth: int | None = None`
- UUID7 ordering-contract text present in source (DATA-03)
</verification>

<success_criteria>
The public `BeliefStore` `typing.Protocol` exists importing only `pydantic`/`typing`/`uuid`
and `doxastica.models` with no `ladybug` import anywhere (DATA-01, mechanically guarded);
`query_scope` takes the closed `BeliefFilter` (DATA-02); `get_impact` returns `ImpactResult`
with the ratified unbounded `depth` default (DATA-04); the UUID7 ordering contract is written
into the source (DATA-03); and the public surface is re-exported from the package root.
</success_criteria>

<output>
Create `.planning/phases/01-protocol-backend-port-data-model-decisions/01-03-SUMMARY.md` when done
</output>
