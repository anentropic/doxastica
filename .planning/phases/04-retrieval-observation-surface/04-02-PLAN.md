---
phase: 04-retrieval-observation-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/doxastica/core.py
autonomous: true
requirements: [CHAIN-04, HIST-01]

must_haves:
  truths:
    - "query_scope returns exactly one current state per (scope, belief) — never the full history (D-01)"
    - "include_retracted=False returns only active-current beliefs; include_retracted=True ALSO returns retracted-current beliefs (D-02)"
    - "An explicit belief_filter.status overrides the include_retracted flag sugar (D-03 precedence)"
    - "event_id_min/max post-filter the derived current tails — a tail newer than event_id_max is ABSENT, never rewound (D-06)"
    - "Non-existent or empty scope returns [] and creates no Scope node (D-08, pure read)"
    - "Results are deterministically sorted by the single _order_key contract; in-memory and ladybug return identical sequences (D-07)"
    - "_current's write-side retracted-tail→None contract is unchanged after the refactor (Phase-3 keystone still green)"
  artifacts:
    - path: "src/doxastica/core.py"
      provides: "query_scope body + status-agnostic _current_tail helper + behaviour-preserving _current refactor"
      contains: "def query_scope"
  key_links:
    - from: "src/doxastica/core.py query_scope"
      to: "BackendPort.match_nodes"
      via: "single scope-wide match_nodes scan + core-side group-by-belief"
      pattern: "match_nodes\\("
    - from: "src/doxastica/core.py query_scope"
      to: "_order_key"
      via: "the ONE ordering contract for both the per-belief max and the result sort"
      pattern: "_order_key"
    - from: "src/doxastica/core.py _current"
      to: "_current_tail"
      via: "_current calls _current_tail then applies retracted→None"
      pattern: "_current_tail"
---

<objective>
Implement the `query_scope` body in `MemoryCore` (driver-blind, in `core.py`) and factor the
status-agnostic `_current_tail` helper out of `_current`, turning the Plan-01 RED test suite
GREEN on both backends. This is the phase's only genuinely new logic: a status-agnostic
ordering-max tail helper, plus `query_scope` as a single scope-wide `match_nodes` scan →
group-by-belief → per-group ordering-max → status filter → belief_ids/event-range post-filter
→ `_order_key` sort → `_hydrate`.

Purpose: Deliver the observation surface the Phase-7 conformance suite and irony join read the
belief base THROUGH (HIST-01), with the retracted-vs-superseded matrix (CHAIN-04) provable via
`query_scope` (current cells) + `get_revision_chain` + `SUPERSEDES` (superseded cells). The
refactor MUST be behaviour-preserving for every Phase-3 caller of `_current`.

Output: `query_scope` body, `_current_tail` helper, and a `_current` that delegates to it — all
in `src/doxastica/core.py`. No new port primitive, no schema change, no new dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-observation-surface/04-CONTEXT.md
@.planning/phases/04-retrieval-observation-surface/04-RESEARCH.md
@.planning/phases/04-retrieval-observation-surface/04-PATTERNS.md
@.planning/phases/04-retrieval-observation-surface/04-VALIDATION.md
@src/doxastica/core.py
@src/doxastica/models.py
@src/doxastica/ports.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factor _current_tail (status-agnostic) and refactor _current to delegate to it</name>
  <files>src/doxastica/core.py</files>
  <read_first>
    - src/doxastica/core.py (`_order_key` lines ~63-73 — the ONE ordering contract; `_current` lines ~169-194 — the `match_nodes` + `max(key=_order_key)` + retracted→None collapse being factored; the runtime model-import line ~52)
    - tests/test_invariants.py (the keystone invariant `current_is_total_single_valued_and_chain_tail` which calls `core._current` and MUST still pass — the behaviour-preservation contract)
    - .planning/phases/04-retrieval-observation-surface/04-PATTERNS.md (§"Refactor (Pattern 1)" — the exact 2-method shape)
  </read_first>
  <behavior>
    - `_current_tail(scope_id, belief_id)` returns the raw ordering-max `BeliefState` dict over ALL statuses for that (scope, belief), or `None` when no state exists — NO retracted collapse.
    - `_current(scope_id, belief_id)` returns the same tail EXCEPT `None` when the ordering-max tail's status is `retracted` (the unchanged Phase-3 D-05 write-side contract).
    - For an active-tail belief: `_current` == `_current_tail` (identical dict).
    - For a contracted (retracted-tail) belief: `_current` returns `None`, `_current_tail` returns the retracted tail.
    - The Phase-3 keystone invariant `current_is_total_single_valued_and_chain_tail` still passes unchanged on both backends.
  </behavior>
  <action>
    In `src/doxastica/core.py` add a private method `_current_tail(self, scope_id, belief_id) ->
    dict[str, Any] | None` that runs `self._backend.match_nodes("BeliefState", {"scope_id":
    scope_id, "belief_id": belief_id})`, returns `None` if empty, else `max(states,
    key=_order_key)` — REUSING the single `_order_key` contract (no second ordering). Then refactor
    `_current` to call `_current_tail` and apply ONLY the retracted→`None` collapse on top (return
    `None` when `tail is None or tail["status"] == Status.retracted.value`). This is exactly the
    PATTERNS.md Pattern-1 shape and must be behaviour-preserving for every existing `_current`
    caller. Add `BeliefFilter` to the existing runtime `from doxastica.models import ...` line (it
    is referenced by the Task-2 `query_scope` signature; `core.py` already imports models at
    runtime, so it joins that line — NOT under TYPE_CHECKING). Do NOT change `_order_key`,
    `_hydrate`, `get_revision_chain`, or any write op.
  </action>
  <verify>
    <automated>uv run pytest tests/test_invariants.py tests/test_revision_spine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_invariants.py tests/test_revision_spine.py -q` is GREEN on both backends (the `_current` refactor is behaviour-preserving — the keystone invariant unchanged).
    - `_current_tail` exists and uses `max(..., key=_order_key)` — no new ordering lambda anywhere (`grep -c _order_key src/doxastica/core.py` count increases only via reuse, not a second key function).
    - `_current` now delegates to `_current_tail` (the `match_nodes`/`max` lives in `_current_tail`, the retracted→None rule lives in `_current`).
    - basedpyright strict + ruff clean on `core.py`.
  </acceptance_criteria>
  <done>`_current_tail` is the status-agnostic ordering-max tail; `_current` delegates to it and keeps its retracted→None contract; the Phase-3 keystone is still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the query_scope body (group-by-belief max → filter → sort → hydrate)</name>
  <files>src/doxastica/core.py</files>
  <read_first>
    - src/doxastica/core.py (`_current_tail` just added; `_order_key` ~63; `_hydrate` ~214 — the value-decode boundary to reuse; `get_revision_chain` ~376 — the parallel read shape)
    - src/doxastica/models.py (BeliefFilter: belief_ids: frozenset[str]|None, status: frozenset[Status]|None, event_id_min/max: UUID|None; Status = {active, retracted})
    - src/doxastica/ports.py (`match_nodes` — the read primitive query_scope composes; no new primitive)
    - src/doxastica/protocol.py (the renamed `query_scope` signature + precedence docstring this body must match — include_retracted)
    - .planning/phases/04-retrieval-observation-surface/04-PATTERNS.md (§query_scope body — the 8-step single-scan composition) and 04-RESEARCH.md §Common Pitfalls (Pitfalls 2/3/5/6 are load-bearing)
  </read_first>
  <behavior>
    - `query_scope(scope, BeliefFilter())` on a populated scope returns exactly one `BeliefState` per (scope, belief_id) whose current tail is `active`.
    - `include_retracted=True` ALSO includes beliefs whose current tail is `retracted`; still one state per belief.
    - An explicit `belief_filter.status` governs and overrides the flag sugar.
    - `belief_ids` narrows the set; `event_id_min`/`event_id_max` inclusively post-filter the derived tails (a tail newer than max is ABSENT, not rewound).
    - Empty/absent scope → `[]`, no `Scope` node created.
    - Result sorted by `_order_key`; identical sequence on both backends; no duplicate beliefs; never a superseded (non-tail) state.
  </behavior>
  <action>
    Implement `MemoryCore.query_scope(self, scope_id, belief_filter, include_retracted=False) ->
    list[BeliefState]` in `core.py` following the PATTERNS.md 8-step composition:
    (1) Resolve the allowed status set — if `belief_filter.status is not None` use it (explicit
    governs, D-03); else `{active, retracted}` when `include_retracted` else `{active}`.
    (2) ONE scope-wide round-trip: `rows = self._backend.match_nodes("BeliefState", {"scope_id":
    scope_id})` — absent scope returns `[]` (D-08; do NOT call `_ensure_scope`, raise nothing, no
    unit_of_work).
    (3) Group `rows` by `belief_id`, keeping the per-group ordering-MAX over ALL statuses (reuse
    `_order_key`) — this is the status-agnostic current tail; the max MUST be taken BEFORE any
    status filter (Pitfall 2: pre-filtering to active leaks a stale active value below a retracted
    tail).
    (4) Status filter the tails: keep iff `Status(t["status"])` is in the allowed set — rebuild the
    enum from the stored string (Pitfall 6; matches the `_hydrate` boundary).
    (5) `belief_ids` narrowing if set.
    (6) Event-range POST-filter (D-06): compare `t["source_event_id"]` (a str) against
    `str(belief_filter.event_id_min)` / `str(belief_filter.event_id_max)` — str-vs-str, the SAME
    form `_order_key` uses (Pitfall 3: never compare str vs UUID); inclusive `>=` / `<=` (A1).
    (7) Sort the kept tails by `_order_key` (D-07: the ONE ordering, reused — no second sort).
    (8) Return `[self._hydrate(t) for t in kept]` — reuse `_hydrate` for the value-decode boundary;
    never re-implement base64/json decode.
    Consume ONLY the 4 closed `BeliefFilter` fields — no free string, no triple-structure leak
    (DATA-02). No dedup pass (D-01 postcondition). No new port primitive, no schema change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_query_scope.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_scope.py -x -q` is GREEN on BOTH backends — every VALIDATION.md row passes, including `test_retracted_superseded_matrix`, `test_query_scope_excludes_superseded`, `test_event_range_boundary_inclusive`, and `test_empty_scope_returns_empty`.
    - `uv run pytest -q` (full suite) is GREEN — the Phase-3 keystone `test_invariants.py` and `test_revision_spine.py` remain green (refactor behaviour-preserving).
    - `grep -c '\.sort(key=_order_key)\|key=_order_key' src/doxastica/core.py` confirms `query_scope` reuses `_order_key` — no second ordering function introduced.
    - `query_scope` calls neither `_ensure_scope` nor `unit_of_work` and raises nothing (pure read, D-08).
    - basedpyright strict clean + ruff clean on `core.py`.
  </acceptance_criteria>
  <done>`query_scope` is implemented as the single-scan group-by-max composition; the full Plan-01 test suite is GREEN on both backends and the Phase-3 keystone is unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → MemoryCore.query_scope | Typed closed `BeliefFilter` + `scope_id` str; no network/auth/session. |
| MemoryCore → BackendPort.match_nodes | Internal LPG-primitive port; `$param`-bound values + validated identifiers (already injection-proofed in the adapters). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering / Info-disclosure | `query_scope` → `match_nodes` | accept | No free-query string is constructed; `query_scope` composes `match_nodes` with bound params + the closed 4-field `BeliefFilter` only (DATA-02 / BACK-01). Phase 4 adds no new interpolation. Low severity. |
| T-04-03 | Info-disclosure | result hydration | accept | `query_scope` returns frozen `BeliefState` models via `_hydrate`; no raw dict / `_ID`/`_LABEL` internal key escapes the seam (adapters strip `_`-prefixed keys; core returns models only). No cross-namespace read (the injected backend owns one namespace, CONN-02). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `uv run pytest tests/test_query_scope.py -x -q` GREEN on both backends (all VALIDATION rows).
- `uv run pytest -q` full suite GREEN — Phase-3 keystone unbroken.
- `! grep -rn include_deprecated src tests .planning/REQUIREMENTS.md .planning/ROADMAP.md` empty (carried from Plan 01).
- basedpyright strict + ruff clean.
- No `Scope` node created by a `query_scope` on an absent scope (D-08); `query_scope` raises nothing.
</verification>

<success_criteria>
- `query_scope` delivers the full HIST-01 signature behaviour (one current tail per belief, the `include_retracted` flag + precedence, belief_ids + inclusive event-range post-filter, deterministic order, empty-scope `[]`) on both backends.
- The CHAIN-04 four-cell matrix is provable: current cells via `query_scope`, superseded cells via `get_revision_chain` + `SUPERSEDES` (D-05).
- The Phase-3 keystone remains green — the `_current` refactor is behaviour-preserving.
</success_criteria>

<output>
Create `.planning/phases/04-retrieval-observation-surface/04-02-SUMMARY.md` when done
</output>
