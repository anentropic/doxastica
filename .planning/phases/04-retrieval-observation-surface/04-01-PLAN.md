---
phase: 04-retrieval-observation-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/doxastica/protocol.py
  - tests/test_query_scope.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CHAIN-04, HIST-01]

must_haves:
  truths:
    - "The public BeliefStore.query_scope signature reads include_retracted (not include_deprecated)"
    - "No include_deprecated token remains in src, tests, REQUIREMENTS.md, or ROADMAP.md"
    - "tests/test_query_scope.py exists, is parametrized over the backend fixture, and fails (RED) because query_scope has no body yet"
    - "The test file encodes every HIST-01 and CHAIN-04 behavior row from VALIDATION.md"
  artifacts:
    - path: "src/doxastica/protocol.py"
      provides: "Renamed include_retracted keyword + updated docstring (D-03)"
      contains: "include_retracted"
    - path: "tests/test_query_scope.py"
      provides: "Failing parametrized two-backend behavior + four-cell matrix tests (Wave 0 scaffold)"
      min_lines: 120
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHAIN-04 / HIST-01 text carrying the include_retracted rename"
      contains: "include_retracted"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-4 success-criteria text carrying the retracted rename"
      contains: "include_retracted"
  key_links:
    - from: "tests/test_query_scope.py"
      to: "doxastica.MemoryCore.query_scope"
      via: "core.query_scope(scope, BeliefFilter(), include_retracted=...)"
      pattern: "query_scope\\([^)]*include_retracted"
    - from: "tests/test_query_scope.py"
      to: "tests/conftest.py backend fixture"
      via: "def test_*(backend: BackendPort)"
      pattern: "def test_.*backend"
---

<objective>
Land the D-03 public-flag rename (`include_deprecated` → `include_retracted`) on the
`BeliefStore` Protocol and its two requirement/roadmap doc sites, AND create the Wave-0
failing test scaffold `tests/test_query_scope.py` that encodes the full HIST-01 + CHAIN-04
behavior contract. This is the test-first foundation: every behavior the Plan-02
implementation must satisfy is written here as a parametrized two-backend test that is RED
until `query_scope` gets its body.

Purpose: Pin the observable contract (the rename + every required behavior) before any
implementation, so Plan 02 is GREEN-driven and the four-cell matrix is provable on both
backends. The rename has zero stored-data blast radius (D-03 / RESEARCH Runtime State
Inventory) — it is a pure source/doc edit plus the not-yet-shipped public keyword.

Output: Renamed `protocol.py` signature+docstring; updated CHAIN-04/HIST-01 text in
REQUIREMENTS.md and the Phase-4 success-criteria lines in ROADMAP.md; new failing
`tests/test_query_scope.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-observation-surface/04-CONTEXT.md
@.planning/phases/04-retrieval-observation-surface/04-RESEARCH.md
@.planning/phases/04-retrieval-observation-surface/04-PATTERNS.md
@.planning/phases/04-retrieval-observation-surface/04-VALIDATION.md
@src/doxastica/protocol.py
@src/doxastica/models.py
@tests/conftest.py
@tests/test_revision_spine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename include_deprecated → include_retracted on the Protocol + the two doc sites (D-03)</name>
  <files>src/doxastica/protocol.py, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - src/doxastica/protocol.py (the query_scope signature lines 111-128 + docstring; the import-purity gate — keep `from __future__ import annotations`, never add a runtime import)
    - src/doxastica/models.py (BeliefFilter closed 4 fields, Status = {active, retracted} — UNCHANGED by D-03; confirm Status stays {active, retracted})
    - .planning/REQUIREMENTS.md (CHAIN-04 line 60-61, HIST-01 line 84-85 — the requirement-text half of D-03)
    - .planning/ROADMAP.md (Phase-4 success-criteria lines 139-140 — "deprecated"/include_deprecated text)
  </read_first>
  <action>
    In `src/doxastica/protocol.py` rename the `query_scope` keyword parameter `include_deprecated`
    to `include_retracted` (signature line ~115) and update both docstring mentions (lines ~123,
    ~126) from `include_deprecated` to `include_retracted`. Per D-03 and PATTERNS.md: change ONLY
    the flag NAME — the status-set precedence wording is already correct (`False` ≡ `{active}`,
    `True` ≡ `{active, retracted}`, explicit `belief_filter.status` governs) and must be kept
    verbatim. Do NOT touch `models.py` — `Status` stays `{active, retracted}` (DATA-06 frozen;
    Option B was rejected). Do NOT add any runtime import to `protocol.py` (import-purity gate;
    `BeliefFilter`/`BeliefState` stay under `TYPE_CHECKING`).
    In `.planning/REQUIREMENTS.md` update CHAIN-04 (the `include_deprecated` flag mention) and
    HIST-01 (the `query_scope(scope, filter, include_deprecated=False)` signature + "deprecated"
    wording) to `include_retracted` / "retracted".
    In `.planning/ROADMAP.md` update the two Phase-4 success-criteria lines (139-140): the
    `include_deprecated=False` / `include_deprecated=True` signature mentions, the
    `include_deprecated` flag mention, and the "deprecated" matrix vocabulary → `include_retracted`
    / "retracted" (the clean two-axis pair is "retracted vs superseded").
  </action>
  <verify>
    <automated>! grep -rn include_deprecated src tests .planning/REQUIREMENTS.md .planning/ROADMAP.md && grep -q 'include_retracted' src/doxastica/protocol.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn include_deprecated src tests .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns NO hits (the D-03 grep gate is empty).
    - `src/doxastica/protocol.py` `query_scope` signature shows `include_retracted: bool = False`.
    - `src/doxastica/models.py` is unmodified; `Status` is still `{active, retracted}`.
    - `protocol.py` adds no runtime import (import-purity guard still green): `uv run pytest -q -k import_purity` passes if such a test exists, otherwise basedpyright/ruff remain clean.
  </acceptance_criteria>
  <done>The public flag is `include_retracted` on the Protocol and in both planning doc sites; the `include_deprecated` token is gone everywhere; `models.py`/`Status` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create the failing Wave-0 test scaffold tests/test_query_scope.py (parametrized, both backends)</name>
  <files>tests/test_query_scope.py</files>
  <read_first>
    - tests/test_revision_spine.py (the load-bearing parametrized two-backend idiom: `def test_*(backend: BackendPort)`, `MemoryCore(backend)` from the INJECTED fixture port — NOT `MemoryCore.in_memory()`; the `_event_id()` uuid7 helper)
    - tests/conftest.py (the `backend` fixture `params=["memory","ladybug"]` + `importorskip` — REUSE verbatim, do NOT re-declare)
    - tests/test_invariants.py (shadow-oracle pattern — reference only, for the OPTIONAL query_scope-parity property)
    - src/doxastica/models.py (BeliefFilter 4 closed fields: belief_ids, status, event_id_min, event_id_max; Status enum)
    - src/doxastica/__init__.py (barrel — MemoryCore, BeliefFilter, Status are all exported; import from `doxastica`)
    - .planning/phases/04-retrieval-observation-surface/04-VALIDATION.md (the Per-Task Verification Map — every row is a required test function name)
    - .planning/phases/04-retrieval-observation-surface/04-RESEARCH.md (§Code Examples: the four-cell matrix operation sequence and the status-resolution precedence)
  </read_first>
  <action>
    Create `tests/test_query_scope.py`. Import `MemoryCore`, `BeliefFilter`, `Status` from the
    `doxastica` barrel (verified exported); `import uuid`; `from doxastica.ports import BackendPort`
    under `TYPE_CHECKING`. Copy the `_event_id()` uuid7 helper from `test_revision_spine.py`. Every
    test takes `backend: BackendPort` and constructs `MemoryCore(backend)` from the injected fixture
    port so BOTH backends run (no conftest edit). Write one test function PER VALIDATION.md row, named
    EXACTLY as the map specifies:
    - `test_query_scope_active_only` — default returns only `current+active`, one state per belief (HIST-01, D-01/D-02).
    - `test_query_scope_include_retracted` — `include_retracted=True` ALSO returns `current+retracted`; still one per belief (D-02). This is the Pitfall-1 guard: a contracted belief MUST appear with the flag on and be ABSENT with it off.
    - `test_status_filter_precedence` — an explicit `BeliefFilter(status=...)` overrides the `include_retracted` flag (D-03 precedence).
    - `test_belief_ids_filter` — `BeliefFilter(belief_ids=...)` narrows the result set.
    - `test_event_range_postfilter` — `event_id_min`/`event_id_max` post-filter current tails; a belief whose current tail is NEWER than `event_id_max` is ABSENT, NOT rewound to an older value (D-06).
    - `test_event_range_boundary_inclusive` — a tail whose `source_event_id` equals `event_id_min` (and one equal to `event_id_max`) is INCLUDED (A1: inclusive `[min,max]`).
    - `test_empty_scope_returns_empty` — querying a never-created scope returns `[]` and creates NO `Scope` node (D-08, pure read). Assert no scope node was created (e.g. a follow-up query still returns `[]`).
    - `test_query_scope_deterministic_order` — result is sorted by the `(source_event_id, state_id)` ordering; assert the returned sequence is monotonic in that key. (Both backends run it; parity falls out.)
    - `test_no_duplicate_beliefs` — exactly one state per `(scope, belief_id)` in the result (D-01 postcondition / SC3).
    - `test_retracted_superseded_matrix` — the CHAIN-04 four-cell matrix using the RESEARCH §Code-Examples operation sequence (belief A revise→revise→contract; B revise→revise; C revise→contract→revise). Assert `query_scope(include_retracted=False)` == active-current set, `include_retracted=True` adds the contracted belief, and the two superseded cells are reached via `get_revision_chain(belief_id)[:-1]` (non-tail entries; split by `.status` for superseded+active vs superseded+retracted). Optionally assert the `SUPERSEDES` edge structurally via the port.
    - `test_query_scope_excludes_superseded` — `query_scope` NEVER returns a non-tail (superseded) state (D-05): after a revise→revise, only the newer value is in the result.
    These tests MUST FAIL (RED) now — `query_scope` has no body until Plan 02. Do NOT implement
    `query_scope` here. Use the closed `BeliefFilter` only (no free string); reach the superseded
    cells solely through `get_revision_chain` + edges (D-05), never by asking `query_scope` for them.
  </action>
  <verify>
    <automated>uv run pytest tests/test_query_scope.py --collect-only -q && uv run pytest tests/test_query_scope.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_scope.py --collect-only -q` succeeds and lists every function named in VALIDATION.md (collection must not error).
    - `uv run pytest tests/test_query_scope.py -q` FAILS (RED) — the body does not exist yet; failures are assertion/AttributeError on `query_scope`, NOT import/collection errors.
    - Every test function signature is `def test_...(backend: BackendPort) -> None` and constructs `MemoryCore(backend)` (parametrized over memory + ladybug).
    - No `conftest.py` change; the existing `backend` fixture is reused.
    - Only `BeliefFilter` is used as the filter argument — no free query string anywhere in the file.
  </acceptance_criteria>
  <done>`tests/test_query_scope.py` collects cleanly, encodes every HIST-01 + CHAIN-04 VALIDATION row as a parametrized two-backend test, and is RED pending the Plan-02 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (NVM / postulate test) → MemoryCore | Typed call surface only; the sole "input" is the closed `BeliefFilter` + a `scope_id` str. No network, no auth, no session (embedded library). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Info-disclosure | `query_scope` filter argument | accept | Cypher-injection / triple-structure leak is designed OUT by DATA-02 + BACK-01: the Protocol exposes NO free-query string and `BeliefFilter` is a closed 4-field model. This phase introduces no new interpolation. The rename keeps the closed filter; no new attack surface. Low severity — no `high` threats to block on. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — composes already-audited stack). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `! grep -rn include_deprecated src tests .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns empty (D-03 gate).
- `uv run pytest tests/test_query_scope.py --collect-only -q` collects without error.
- `uv run pytest tests/test_query_scope.py -q` is RED (expected — body lands in Plan 02).
- `uv run pytest tests/test_invariants.py tests/test_revision_spine.py -q` stays GREEN (this plan touches neither core.py nor the keystone).
- basedpyright strict + ruff clean on `protocol.py` and the new test file.
</verification>

<success_criteria>
- The public flag is `include_retracted` on the Protocol and in both planning doc sites; `include_deprecated` is gone everywhere.
- `tests/test_query_scope.py` exists, collects, encodes every VALIDATION.md row, runs on both backends, and is RED.
- Phase-3 keystone (`test_invariants.py`) remains green (untouched).
</success_criteria>

<output>
Create `.planning/phases/04-retrieval-observation-surface/04-01-SUMMARY.md` when done
</output>
