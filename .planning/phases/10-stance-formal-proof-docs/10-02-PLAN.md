---
phase: 10-stance-formal-proof-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_stance.py
autonomous: true
requirements: [STANCE-07]
must_haves:
  truths:
    - "The order axioms are proven EXHAUSTIVELY over the 4-member domain, not sampled (D-05)"
    - "Antisymmetry is asserted by name — it is literally unasserted today (D-04)"
    - "Transitivity and trichotomy/totality are asserted over all pairs/triples"
    - "No arithmetic/bitwise operator is reachable — a CLOSURE claim over operator × member pairs (D-06)"
    - "Cross-type ordering (<, >, <=, >=) raises TypeError; == / != do NOT raise (Pitfall 5)"
  artifacts:
    - path: "tests/test_stance.py"
      provides: "Exhaustive order-law enumeration + no-arithmetic closure guard"
      contains: "itertools.product(Stance"
  key_links:
    - from: "itertools.product(Stance, repeat=2/3)"
      to: "order axioms"
      via: "pytest.mark.parametrize enumeration"
      pattern: "itertools.product"
    - from: "operator.* functions"
      to: "TypeError closure"
      via: "parametrized closure over member pairs"
      pattern: "operator\\."
---

<objective>
SC2 — turn the order/no-arithmetic scaffold into a mechanical proof. The domain is 4
elements, so EXHAUSTIVELY ENUMERATE the order axioms with `itertools.product(Stance, ...)`
rather than Hypothesis-sampling a tiny space (D-05): a complete enumeration is a proof, a
sample is an anecdote. Antisymmetry is literally unasserted today (D-04) and is the weakest
current spot. Strengthen the no-arithmetic guard from three witnesses to a CLOSURE claim over
every `operator.*` × every member pair (D-06), which also sidesteps the per-line
basedpyright `reportOperatorIssue` ignore wall (operands enter as `Any`-typed function args).

Per D-01, the existing `test_stance_total_order` (one fixed chain + a couple of reflected
operators) is a scaffold, not a proof — the axioms ARE the substance.

Purpose: prove the total order is total AND antisymmetric, and no arithmetic is reachable.
Output: exhaustive order-law enumerations + a closure-form no-arithmetic guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stance-formal-proof-docs/10-CONTEXT.md
@.planning/phases/10-stance-formal-proof-docs/10-RESEARCH.md
@.planning/phases/10-stance-formal-proof-docs/10-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan (in `tests/test_stance.py`):
- `test_irreflexivity` — `not (a < a)` and `not (a > a)` and `a == a` over `list(Stance)`.
- `test_totality_trichotomy` — `(a < b) + (a == b) + (a > b) == 1` over all 16 pairs.
- `test_antisymmetry` — `a < b ⟹ not (b < a)` over all 16 pairs (the named D-04 artifact).
- `test_transitivity` — `a < b and b < c ⟹ a < c` over all 64 triples.
- `test_no_arithmetic_operator_is_reachable` — closure over `operator.*` × pairs (D-06).
- `test_cross_type_comparison_raises` — `<`/`>`/`<=`/`>=` raise for non-Stance operands.
- Module constants `_PAIRS`, `_TRIPLES`, `_ARITH`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive order-law enumeration (D-04, D-05)</name>
  <files>tests/test_stance.py</files>
  <read_first>
    - tests/test_stance.py §16-33 (`test_stance_total_order`, `test_stance_membership_...`).
    - RESEARCH.md "Pattern 3" (itertools.product order laws) and VALIDATION.md "vacuous pass → SC2".
    - src/doxastica/models.py §66-98 (`Stance`, plain Enum + `@total_ordering`, integer rank).
  </read_first>
  <action>
    Add module-level `_PAIRS = list(itertools.product(Stance, repeat=2))` (16) and
    `_TRIPLES = list(itertools.product(Stance, repeat=3))` (64). Add four parametrized enumeration
    tests asserting the axioms, NOT a hardcoded chain (D-05): `test_irreflexivity` over `list(Stance)`
    asserting `not (a < a)` and `not (a > a)` and `a == a`; `test_totality_trichotomy` over `_PAIRS`
    asserting `(a < b) + (a == b) + (a > b) == 1` (simultaneously totality + antisymmetry +
    irreflexivity); `test_antisymmetry` over `_PAIRS` asserting `if a < b: assert not (b < a) and a != b`
    (the named D-04 artifact — antisymmetry is unasserted today); `test_transitivity` over `_TRIPLES`
    asserting `if a < b and b < c: assert a < c`. Keep the existing `test_stance_total_order`,
    `test_stance_membership_is_exactly_the_four_ranks`, `test_stance_hydration_is_name_based`, and
    `test_stance_name_is_the_wire_token` — they are correct example anchors; the new tests are the laws.
    Do NOT change `src/` — the enum is proven ABOUT, not modified.
  </action>
  <verify>
    <automated>uv run pytest tests/test_stance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_stance.py -q` exits 0; the four new tests collect 4 + 16 + 16 + 64 = 100 parametrized cases.
    - `grep -c "itertools.product(Stance" tests/test_stance.py` is ≥ 2.
    - Named antisymmetry artifact present: `grep -c "def test_antisymmetry" tests/test_stance.py` is 1.
    - VACUOUS-PASS DETECTION (VALIDATION SC2): temporarily mutating `Stance.__lt__` to always return
      `False` makes `test_totality_trichotomy` / `test_transitivity` / `test_antisymmetry` FAIL.
  </acceptance_criteria>
  <done>Irreflexivity, antisymmetry, transitivity, and trichotomy/totality are asserted exhaustively over the whole 4-member domain; a broken `__lt__` is caught.</done>
</task>

<task type="auto">
  <name>Task 2: No-arithmetic closure guard (D-06)</name>
  <files>tests/test_stance.py</files>
  <read_first>
    - tests/test_stance.py §36-49 (`test_stance_arithmetic_and_cross_type_raise`, the three-witness form with per-line pyright ignores).
    - RESEARCH.md "Pattern 4" (operator.* closure) and Pitfall 5 (`==`/`!=` do NOT raise cross-type).
  </read_first>
  <action>
    Replace the three-witness `test_stance_arithmetic_and_cross_type_raise` with a CLOSURE form (D-06).
    Add `_ARITH = [operator.add, operator.sub, operator.mul, operator.truediv, operator.floordiv,
    operator.mod, operator.pow, operator.and_, operator.or_, operator.xor, operator.lshift,
    operator.rshift]`. Add `test_no_arithmetic_operator_is_reachable` parametrized over `_ARITH` ×
    `_PAIRS`, asserting `pytest.raises(TypeError)` for `op(a, b)` on every arithmetic/bitwise op × every
    member pair. Add `test_cross_type_comparison_raises` parametrized over `list(Stance)` × representative
    non-Stance operands `[5, "certain", None, 1.5, Stance]`, asserting `pytest.raises(TypeError)` for each
    of `operator.lt/gt/le/ge` — and assert `==`/`!=` do NOT raise (they fall back to identity → `False`/
    `True`, Pitfall 5), matching STANCE-06's precise claim. Using `operator.*` functions passes operands
    as `Any`-typed args, so drop the per-line `# pyright: ignore[reportOperatorIssue]` wall (at most one
    narrow ignore if strict still flags a call site).
  </action>
  <verify>
    <automated>uv run pytest tests/test_stance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_stance.py -q` exits 0.
    - Closure form present: `grep -c "def test_no_arithmetic_operator_is_reachable" tests/test_stance.py` is 1.
    - `==`/`!=` cross-type asserted NON-raising (Pitfall 5): the test file asserts `member == other` returns a bool, not a TypeError.
    - basedpyright is clean on the file under CI-parity env (`uv sync --locked --dev --extra ladybug && prek run --all-files` includes basedpyright) — no scattered `reportOperatorIssue` ignores remain.
  </acceptance_criteria>
  <done>No arithmetic/bitwise operator is reachable across the whole domain (closure), cross-type ordering raises, `==`/`!=` do not raise, and the per-line pyright-ignore wall is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only change; the `Stance` enum is asserted about, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | stance ordinal/arithmetic surface | accept (no new surface) | No new threats introduced (RESEARCH §Security Domain). The no-arithmetic closure guard hardens the existing type-level guarantee; no `src/` code path or input surface changes. |
</threat_model>

<verification>
- `uv run pytest tests/test_stance.py -q` green.
- Vacuous-pass detection: a broken `Stance.__lt__` fails the order-law enumerations.
- Phase gate (once all Wave-1 plans land): `uv sync --locked --dev --extra ladybug && prek run --all-files`.
</verification>

<success_criteria>
- Order proven total AND antisymmetric via exhaustive enumeration (SC2, D-04, D-05).
- No arithmetic operator reachable — closure claim, not three witnesses (SC2, D-06).
- Cross-type ordering raises `TypeError`; `==`/`!=` do not (STANCE-06).
</success_criteria>

<output>
Create `.planning/phases/10-stance-formal-proof-docs/10-02-SUMMARY.md` when done
</output>
