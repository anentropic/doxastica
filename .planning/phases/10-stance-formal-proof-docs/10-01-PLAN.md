---
phase: 10-stance-formal-proof-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_invariants.py
autonomous: true
requirements: [STANCE-07]
must_haves:
  truths:
    - "The stateful shadow oracle records the Stance MEMBER per tracked belief-in-scope entry (D-02)"
    - "The base-comparison key is {belief_id: (value, stance)} everywhere the SUT is compared to the oracle (SC1)"
    - "contract mirrors the prior stance VERBATIM in the oracle (STANCE-04), matching the SUT"
    - "Two ops agreeing on value but differing on stance FAIL K*6 parity instead of passing vacuously (SC1)"
    - "The differing-stance discriminating case is PROVEN reached, not merely asserted (D-03)"
  artifacts:
    - path: "tests/test_invariants.py"
      provides: "Stance-carrying oracle + deterministic non-vacuity discrimination proof"
      contains: "test_widened_key_discriminates_stance"
  key_links:
    - from: "_SpineMachine._shadow_base"
      to: "_SpineMachine._observed_base"
      via: "tuple-equality on (value, stance)"
      pattern: "value, stance"
    - from: "revise/expand rules"
      to: "hypothesis.event()"
      via: "stance-flip observability label"
      pattern: "event\\("
---

<objective>
SC1 — the load-bearing new work. Widen the dual-backend stateful oracle in
`tests/test_invariants.py` so it carries `stance` per belief-in-scope entry and the
state-equality key becomes `{belief_id: (value, stance)}` everywhere the SUT is
compared to the oracle. Then PROVE the widening is non-vacuous (D-03): a deterministic
discrimination test that fails if the widening is reverted, plus a `hypothesis.event()`
label proving the differing-stance revise/expand case is actually generated.

Per D-01, the existing suite is an honest scaffold, not a proof — the quantification IS
the substance. Per D-02, the oracle's stance semantics MUST mirror the SUT exactly
(`contract` copies prior stance verbatim, STANCE-04), or K*2/K*3/K*6 parity false-positives.

Purpose: make stance mechanically proven in the property suite, not vacuously green.
Output: a Stance-carrying oracle + the `test_widened_key_discriminates_stance` proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stance-formal-proof-docs/10-CONTEXT.md
@.planning/phases/10-stance-formal-proof-docs/10-RESEARCH.md
@.planning/phases/10-stance-formal-proof-docs/10-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan:
- `tests/test_invariants.py::test_widened_key_discriminates_stance` — deterministic D-03 non-vacuity proof (both backends).
- `_SpineMachine.Entry` widened to a 5-tuple carrying the `Stance` member.
- A `hypothesis.event()` stance-flip label emitted from the `revise`/`expand` rules.
Widened-in-place (not new): `_record`, `_shadow_current`, `_shadow_base`, `_observed_base`,
`_chain_tail`, the `contract` rule mirror, the keystone `current_is_total_single_valued_and_chain_tail`
invariant, and the standalone `_base_of` / `test_extensionality_k6` K*6 test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Widen the stateful oracle to carry the Stance member (D-02)</name>
  <files>tests/test_invariants.py</files>
  <read_first>
    - tests/test_invariants.py §92-109 (`_chain_tail`), §112-135 (`_SpineMachine`, `Entry`),
      §165-255 (`_record`, `_shadow_current`, `_shadow_base`, `_observed_base`),
      §262-299 (contract rule), §301-330 (`_assert_hansson_success_inclusion`),
      §387-424 (keystone `current_is_total_single_valued_and_chain_tail`), §537-543 (machine subclasses).
    - RESEARCH.md "Pattern 1" — the exact line-by-line widening list and the token-vs-member note.
    - src/doxastica/core.py — `_current` stores `"stance"` as the pre-serialized `.name` token; `contract` copies verbatim (STANCE-04).
  </read_first>
  <action>
    Widen the oracle in place per RESEARCH Pattern 1. Change `Entry = tuple[str, int, Any, str]`
    to `tuple[str, int, Any, str, Stance]`. Add a `stance: Stance` parameter to `_record` and
    append it as the 5th tuple slot. Make `_shadow_current` return a 3-tuple
    `(has_current, value, stance)` (returning `(False, None, None)` on empty/retracted) and unpack
    the 5-tuple in its `max(...)`. Make `_shadow_base` build `base[b] = (value, stance)` and
    `_observed_base` return `{s.belief_id: (s.value, s.stance) ...}`. Add `stance=st.sampled_from(Stance)`
    to the `revise` and `expand` rule signatures and pass it to both `core.revise(...)`/`core.expand(...)`
    and `_record(...)`. In the `contract` rule, unpack the widened `_shadow_current` and mirror the
    retracted tail with `_record(..., "retracted", prior_stance)` — this is the STANCE-04 verbatim-copy
    match point (D-02): copying a default here instead of `prior_stance` false-positives K*2/K*3.
    Add the third return slot to the second `_shadow_current` unpack in `_assert_hansson_success_inclusion`.
    In the keystone `current_is_total_single_valued_and_chain_tail`, unpack `expected_stance` and add
    `assert Stance[current["stance"]] is expected_stance` — name-lookup, NOT `Stance(current["stance"])`
    value-lookup (Pitfall 3). Extend `_chain_tail` to return `"stance": tail.stance` (a member) and
    assert it in the keystone alongside `state_id`. Store the Stance MEMBER (not the `.name` token) in
    the oracle so `_observed_base == _shadow_base` is direct tuple-equality (Pattern 1 rationale).
    Do NOT change any `src/` code — the SUT is correct as-shipped from Phase 9.
  </action>
  <verify>
    <automated>uv run pytest tests/test_invariants.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_invariants.py -q` exits 0 on both backends (ladybug SKIP-not-fail if driver absent).
    - `grep -n "tuple\[str, int, Any, str, Stance\]" tests/test_invariants.py` matches the widened `Entry`.
    - `grep -c "value, stance" tests/test_invariants.py` is ≥ 3 (shadow_base, observed_base, shadow_current unpacks).
    - The keystone uses `Stance[current["stance"]]` (name-lookup), NOT `Stance(current["stance"])`:
      `grep -c 'Stance(current\["stance"\])' tests/test_invariants.py` is 0.
  </acceptance_criteria>
  <done>The oracle records the Stance member, the base key is (value, stance) everywhere, contract mirrors verbatim, and the full invariants suite is green on both backends.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the widening is non-vacuous (D-03, MANDATORY)</name>
  <files>tests/test_invariants.py</files>
  <read_first>
    - tests/test_invariants.py §568-588 (`_build_backend` per-example helper, `_base_of`),
      §622-648 (standalone `test_extensionality_k6`).
    - RESEARCH.md "Pattern 2" (deterministic discrimination + event()) and Pitfall 1 (vacuous pass),
      VALIDATION.md "How to Detect a VACUOUS pass → SC1".
  </read_first>
  <action>
    Add the deterministic discrimination proof `test_widened_key_discriminates_stance` next to
    `_base_of`, parametrized `("backend_kind", ["memory", "ladybug"])` and built via `_build_backend`.
    It revises belief `b1` at some value in scope `alice` with `stance=Stance.believed` and expands the
    SAME `b1`/value in scope `bob` with `stance=Stance.certain`, projects each scope to a
    `{belief_id: (value, stance)}` dict, and asserts the two dicts are UNEQUAL (they agree on value,
    differ on stance) plus each equals its exact expected `(value, Stance.<member>)` entry. This test
    FAILS if the widening is reverted to value-only — it is the proof the guard guards (VALIDATION SC1).
    Widen the standalone K*6 path so it too carries stance: change `_base_of` to project
    `{belief_id: (value, stance)}` and update `test_extensionality_k6` so `revise` and `expand` are
    driven with a stance draw (add `stance=st.sampled_from(Stance)` via `@given`) and the byte-identical
    assertion compares the `(value, stance)` bases — proving K*6 (`revise ≡ expand`) now compares stance.
    Also emit a `hypothesis.event("write flips the current stance of an existing belief")` in the
    stateful `revise`/`expand` rules when the new op changes the current winning stance for its
    `(scope, belief)` (compute BEFORE mirroring the op), so `--hypothesis-show-statistics` documents the
    discriminating path is exercised. Adjust `test_vacuity_k4`'s `_base_of` expectations to the widened
    `(value, stance)` shape (default stance is `certain`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_invariants.py -q --hypothesis-show-statistics</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_invariants.py::test_widened_key_discriminates_stance -q` exits 0 on both backends.
    - VACUOUS-PASS DETECTION (VALIDATION SC1): reverting the `_base_of` widening to `{belief_id: value}`
      makes `test_widened_key_discriminates_stance` FAIL — the test is not vacuous.
    - `event("write flips the current stance of an existing belief")` appears in the
      `--hypothesis-show-statistics` output for the stateful machine run.
    - `grep -c "test_widened_key_discriminates_stance" tests/test_invariants.py` is ≥ 1.
    - Full file green: `uv run pytest tests/test_invariants.py -q` exits 0.
  </acceptance_criteria>
  <done>The differing-stance case is proven reached (deterministic test + event label), K*6 now compares stance, and reverting the widening breaks the guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only change; no new external input surface crosses into `src/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | stance token on the wire | accept (no new surface) | No new threats introduced (RESEARCH §Security Domain). Stance is already serialized as a closed-enum `.name` and hydrated via `Stance[token]` name-lookup — unchanged by this test-only plan. |
</threat_model>

<verification>
- `uv run pytest tests/test_invariants.py -q` green on both backends (ladybug SKIP-not-fail if driver absent).
- Vacuous-pass detection passes: reverting the base-key widening breaks `test_widened_key_discriminates_stance`.
- Phase gate (run once all Wave-1 plans land): `uv sync --locked --dev --extra ladybug && prek run --all-files`.
</verification>

<success_criteria>
- Oracle records the Stance member per entry; base key is `{belief_id: (value, stance)}` everywhere (SC1).
- `contract` mirrors prior stance verbatim in the oracle (STANCE-04) — no false positive.
- Non-vacuity is PROVEN (D-03): deterministic discrimination test + `event()` label.
- K*6 Extensionality parity now compares stance (SC1).
</success_criteria>

<output>
Create `.planning/phases/10-stance-formal-proof-docs/10-01-SUMMARY.md` when done
</output>
