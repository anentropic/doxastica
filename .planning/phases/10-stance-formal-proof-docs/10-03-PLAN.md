---
phase: 10-stance-formal-proof-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_stance_persistence.py
autonomous: true
requirements: [STANCE-07]
must_haves:
  truths:
    - "Round-trip, contract-verbatim, and get_scope_at hold for ALL FOUR stance members (D-08)"
    - "Each property is proven on BOTH backends (memory + ladybug), SKIP-not-fail when ladybug absent"
    - "The is-identity assertions are kept so a name-vs-value hydrate regression raises loud (D-07)"
    - "Quantification is exhaustive over the 4-member domain via parametrize, not a single pinned witness (D-05, D-07)"
  artifacts:
    - path: "tests/test_stance_persistence.py"
      provides: "Stance-quantified persistence proofs over list(Stance) × backend"
      contains: "parametrize(\"stance\", list(Stance))"
  key_links:
    - from: "pytest.mark.parametrize(stance, list(Stance))"
      to: "backend fixture (params=[memory, ladybug])"
      via: "8 cases per property, exhaustive × dual-backend"
      pattern: "list\\(Stance\\)"
---

<objective>
SC3 — quantify persistence over stance. `tests/test_stance_persistence.py` is a solid
backbone (backend-parametrized, `is`-identity so a name-vs-value hydrate regression raises
loud, driven through `MemoryCore` not the bare port) — KEEP that shape (D-07). But each test
pins ONE stance today; a `doubted`-only hydrate bug would sail through. Add
`@pytest.mark.parametrize("stance", list(Stance))` so round-trip, `contract`-verbatim
(STANCE-04), and `get_scope_at` reconstruction (STANCE-05) hold for ALL FOUR members on BOTH
backends (D-08).

Per D-05 discipline (enumerate tiny domains, Hypothesis for large ones), use exhaustive
`parametrize` over `list(Stance)` — NOT `@given(st.sampled_from(Stance))`, which would trip the
`function_scoped_fixture` health check against the `backend` fixture and bleed state across
examples (Pitfall 2). Exhaustive parametrize is stronger AND simpler here.

Purpose: prove stance persistence holds for every member, both backends.
Output: the three persistence properties quantified over `list(Stance)`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stance-formal-proof-docs/10-CONTEXT.md
@.planning/phases/10-stance-formal-proof-docs/10-RESEARCH.md
@.planning/phases/10-stance-formal-proof-docs/10-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Widened-in-place (in `tests/test_stance_persistence.py`), now quantified over `list(Stance)`:
- `test_stance_round_trips_byte_stable` — every member survives `revise → query_scope`.
- `test_contract_preserves_stance_verbatim` — every member copied verbatim onto the retracted tail (STANCE-04).
- `test_get_scope_at_reconstructs_stance` — every member reconstructed by time-travel (STANCE-05).
Each × the `backend` fixture (`params=["memory","ladybug"]`) = 8 cases per property.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Quantify round-trip over list(Stance) × backend (D-08)</name>
  <files>tests/test_stance_persistence.py</files>
  <read_first>
    - tests/test_stance_persistence.py §49-70 (`test_stance_round_trips_byte_stable`, `test_stance_defaults_to_certain`).
    - tests/conftest.py §34-56 (`backend` fixture, `params=["memory","ladybug"]`, `importorskip` SKIP-not-fail).
    - RESEARCH.md "Pattern 5" and "Alternatives Considered" (why parametrize, not `@given`, on the fixture-based tests — Pitfall 2).
  </read_first>
  <action>
    Add `@pytest.mark.parametrize("stance", list(Stance))` above `test_stance_round_trips_byte_stable`,
    give it a `stance: Stance` parameter, drive `core.revise("alice", "b1", "v", _event_id(), stance=stance)`,
    and assert `state.stance is stance` (member-identity — a value-vs-name hydrate bug raises loud, D-07)
    plus `state.stance.name == stance.name`. Keep the existing `test_stance_defaults_to_certain` as-is
    (it proves the omitted default, orthogonal to the quantification). Do NOT combine `@given` with the
    `backend` fixture (Pitfall 2); use `parametrize` — exhaustive over 4 members × 2 backends = 8 cases.
    No `src/` change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_stance_persistence.py::test_stance_round_trips_byte_stable -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_stance_persistence.py::test_stance_round_trips_byte_stable -q` exits 0; collects 8 cases (4 members × 2 backends), ladybug SKIP-not-fail if driver absent.
    - `is`-identity assertion retained: `grep -c "state.stance is stance" tests/test_stance_persistence.py` is ≥ 1.
    - No `@given` on a `backend`-fixture test: `grep -c "@given" tests/test_stance_persistence.py` is 0.
  </acceptance_criteria>
  <done>Round-trip byte-stability is proven for all four members on both backends, with member-identity assertions intact.</done>
</task>

<task type="auto">
  <name>Task 2: Quantify contract-verbatim + get_scope_at over list(Stance) × backend (STANCE-04/05)</name>
  <files>tests/test_stance_persistence.py</files>
  <read_first>
    - tests/test_stance_persistence.py §78-103 (`test_contract_preserves_stance_verbatim`, `test_get_scope_at_reconstructs_stance`).
    - VALIDATION.md "vacuous pass → SC3" (a `doubted`-only hydrate bug must raise for the `doubted` parametrization).
  </read_first>
  <action>
    Add `@pytest.mark.parametrize("stance", list(Stance))` to `test_contract_preserves_stance_verbatim`
    (drive `core.revise("alice", "p", "v", _event_id(), stance=stance)`, contract, then assert the
    retracted tail `retracted.stance is active.stance` — verbatim copy, STANCE-04) and to
    `test_get_scope_at_reconstructs_stance` (revise with `stance=stance`, time-travel via `get_scope_at`,
    assert `state.stance is stance` — STANCE-05). Keep the `is`-identity assertions and the
    drive-through-`MemoryCore(backend)` shape (D-07). Exhaustive over 4 members × 2 backends per property.
    No `src/` change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_stance_persistence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_stance_persistence.py -q` exits 0; each quantified property collects 8 cases.
    - `grep -c "parametrize(\"stance\", list(Stance))" tests/test_stance_persistence.py` is ≥ 3 (all three properties quantified).
    - VACUOUS-PASS DETECTION (VALIDATION SC3): introducing a `doubted`-only hydrate bug
      (`Stance(props["stance"])` value-lookup in `core.py`) makes the `is`-identity assertion RAISE for the `doubted` parametrization.
  </acceptance_criteria>
  <done>contract-verbatim and get_scope_at reconstruction are proven for all four members on both backends; a member-specific hydrate bug is caught.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only change driven through `MemoryCore`; no new external input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | stance hydrate path (name-token → member) | accept (no new surface) | No new threats introduced (RESEARCH §Security Domain). The `is`-identity assertions harden the existing name-lookup hydrate discipline; no `src/` change. |
</threat_model>

<verification>
- `uv run pytest tests/test_stance_persistence.py -q` green on both backends (ladybug SKIP-not-fail if driver absent).
- Vacuous-pass detection: a `doubted`-only hydrate bug raises for the `doubted` case.
- Phase gate (once all Wave-1 plans land): `uv sync --locked --dev --extra ladybug && prek run --all-files`.
</verification>

<success_criteria>
- Round-trip / contract-verbatim / get_scope_at hold for ALL four members on BOTH backends (SC3, D-08).
- `is`-identity assertions retained (D-07); no `@given` on fixture-based tests (Pitfall 2).
</success_criteria>

<output>
Create `.planning/phases/10-stance-formal-proof-docs/10-03-SUMMARY.md` when done
</output>
