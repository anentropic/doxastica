---
phase: 07-agm-hansson-conformance-suite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_recovery_xfail.py
autonomous: true
requirements: [FORMAL-04, BACK-05]
must_haves:
  truths:
    - "AGM Recovery is present as a single, loud, NAMED strict xfail with a documented rationale — never a passing assertion against correct code"
    - "The Recovery counterexample is deterministic and reports as `xfailed` (NOT `xpassed`, NOT `failed`)"
    - "The strict=True flag is on the @pytest.mark.xfail mark itself (no global xfail_strict exists), so a drift toward closed belief sets XPASSes -> suite red"
    - "Superseded-chain replacement POSITIVES pass on both backends: after contract(p) then revise(p,v') the chain reads active->retracted->active, current resolves to v', the old value is NOT restored, the contracted state is retained (append-only)"
    - "The Recovery xfail and the superseded-chain positives stay DISTINCT from temporal recoverability (get_scope_at) — Recovery is never asserted via time-travel"
  artifacts:
    - path: "tests/test_recovery_xfail.py"
      provides: "The strict-xfail Recovery counterexample + the D-05 superseded-chain replacement positives over the backend fixture"
      contains: "xfail(strict=True"
  key_links:
    - from: "tests/test_recovery_xfail.py test_superseded_chain_replaces_recovery"
      to: "MemoryCore.get_revision_chain / query_scope"
      via: "asserts chain reads active->retracted->active and current == v'"
      pattern: "get_revision_chain"
---

<objective>
Create `tests/test_recovery_xfail.py` (NEW): the single deliberate AGM exclusion. Encode Recovery
as a deterministic, hand-built belief-base counterexample that ASSERTS Recovery's (closed-set)
conclusion, marked `@pytest.mark.xfail(strict=True, reason=...)` — the assertion FAILS against the
correct superseded-chain engine, so the xfail is GREEN; if the engine ever drifts to satisfy
Recovery, the test XPASSes and the suite goes RED (the drift guard). Plus the D-05 superseded-chain
REPLACEMENT positives that pass in Recovery's place.

Purpose: FORMAL-04 (Recovery strict xfail + superseded-chain positives); BACK-05 for the positives.
Output: `tests/test_recovery_xfail.py` with one strict-xfail counterexample + the passing positives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agm-hansson-conformance-suite/07-CONTEXT.md
@.planning/phases/07-agm-hansson-conformance-suite/07-RESEARCH.md
@.planning/phases/07-agm-hansson-conformance-suite/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (exclude from drift checks):

- `tests/test_recovery_xfail.py` — NEW file with:
  - `test_recovery_does_not_hold_for_belief_bases` — the strict-xfail Recovery counterexample
    (RATIFIED base below; @pytest.mark.xfail(strict=True, reason=...))
  - `test_superseded_chain_replaces_recovery(backend)` — the D-05 PASSING positives over the
    `backend` fixture (both backends)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The strict-xfail Recovery counterexample (FORMAL-04, D-04)</name>
  <files>tests/test_recovery_xfail.py</files>
  <read_first>
    - tests/test_backend_parity.py (lines 30-43 the imports + `MemoryCore` value-round-trip idiom; the xfail-flip discipline)
    - tests/test_cascade.py (lines 13-14 the explicit "Relevance/Core-Retainment POSTULATE tests are Phase 7" deferral; 29-46 the new-file header/import style) — for file-style consistency
    - src/doxastica/core.py (lines 128-133 `MemoryCore.in_memory()`; 382-427 `contract` append-only retracted copy; the `revise`/`query_scope` bodies)
    - 07-PATTERNS.md section "`tests/test_recovery_xfail.py` — NEW (FORMAL-04)" (the strict-xfail mark + the RATIFIED base + the Open-Q1 resolution)
    - 07-RESEARCH.md Pattern 5 + Open Question 1 + Pitfall 1 + Pitfall 3 (the erroneous-XPASS trap, the per-mark `strict=True` requirement) and the verified `pyproject.toml:68-69` `addopts="-v"` / NO `xfail_strict` fact
  </read_first>
  <action>
    Create `tests/test_recovery_xfail.py` with a single deterministic function
    `test_recovery_does_not_hold_for_belief_bases` marked
    `@pytest.mark.xfail(strict=True, reason="AGM Recovery excluded — belief base (not closed set);
    Hansson; replaced by superseded-chain semantics")`. CRITICAL config trap (verified
    `pyproject.toml` has only `addopts = "-v"`, NO `xfail_strict`): `strict=True` MUST be on the mark
    itself or the drift guard silently fails on XPASS (Pitfall 3). RATIFIED counterexample base
    (resolves Open Q1 / Pitfall 1 — the naive `{p,q}` base does NOT bite because independent `q`
    survives): use a SINGLE belief `p` re-asserted at a NEW value, in scope "s":
    `revise("s","p","v", e())` -> `contract("s","p", e())` -> `revise("s","p","vprime", e())`, drawing
    each `source_event_id` from `uuid.uuid7()` in monotonic call order. Compute the observed base
    `{belief_id: value}` from `query_scope("s", BeliefFilter())`. Assert the CLOSED-SET Recovery
    conclusion that the superseded chain correctly DENIES: `assert base == {"p": "v"}` (closed-set
    Recovery would restore the pre-contraction content; the superseded chain does not resurrect
    superseded content, so `base == {"p": "vprime"}` and the assertion FAILS -> xfail GREEN). Document
    in the docstring: (a) why this is excluded (belief base, not closed set — Hansson), (b) that the
    superseded chain replaces it, (c) that `strict=True` is the drift guard, (d) the explicit
    distinction from temporal recoverability (`get_scope_at` is a SEPARATE held property — Pitfall 5;
    do NOT call `get_scope_at` here). Use `MemoryCore.in_memory()` (the memory backend is the AGM
    oracle; this single counterexample need not be parametrized — it is a deterministic negative
    result, not a conformance sweep).
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery_xfail.py::test_recovery_does_not_hold_for_belief_bases -q -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_recovery_xfail.py::test_recovery_does_not_hold_for_belief_bases -q -rxX` reports the test as `xfailed` (shows `x`), NOT `xpassed` (`X`), NOT `failed`.
    - `grep -nE 'xfail\(\s*strict=True' tests/test_recovery_xfail.py` returns the mark (strict=True is on the mark, not relying on a global).
    - The docstring states the exclusion rationale, the superseded-chain replacement, and the distinction from temporal recoverability.
    - The test does NOT call `get_scope_at`: `grep -c 'get_scope_at' tests/test_recovery_xfail.py` returns 0.
  </acceptance_criteria>
  <done>Recovery is a loud, named, deterministic strict xfail that reports `xfailed` against the correct engine and would go red (XPASS) if the engine ever satisfied Recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Superseded-chain replacement positives (FORMAL-04, D-05, BACK-05)</name>
  <files>tests/test_recovery_xfail.py</files>
  <read_first>
    - tests/test_backend_parity.py (the `backend: BackendPort` fixture consumption + `MemoryCore(backend)` injection idiom + value-round-trip)
    - tests/test_scope_at.py (lines 58-69 the `MemoryCore(backend)` injected-fixture-port precedent for function tests)
    - src/doxastica/core.py (lines 547-557 `get_revision_chain` cross-scope `_order_key`-sorted; 382-427 `contract`; the `revise`/`query_scope` bodies)
    - 07-PATTERNS.md section "The superseded-chain replacement POSITIVES (D-05)" (the exact assertion shape) and the Boundary note (keep distinct from temporal recoverability)
    - tests/conftest.py (the `backend` fixture — `params=["memory","ladybug"]` + importorskip)
  </read_first>
  <action>
    Add `test_superseded_chain_replaces_recovery(backend: BackendPort)` to the same file, consuming the
    `conftest.py` `backend` fixture (so it runs on BOTH backends — BACK-05; ladybug SKIPS when absent).
    Construct `MemoryCore(backend)`, then `revise("s","p","v", e())` -> `contract("s","p", e())` ->
    `revise("s","p","vprime", e())` (each `e()` a fresh `uuid.uuid7()`). Assert the D-05 replacement
    behaviour (these PASS): (a) `get_revision_chain("p")` statuses read `["active","retracted","active"]`
    in order; (b) `chain[-1].value == "vprime"` (current resolves to v', not v); (c) the contracted
    state is retained — at least one `retracted` state remains in the chain (append-only, nothing
    deleted); (d) the observed base `query_scope("s", BeliefFilter())` maps `p -> "vprime"` (the old
    value is NOT silently restored). State in the docstring that this is Recovery's REPLACEMENT and is
    DISTINCT from temporal recoverability (`get_scope_at`) — do NOT call `get_scope_at` here (Pitfall
    5). Compare DECODED values where the chain/`query_scope` surface returns them (the public
    `BeliefState.value` is already decoded; if reading raw backend rows, use `MemoryCore._decode_value`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery_xfail.py -q -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_recovery_xfail.py -q -rxX` passes overall (the positives PASS, the Recovery test `xfailed`), exit code 0.
    - `uv run --extra ladybug pytest tests/test_recovery_xfail.py -q -rxX` passes on both backends (the positives run under both `memory` and `ladybug` params).
    - When ladybug is absent, the `ladybug`-param positives SKIP (not fail).
    - `grep -c 'get_scope_at' tests/test_recovery_xfail.py` returns 0 (kept distinct from temporal recoverability).
    - The chain-status assertion `["active", "retracted", "active"]` is present.
  </acceptance_criteria>
  <done>The superseded-chain replacement positives pass on both backends, proving the documented replacement for Recovery while the strict-xfail records the deliberate exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> MemoryCore -> BackendPort | Only boundary; all inputs are hand-built deterministic ops + fixture-provided backends |
| (N/A) network / auth / session / multi-user | None — embedded single-process library test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | Per-test backend DBs | mitigate | Positives consume the existing `backend` fixture (throwaway `:memory:`/in-memory per test, fixture-managed close); the xfail uses `MemoryCore.in_memory()` (zero-dep, no file) — no shared state, no path lock |
| T-07-04 | Repudiation/Integrity | Silent drift away from belief-base semantics | mitigate | `@pytest.mark.xfail(strict=True)` on the mark itself (no global `xfail_strict`): any drift to satisfying Recovery XPASSes and turns the suite red — a mechanically-guarded negative result |
| T-07-WEB | S/T/R/I/D/E (web app threats) | — | accept | N/A — no network/auth/session/untrusted-input surface; deterministic in-process test code, no new dependency |
</threat_model>

<verification>
- `uv run pytest tests/test_recovery_xfail.py -q -rxX` exit 0; Recovery shows `x` (xfailed), positives pass.
- `uv run --extra ladybug pytest tests/test_recovery_xfail.py -q -rxX` exit 0 on both backends.
- `uv run ruff check tests/test_recovery_xfail.py` clean; `uv run basedpyright tests/test_recovery_xfail.py` clean.
</verification>

<success_criteria>
- Recovery is a loud, named, deterministic `@pytest.mark.xfail(strict=True)` counterexample reporting `xfailed` against correct code, with a drift guard that goes red on XPASS (FORMAL-04, D-04).
- The superseded-chain replacement positives pass on both backends (FORMAL-04, D-05, BACK-05).
- Recovery and its positives are kept distinct from temporal recoverability (no `get_scope_at` call).
</success_criteria>

<output>
Create `.planning/phases/07-agm-hansson-conformance-suite/07-03-SUMMARY.md` when done.
</output>
