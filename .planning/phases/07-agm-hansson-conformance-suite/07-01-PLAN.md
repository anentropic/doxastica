---
phase: 07-agm-hansson-conformance-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_invariants.py
  - src/doxastica/core.py
autonomous: true
requirements: [FORMAL-01, FORMAL-02, FORMAL-03, BACK-05]
must_haves:
  truths:
    - "AGM revision postulates K*2 Success, K*3 Inclusion, K*5 Consistency hold on every step of every op sequence on BOTH backends (memory + ladybug-when-present)"
    - "AGM K*4 Vacuity and K*6 Extensionality hold as single-operation properties on both backends"
    - "Hansson Contraction Success, Inclusion, Relevance, Core-Retainment hold on every step on both backends (phrased as superseded-chain surgical contraction, D-07)"
    - "Hansson Uniformity (idempotent re-contraction) holds on both backends"
    - "CURRENT_STATE uniqueness (single-valued derived-current theorem), chain immutability, and world-scope no-contraction are registered in the named conformance set on both backends"
    - "Every postulate compares the core's observed base against the INDEPENDENT shadow oracle — never the SUT against itself (anti-tautology, D-06)"
  artifacts:
    - path: "tests/test_invariants.py"
      provides: "AGM + Hansson postulate @invariant/@given assertions on the extended _SpineMachine, plus the named FORMAL-03 structural set"
      contains: "_shadow_current"
    - path: "src/doxastica/core.py"
      provides: "Extracted pure rows->current-tails helper single-sourcing the _order_key group-by-max for query_scope and (later) the irony join (D-01a)"
      contains: "_order_key"
  key_links:
    - from: "tests/test_invariants.py postulate assertions"
      to: "_shadow_current / self.entries (the independent oracle)"
      via: "expected computed only from oracle, SUT read via query_scope/_current"
      pattern: "_shadow_current|self\\.entries"
    - from: "src/doxastica/core.py query_scope"
      to: "the extracted rows->tails helper"
      via: "query_scope steps 3-4 delegate to the shared helper"
      pattern: "def query_scope"
---

<objective>
Extend the Phase-3 keystone `tests/test_invariants.py` `_SpineMachine` in place with the AGM
revision postulate suite (K*2/K*3/K*5 as `@invariant`s, K*4/K*6 as `@given` per D-06a), the Hansson
base-contraction postulate suite (Contraction Success/Inclusion/Relevance/Core-Retainment as
`@invariant`s, Uniformity per D-06a), and register the named FORMAL-03 structural set
(`CURRENT_STATE` uniqueness theorem, chain immutability, world-scope no-contraction — all three
already present, route into the conformance set per D-08). Also extract the optional pure
`rows -> current tails` helper into `core.py` (D-01a) so `query_scope` and the Wave-2 irony join
single-source the ONE `_order_key` group-by-max.

This is the Wave-1 keystone the other plans reference. It adds NO Protocol operation — only test
assertions over the nine shipped ops plus one behaviour-preserving helper extraction.

Purpose: FORMAL-01 (AGM K*2–K*6), FORMAL-02 (Hansson 5 postulates), the `test_invariants.py` half of
FORMAL-03, and BACK-05 (transversal via the existing two-subclass `.TestCase` idiom).
Output: extended `_SpineMachine` postulate/invariant assertions + the `core.py` `rows -> tails` helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agm-hansson-conformance-suite/07-CONTEXT.md
@.planning/phases/07-agm-hansson-conformance-suite/07-RESEARCH.md
@.planning/phases/07-agm-hansson-conformance-suite/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates/extends these symbols (the source-grounding pass MUST exclude them from drift checks):

- `tests/test_invariants.py` — NEW `@invariant` methods on `_SpineMachine`:
  - `agm_success_inclusion_consistency` (or one method per K*2/K*3/K*5) — AGM revision postulates
  - `hansson_contraction_success_inclusion`, `hansson_relevance_core_retainment` — Hansson postulates
  - (registration only, NO new code) `current_is_total_single_valued_and_chain_tail`,
    `chain_is_immutable`, `world_contract_raises` named into the FORMAL-03 conformance set via docstrings/comment registry
- `tests/test_invariants.py` — NEW standalone `@given` tests over the `backend` fixture:
  - `test_vacuity_k4`, `test_extensionality_k6`, `test_uniformity`
- `src/doxastica/core.py` — NEW pure helper extracted from `query_scope` steps 3-4 (D-01a),
  neutrally named, e.g. `_current_tails(rows, allowed)` returning `{belief_id: tail}` /
  `list[tail]`; `query_scope` refactored to call it (behaviour-preserving). Exact signature is the
  executor's to finalize, recorded in the SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the rows -> current-tails helper in core.py (D-01a)</name>
  <files>src/doxastica/core.py</files>
  <read_first>
    - src/doxastica/core.py (lines 86-112 `_order_key`/`_is_active_tail`; 208-248 `_current_tail`/`_current`; 251-266 `_encode_value`/`_decode_value`; 602-610 the `query_scope` steps 3-4 to extract; and the full `query_scope` body for the call site)
    - 07-PATTERNS.md section "`src/doxastica/core.py` — OPTIONAL `rows → tails` helper extraction" (the exact steps + constraints)
    - tests/test_import_purity.py (the driver-blind static guard the helper must not break)
    - tests/test_query_scope.py (the behaviour-preserving contract the refactor must keep green)
  </read_first>
  <action>
    Extract `query_scope`'s steps 3-4 (group-by-`belief_id` -> per-group ordering-MAX via `_order_key`
    -> status filter AFTER the max) into a NEW pure module-level or static helper in core.py,
    neutrally named (e.g. `_current_tails`), taking the already-scanned/scope-filtered `rows` plus the
    allowed-status set and returning the per-`belief_id` current tails. Refactor `query_scope` to call
    it so the ONE `_order_key` group-by-max is single-sourced (per D-01a). CRITICAL: keep it
    behaviour-preserving — the status filter MUST run AFTER the per-belief max (pre-filtering to
    `active` leaks a stale active value below a retracted tail; RESEARCH Anti-Pattern + Pitfall 2).
    Keep it driver-blind (compose only stdlib + `_order_key`/`Status`; NO `ladybug` import) so
    test_import_purity.py stays green. Do NOT change `_order_key`, `_current_tail`, `_current`, or any
    public signature. This helper is the single-source the Wave-2 irony join (07-04) reuses.
  </action>
  <verify>
    <automated>uv run pytest tests/test_query_scope.py tests/test_invariants.py tests/test_import_purity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_query_scope.py -q` passes (behaviour-preserving refactor).
    - `uv run pytest tests/test_import_purity.py -q` passes (core stays driver-blind; no `ladybug` import added).
    - `uv run basedpyright src/doxastica/core.py` reports no new errors.
    - `query_scope` calls the new helper rather than inlining the group-by-max loop.
  </acceptance_criteria>
  <done>The group-by-max + status-filter logic exists as a single pure helper, `query_scope` delegates to it, and all existing core tests stay green on both backends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AGM revision postulate assertions to _SpineMachine (FORMAL-01, BACK-05)</name>
  <files>tests/test_invariants.py</files>
  <behavior>
    - K*2 Success (@invariant): after `revise(scope,p,v)`, `p` is present in `query_scope(scope)` with decoded value == v.
    - K*3 Inclusion (@invariant): keys(observed base after revise) == prior keys ∪ {p} (revise ≡ expand: adds the tail, removes only the superseded same-belief tail).
    - K*5 Consistency (@invariant): no `belief_id` appears twice in `query_scope(scope)` (single-valued base).
    - K*4 Vacuity (@given, single-op): revising a fresh `belief_id` == expanding it (prior base ∪ {(p,v)}).
    - K*6 Extensionality (@given, single-op): `revise(s,p,v)` and `expand(s,p,v)` yield byte-identical `query_scope` projections.
    - Every assertion computes "expected" ONLY from the independent oracle (`_shadow_current`/`self.entries`), never from a second SUT call.
  </behavior>
  <read_first>
    - tests/test_invariants.py (lines 62-66, 82-84 `_EVENT_POOL` + `st.sampled_from`; 87-104 `_chain_tail` independent cross-check; 121-183 `self.entries` + `_shadow_current`; 198-255 the write rules + `_record`; 258-315 the keystone `@invariant` shape; 335-355 the two-subclass `.TestCase` idiom)
    - 07-PATTERNS.md section "`tests/test_invariants.py` — EXTEND `_SpineMachine`" (postulate assignment guidance + the oracle/pool/idiom to preserve)
    - 07-RESEARCH.md Pattern 3 (the AGM postulate -> doxastica-phrasing table, K*1 dropped, K*2-K*6 shapes) and Pitfall 2 (anti-tautology) and Pitfall 6 (colliding event pool)
    - tests/conftest.py (the `backend` fixture for the `@given` single-op tests)
  </read_first>
  <action>
    Extend `_SpineMachine` IN PLACE (do NOT rewrite it, do NOT spawn a parallel machine — D-06).
    Add sequence-sensitive AGM postulates as `@invariant` methods comparing `query_scope(scope,
    BeliefFilter())` (the observed base = AGM K) against `_shadow_current` over `_SCOPE_POOL` x
    `_BELIEF_POOL`: K*2 Success, K*3 Inclusion (post-base keys == prior keys ∪ {p}), K*5 Consistency
    (no duplicate `belief_id`). Use the doxastica phrasings from RESEARCH Pattern 3 (revise ≡ expand;
    K*3/K*4 collapse to expansion identities — there is no consistency engine). Add K*4 Vacuity and
    K*6 Extensionality as standalone `@given` tests over the `backend` fixture (single-op, D-06a):
    Vacuity = revising a fresh `belief_id` equals expanding it; Extensionality = `revise(s,p,v)` and
    `expand(s,p,v)` produce byte-identical `query_scope` projections. MANDATORY discipline: compute
    expected ONLY from the oracle (`_shadow_current`/`self.entries`) — NEVER call `query_scope` /
    `_current` on both sides of an assertion (Pitfall 2). Any new write rule MUST draw
    `source_event_id` from `_EVENT_POOL` via `st.sampled_from`, never a fresh `uuid7()` (Pitfall 6).
    BACK-05 is inherited transversally — the existing `Memory*`/`Ladybug*` `.TestCase` subclasses run
    every new `@invariant` on both backends with no per-backend assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/test_invariants.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_invariants.py -q` passes (memory backend).
    - `uv run --extra ladybug pytest tests/test_invariants.py -q` passes (both backends; ladybug subclass runs, not skipped).
    - When ladybug is absent, `uv run pytest tests/test_invariants.py -q` shows the `Ladybug*` cases SKIPPED (not failed).
    - Grep confirms no new AGM postulate assertion calls `self.core.query_scope` or `self.core._current` on BOTH sides: `grep -nE 'def (agm|test_vacuity|test_extensionality)' tests/test_invariants.py` returns the new methods; their bodies reference `_shadow_current`/`self.entries`.
    - New write rules (if any) use `st.sampled_from(_EVENT_POOL)`, not `uuid.uuid7()` directly.
  </acceptance_criteria>
  <done>K*2/K*3/K*5 ride the machine as `@invariant`s and K*4/K*6 are `@given` single-op tests, all comparing against the independent oracle, all green on both backends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Hansson contraction postulates + register the FORMAL-03 structural set (FORMAL-02, FORMAL-03, BACK-05)</name>
  <files>tests/test_invariants.py</files>
  <behavior>
    - Contraction Success (@invariant): after `contract(scope,p,e)` on an asserted key, `p` is ABSENT from `query_scope(scope)`.
    - Inclusion (@invariant): keys(base after contract) ⊆ keys(base before) — contraction adds no new asserted belief.
    - Relevance + Core-Retainment (@invariant, collapse to the same surgical claim under D-07): the symmetric difference of the bases across `contract(p)` is exactly {p}; for every `belief_id ≠ p` the current tail is byte-identical before and after.
    - Uniformity (@given/@invariant per D-06a): contracting the same key twice is idempotent at the base level (second is a vacuous no-op, D-05).
    - FORMAL-03 named set: `current_is_total_single_valued_and_chain_tail` (CURRENT_STATE-uniqueness theorem), `chain_is_immutable`, `world_contract_raises` registered into the conformance set (already present — register, do not re-implement).
  </behavior>
  <read_first>
    - tests/test_invariants.py (lines 198-255 the `contract` `@precondition`-gated rule + `_asserted_keys()` + `world_contract_raises` already at :246-255; 258-315 `current_is_total_single_valued_and_chain_tail` + `chain_is_immutable` already present)
    - src/doxastica/core.py (lines 382-427 the `contract` body — append-only retracted copy; 547-557 `get_revision_chain`)
    - 07-PATTERNS.md section on Hansson postulates + the "register the named conformance set per D-08" note (world_contract_raises is already a `@rule`)
    - 07-RESEARCH.md Pattern 4 (the Hansson postulate -> superseded-chain phrasing table; the "Relevance + Core-Retainment collapse to symmetric-difference-is-{p}" note) and the Anti-Pattern "Fabricating a derivation relation"
  </read_first>
  <action>
    Add the Hansson base-contraction postulates as `@invariant`s on `_SpineMachine`, phrased against
    doxastica's append-only superseded-chain contraction (D-07) — NOT classical partial-meet remainder
    sets. Contraction Success: after `contract` on an asserted key, that `belief_id` is absent from
    `query_scope`. Inclusion: `keys(after) ⊆ keys(before)`. Relevance and Core-Retainment collapse to
    ONE structural claim under the engine-free model (D-07) but MUST exist as TWO distinct named
    test/assert methods for proof completeness (RESEARCH A2): the symmetric difference of the observed
    bases across a `contract(p)` is exactly `{p}`, and for every `belief_id ≠ p` the current tail is
    byte-identical before and after. Do NOT fabricate a value-semantic derivation relation the core
    lacks (Anti-Pattern). Uniformity: contracting the same `(scope,belief_id)` twice is idempotent at
    the base level (the second `contract` is a vacuous no-op per D-05) — implement per D-06a as a
    `@given` test or an `@invariant`, executor's discretion. To compare before/after bases across a
    contraction, capture the observed base in the `contract` rule body (or mirror via the oracle) —
    keep the expected side computed from the independent oracle, never a second SUT read. Then register
    the FORMAL-03 named structural set per D-08: `current_is_total_single_valued_and_chain_tail` (the
    CURRENT_STATE-uniqueness theorem — there is no pointer edge; it is the unique ordering-max under a
    unique `state_id` tiebreak), `chain_is_immutable`, and `world_contract_raises` are ALREADY present;
    NAME them into the conformance set (a clear docstring/comment registry block listing the FORMAL-03
    members) rather than re-implementing. Preserve the `_EVENT_POOL` draw and the dual-backend
    `.TestCase` idiom for BACK-05.
  </action>
  <verify>
    <automated>uv run pytest tests/test_invariants.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_invariants.py -q` passes (memory).
    - `uv run --extra ladybug pytest tests/test_invariants.py -q` passes (both backends).
    - Two distinct named methods exist for Relevance and Core-Retainment: `grep -nE 'relevance|core_retainment' tests/test_invariants.py` returns at least two distinct symbols.
    - A FORMAL-03 registry block names `current_is_total_single_valued_and_chain_tail`, `chain_is_immutable`, and `world_contract_raises`: `grep -nE 'FORMAL-03' tests/test_invariants.py` returns the registry comment.
    - No Hansson assertion fabricates a derivation relation — the Relevance/Core-Retainment bodies assert the symmetric-difference / per-belief-tail-unchanged claim (verified by reading the method bodies).
  </acceptance_criteria>
  <done>Contraction Success/Inclusion/Relevance/Core-Retainment/Uniformity ride the machine against the independent oracle, the FORMAL-03 structural set is named into the conformance registry, all green on both backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> MemoryCore -> BackendPort | The only boundary in scope; all inputs are test-generated (Hypothesis strategies over fixed pools), no external/untrusted input |
| (N/A) network / auth / session / multi-user | No network, no auth, no sessions — an embedded single-process library test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | The extracted `core.py` helper | mitigate | Helper composes ONLY `_order_key`/`Status` + stdlib; no `ladybug` import, no string-built query — `tests/test_import_purity.py` statically guards core driver-blindness |
| T-07-02 | Tampering | Per-example backend DBs | mitigate | Hypothesis builds throwaway `:memory:`/in-memory DBs per example; ladybug uses `max_db_size=2**30` cap + `teardown` close (existing pattern preserved); no shared-path lock or state bleed |
| T-07-IV | Information disclosure | Driver/dialect leak across the core seam | mitigate | The helper stays driver-blind (no Cypher, no `ladybug` symbol); existing import-purity guard enforces it |
| T-07-WEB | S/R/I/D/E (web app threats) | — | accept | N/A for an embedded, zero-network, zero-auth belief-revision library test suite — no realistic surface exists |
</threat_model>

<verification>
- `uv run pytest tests/test_invariants.py -q` green (memory).
- `uv run --extra ladybug pytest tests/test_invariants.py -q` green (both backends; ladybug NOT skipped when the extra is installed).
- `uv run pytest tests/test_query_scope.py tests/test_import_purity.py -q` green (helper extraction behaviour-preserving + driver-blind).
- `uv run basedpyright src tests` clean; `uv run ruff check .` clean.
</verification>

<success_criteria>
- AGM K*2/K*3/K*5 are `@invariant`s and K*4/K*6 are `@given` single-op tests, all against the independent oracle, green on both backends (FORMAL-01).
- Hansson Contraction Success/Inclusion/Relevance/Core-Retainment/Uniformity green on both backends, phrased as superseded-chain surgical contraction (FORMAL-02, D-07).
- The FORMAL-03 named structural set (uniqueness theorem, chain immutability, world-scope no-contraction) is registered into the conformance set (D-08, the `test_invariants.py` half).
- The `rows -> tails` helper is extracted, single-sourcing the `_order_key` group-by-max (D-01a), with `query_scope` behaviour preserved.
- BACK-05 satisfied transversally — every new `@invariant` runs on both backends with no per-backend assertion.
</success_criteria>

<output>
Create `.planning/phases/07-agm-hansson-conformance-suite/07-01-SUMMARY.md` when done. Record the
exact extracted-helper signature so 07-04 (irony join) consumes it.
</output>
