---
phase: 07-agm-hansson-conformance-suite
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/test_irony_join.py
autonomous: true
requirements: [FORMAL-05, BACK-05]
must_haves:
  truths:
    - "Two scopes diverging on `belief_id` are computed from ONE port round-trip (a single match_nodes('BeliefState', {}) scan), filtered to the two scopes in Python, inner-joined on belief_id, emitting rows where the decoded value differs"
    - "The expected divergent rows are computed by a plain-Python oracle in the test from the known synthetic writes (D-03) — not by re-running the join logic"
    - "The demonstration runs on both backends via the `backend` fixture, with identical core logic and identical results (cross-backend parity, D-03)"
    - "No narrative naming (irony/actor/world_truth/dramatic_irony) appears in any core code touched; the divergence-join helper lives in the test file (D-03a)"
  artifacts:
    - path: "tests/test_irony_join.py"
      provides: "The FORMAL-05 two-scope divergence demo: a test-level diverging_beliefs helper (one match_nodes scan) + a plain-Python expected oracle, over the backend fixture"
      contains: "match_nodes"
  key_links:
    - from: "tests/test_irony_join.py diverging_beliefs"
      to: "the 07-01 extracted rows->tails helper + MemoryCore._decode_value"
      via: "one match_nodes scan -> per-scope current tails -> inner-join on belief_id -> value differs"
      pattern: "match_nodes|_decode_value"
---

<objective>
Create `tests/test_irony_join.py` (NEW): the FORMAL-05 showcase — two scopes diverging on a
proposition (`belief_id`), computed as ONE port round-trip and compared against a plain-Python
expected oracle. Per D-01/D-01a/D-02/D-03: ONE `match_nodes("BeliefState", {})` scan -> derive the
active current tail per `(scope, belief)` reusing the 07-01 extracted `rows -> tails` helper -> keep
the two named scopes -> inner-join on `belief_id` -> emit rows where the decoded `value` differs.

Boundary (D-03a): the divergence-join helper is TEST-LEVEL (no narrative naming leaks into core); an
"actor" scope is just any non-world scope; `WORLD_SCOPE_ID` already exists.

Purpose: FORMAL-05 (the irony join on synthetic data); BACK-05 transversally via the `backend` fixture.
Output: `tests/test_irony_join.py` with the test-level `diverging_beliefs` helper + the plain-Python
expected oracle, green on both backends.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agm-hansson-conformance-suite/07-CONTEXT.md
@.planning/phases/07-agm-hansson-conformance-suite/07-RESEARCH.md
@.planning/phases/07-agm-hansson-conformance-suite/07-PATTERNS.md
@.planning/phases/07-agm-hansson-conformance-suite/07-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (exclude from drift checks):

- `tests/test_irony_join.py` — NEW file with:
  - a TEST-LEVEL `diverging_beliefs(core, scope_a, scope_b)` helper (one `match_nodes` scan; reuses
    the 07-01 `core.py` `rows -> tails` helper and `MemoryCore._decode_value`)
  - a plain-Python expected oracle computed from the synthetic writes (D-03)
  - `test_irony_join_two_scopes_diverge(backend)` over the `backend` fixture (both backends)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Two-scope divergence demo on synthetic data, one round-trip (FORMAL-05, D-01..D-03, BACK-05)</name>
  <files>tests/test_irony_join.py</files>
  <read_first>
    - .planning/phases/07-agm-hansson-conformance-suite/07-01-SUMMARY.md (the EXACT signature of the extracted `rows -> tails` helper this join reuses — REQUIRED before writing the helper)
    - src/doxastica/core.py (lines 86-112 `_order_key`/`_is_active_tail`; 208-229 `_current_tail`; 251-266 `_decode_value`; 602-610 the `query_scope` steps 3-4 the helper extracts; and the extracted helper added by 07-01)
    - src/doxastica/ports.py (the generic `BackendPort` surface — `match_nodes` signature; the grounding for Option A: no belief-revision-aware port method exists or should be added)
    - tests/test_scope_at.py (lines 58-69 the function-test `MemoryCore(backend)` injected-fixture-port precedent)
    - tests/conftest.py (the `backend` fixture)
    - 07-PATTERNS.md section "`tests/test_irony_join.py` — NEW (FORMAL-05)" (the one-round-trip + inner-join shape, the plain-Python expected oracle, the D-03a boundary)
    - 07-RESEARCH.md Pattern 7 + Open Question 3 (keep the divergence-join test-level; extract only the `rows->tails` helper into core) + the Anti-Pattern "two query_scope calls or a second match_nodes"
    - CLAUDE.md (the no-narrative-in-core boundary, D-03a)
  </read_first>
  <action>
    Create `tests/test_irony_join.py`. Define a TEST-LEVEL helper
    `diverging_beliefs(core, scope_a, scope_b)` (per D-03a / RESEARCH Open Q3 — keep the
    narrative-adjacent demo OUT of core; reuse only the 07-01 `core.py` `rows -> tails` helper).
    Shape (D-01/D-01a/D-02): perform exactly ONE port round-trip — `core._backend.match_nodes(
    "BeliefState", {})` (the full-label scan; "single query" = one round-trip — synthetic data is
    small so the scan is cheap and honest). Then derive the active current tail per `(scope, belief)`
    by reusing the 07-01 extracted helper (single-sourcing the `_order_key` group-by-max + retracted
    -tail collapse), filtering to `scope_a` and `scope_b` SEPARATELY in Python (NOT two `match_nodes`
    calls and NOT two `query_scope` calls — that is two round-trips, the Anti-Pattern; `match_nodes`
    AND-equality cannot express `scope_id IN {a,b}`). Inner-join on `belief_id` (D-02: `belief_id` IS
    the scope-independent proposition id — do NOT use the SUPERSEDED ARCHITECTURE.md
    `CURRENT_STATE`/`HOLDS`/`belief_id_logical` Cypher; none of those exist). Emit
    `(belief_id, value_a, value_b)` rows where the DECODED values differ (use
    `MemoryCore._decode_value` on the stored `value`). Write `test_irony_join_two_scopes_diverge(
    backend: BackendPort)` consuming the `backend` fixture: build `MemoryCore(backend)`, write known
    synthetic data into two non-world scopes (e.g. a world scope and an actor scope, or two actor
    scopes) including at least one shared `belief_id` with DIFFERING values, at least one shared
    `belief_id` with EQUAL values (must NOT appear), at least one `belief_id` present in only one
    scope (must NOT appear — inner join), and at least one RETRACTED tail that must be excluded.
    Compute the EXPECTED divergent rows directly in plain Python from the known writes (D-03) and
    assert equality (order-insensitively, e.g. compare sets/sorted lists). Because the `backend`
    fixture runs the test once per backend, comparison to the plain-Python expected already proves
    cross-backend parity. Use only generic public/`_decode_value` surface — NO `ladybug` import, NO
    constructed query string, NO narrative naming.
  </action>
  <verify>
    <automated>uv run pytest tests/test_irony_join.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_irony_join.py -q` exits 0 (memory backend).
    - `uv run --extra ladybug pytest tests/test_irony_join.py -q` exits 0 under BOTH `memory` and `ladybug` params; when the driver is absent the `ladybug` param SKIPS (not fails).
    - The join performs exactly ONE `match_nodes` call: `grep -c 'match_nodes' tests/test_irony_join.py` returns 1 (a single scan), and there is no second `match_nodes` / no `query_scope` used to build the join inputs.
    - No narrative naming leaks anywhere: `grep -niE 'irony|actor|dramatic|world_truth' tests/test_irony_join.py` may appear only in comments/docstrings/test-data scope names, NEVER as a core symbol; and `grep -c 'import ladybug' tests/test_irony_join.py` returns 0.
    - The expected divergent rows are computed by a plain-Python oracle (not by calling `diverging_beliefs` to derive the expectation): the test builds the expectation from the synthetic writes independently.
    - The test data includes an equal-value shared belief (excluded), a single-scope belief (excluded), and a retracted tail (excluded) — verified by reading the test body.
  </acceptance_criteria>
  <done>The two-scope divergence is computed from one round-trip, joined on `belief_id`, matches a plain-Python expected oracle, and is green on both backends with zero narrative naming in core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> MemoryCore -> BackendPort | Only boundary; synthetic test-authored writes + fixture-provided backends |
| (N/A) network / auth / session / multi-user | None — embedded single-process library test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | The divergence join | mitigate | Composes ONLY `match_nodes` (property-equality, no string-built/constructed query) + a Python join — honors BACK-01 narrow-port + DATA-02 closed-filter design; no Cypher passthrough, no `ladybug` import (kept test-level + driver-blind) |
| T-07-02 | Tampering | Per-test backend DBs | mitigate | Consumes the existing `backend` fixture (throwaway `:memory:`/in-memory per test, fixture-managed close); no shared state |
| T-07-IV | Information disclosure | Narrative/dialect leak into core | mitigate | D-03a: the divergence-join helper stays TEST-LEVEL with neutral naming; only the generic `rows->tails` helper (added in 07-01, guarded by `test_import_purity.py`) lives in core |
| T-07-WEB | S/T/R/I/D/E (web app threats) | — | accept | N/A — no network/auth/session/untrusted-input surface; in-process synthetic-data test, no new dependency |
</threat_model>

<verification>
- `uv run pytest tests/test_irony_join.py -q` exit 0 (memory).
- `uv run --extra ladybug pytest tests/test_irony_join.py -q` exit 0 (both backends).
- `grep -c 'match_nodes' tests/test_irony_join.py` == 1 (one round-trip).
- `uv run ruff check tests/test_irony_join.py` clean; `uv run basedpyright tests/test_irony_join.py` clean.
</verification>

<success_criteria>
- Two scopes diverging on `belief_id` computed from ONE port round-trip, joined on `belief_id`, value-differs filter (FORMAL-05, D-01/D-01a/D-02).
- Matched against a plain-Python expected oracle (D-03); green on both backends (BACK-05).
- Zero narrative naming in core; the divergence-join helper stays test-level (D-03a).
</success_criteria>

<output>
Create `.planning/phases/07-agm-hansson-conformance-suite/07-04-SUMMARY.md` when done.
</output>
