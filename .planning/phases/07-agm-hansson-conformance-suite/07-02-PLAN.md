---
phase: 07-agm-hansson-conformance-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scope_at.py
autonomous: true
requirements: [FORMAL-03, BACK-05]
must_haves:
  truths:
    - "get_scope_at ≡ replay (the operational-fold property) is named/registered into the FORMAL-03 conformance set on both backends"
    - "The replay oracle (`fold`) remains INDEPENDENT — it never calls get_scope_at or any production reconstruction helper"
    - "The property runs on both the memory and ladybug backends via the existing two-subclass .TestCase idiom"
  artifacts:
    - path: "tests/test_scope_at.py"
      provides: "The Phase-6 `scope_at_equals_fold_for_every_cut` property named into the FORMAL-03 conformance set (D-08) — registration, not re-implementation"
      contains: "scope_at_equals_fold_for_every_cut"
  key_links:
    - from: "tests/test_scope_at.py scope_at_equals_fold_for_every_cut"
      to: "the independent `fold` oracle"
      via: "expected computed by fold(), compared to get_scope_at()"
      pattern: "def fold|scope_at_equals_fold"
---

<objective>
Lift / register the Phase-6 operational-fold property `get_scope_at ≡ replay` into the FORMAL-03
conformance set (D-08). The property and its INDEPENDENT `fold` oracle already exist in
`tests/test_scope_at.py` (`_ScopeAtMachine.scope_at_equals_fold_for_every_cut`, already running on
both backends via `MemoryScopeAtFoldMachine` / `LadybugScopeAtFoldMachine`). D-08 requires only that
it be NAMED into the conformance set — do NOT build a new replay function (Anti-Pattern /
Don't-Hand-Roll).

Purpose: the `get_scope_at ≡ replay` member of FORMAL-03; BACK-05 transversally.
Output: a conformance-set registration of the existing fold property (a clear registry
docstring/comment naming it as a FORMAL-03 member, and confirmation it runs on both backends).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agm-hansson-conformance-suite/07-CONTEXT.md
@.planning/phases/07-agm-hansson-conformance-suite/07-RESEARCH.md
@.planning/phases/07-agm-hansson-conformance-suite/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan touches these symbols (exclude from drift checks):

- `tests/test_scope_at.py` — a FORMAL-03 conformance-set registry block (docstring/comment) naming
  `scope_at_equals_fold_for_every_cut` as the `get_scope_at ≡ replay` member of the named structural
  set (D-08). NO new replay function. If the executor chooses to ALSO re-express it as a registered
  `@invariant` (D-08 allows either), that method is new — but re-referencing the existing property is
  the cleaner, sufficient path.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register get_scope_at ≡ replay into the FORMAL-03 conformance set (D-08, FORMAL-03, BACK-05)</name>
  <files>tests/test_scope_at.py</files>
  <read_first>
    - tests/test_scope_at.py (lines 367-390 the independent `fold` oracle; 457-474 the registered `scope_at_equals_fold_for_every_cut` `@invariant`; 494-515 the `MemoryScopeAtFoldMachine` / `LadybugScopeAtFoldMachine` two-subclass `.TestCase` idiom)
    - 07-PATTERNS.md section "`tests/test_scope_at.py` — LIFT/register `_ScopeAtMachine.fold`" (the exact analog + the "register, do not re-implement" instruction)
    - 07-RESEARCH.md FORMAL-03 row of the Phase-Requirements -> Test-Map table and the "Don't Hand-Roll: get_scope_at ≡ replay oracle" row
    - 07-CONTEXT.md D-08 (the full named structural set) and the "Claude's Discretion" note that either re-expressing as `@invariant` or re-running the Phase-6 property satisfies FORMAL-03
  </read_first>
  <action>
    Confirm the existing `_ScopeAtMachine.scope_at_equals_fold_for_every_cut` property compares
    `get_scope_at` against the INDEPENDENT `fold` oracle (which has its own `(src, seq)` winner
    selection over `self.entries` with the same inclusive `<= as_of` cut and the retracted-as-of-absent
    rule) and runs on BOTH backends via the two `.TestCase` subclasses. Add a clear FORMAL-03
    conformance-set registry block (a module-level docstring or a comment marker, consistent with the
    registry block added in 07-01 to `test_invariants.py`) that NAMES
    `scope_at_equals_fold_for_every_cut` as the `get_scope_at ≡ replay` member of the FORMAL-03 named
    structural-invariant set per D-08. Do NOT build a new replay function and do NOT call any production
    reconstruction helper from the oracle (anti-tautology). If you choose to also re-express the
    property as a registered `@invariant` elsewhere, keep the `fold` oracle independent; but
    re-referencing the existing dual-backend property is sufficient and preferred. Leave the existing
    `fold`/cut/`_make_backend`/`teardown` logic unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scope_at.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scope_at.py -q` passes (memory).
    - `uv run --extra ladybug pytest tests/test_scope_at.py -q` passes (both backends; `LadybugScopeAtFoldMachine` runs, not skipped, when the extra is installed).
    - A FORMAL-03 registry marker naming the replay property exists: `grep -nE 'FORMAL-03' tests/test_scope_at.py` returns the registry block, and it references `scope_at_equals_fold_for_every_cut`.
    - The `fold` oracle still does not call `get_scope_at` / `_current` / `query_scope`: `grep -nE 'def fold' tests/test_scope_at.py` exists and its body computes the winner via `max(...)` over `self.entries`, not via a SUT call.
  </acceptance_criteria>
  <done>The Phase-6 `get_scope_at ≡ replay` property is named into the FORMAL-03 conformance set, still independent, still green on both backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> MemoryCore -> BackendPort | The only boundary; all inputs are Hypothesis-generated over fixed pools |
| (N/A) network / auth / session / multi-user | None — embedded single-process library test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | Per-example ladybug DBs in the fold machine | mitigate | Existing `max_db_size` cap + `teardown` close preserved unchanged; no new DB lifecycle introduced (registration-only change) |
| T-07-WEB | S/T/R/I/D/E (web app threats) | — | accept | N/A — no network/auth/session/untrusted-input surface in an embedded test suite; this plan adds NO new code path, only a registry marker |
</threat_model>

<verification>
- `uv run pytest tests/test_scope_at.py -q` green (memory).
- `uv run --extra ladybug pytest tests/test_scope_at.py -q` green (both backends).
- `uv run ruff check tests/test_scope_at.py` clean; `uv run basedpyright tests/test_scope_at.py` clean.
</verification>

<success_criteria>
- `get_scope_at ≡ replay` is registered as a named FORMAL-03 conformance member (D-08).
- The `fold` replay oracle remains independent of the SUT (no tautology).
- Runs on both backends transversally (BACK-05).
</success_criteria>

<output>
Create `.planning/phases/07-agm-hansson-conformance-suite/07-02-SUMMARY.md` when done.
</output>
