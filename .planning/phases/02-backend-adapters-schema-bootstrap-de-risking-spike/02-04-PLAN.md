---
phase: 02-backend-adapters-schema-bootstrap-de-risking-spike
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - pyproject.toml
  - uv.lock
  - .github/workflows/quality.yml
  - .github/workflows/pr.yml
  - CLAUDE.md
autonomous: true
requirements: [BACK-03, FORMAL-06]
must_haves:
  truths:
    - "pydantic is the only required runtime dep; ladybug is an opt-in extra (D-03, Option B)"
    - "`pip install doxastica` gives a working in-memory AGM core with zero ladybug install (D-03, D-05)"
    - "`pip install doxastica[ladybug]` adds the reference backend (D-03)"
    - "CI runs a base job WITHOUT ladybug (import-purity + in-memory subset) proving D-02 isolation (D-03)"
    - "CI runs a full job WITH [ladybug] over both backends (D-03, FORMAL-06)"
    - "CLAUDE.md's exactly-ladybug+pydantic and pinned-storage prose is reversed to pydantic-required/ladybug-extra (D-03 [BLOCKING])"
  artifacts:
    - path: "pyproject.toml"
      provides: "Option B deps: pydantic required, ladybug/all optional-dependencies"
      contains: "[project.optional-dependencies]"
    - path: "uv.lock"
      provides: "Re-resolved lock after ladybug demotion to an extra"
      contains: "ladybug"
    - path: ".github/workflows/quality.yml"
      provides: "Two-environment CI: base-isolation job + full [ladybug] job"
      contains: "ladybug"
    - path: "CLAUDE.md"
      provides: "Decision-grade reversal of the runtime-dep + pinned-storage constraints"
      contains: "reference-backend extra"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock re-resolves after the dependency reclassification"
      pattern: "ladybug"
    - from: ".github/workflows/quality.yml"
      to: "pyproject.toml extras"
      via: "base job omits the extra; full job uv sync --extra ladybug"
      pattern: "extra ladybug"
---

<objective>
Land Option B packaging (D-03): demote `ladybug` from a required runtime dependency to an
opt-in extra so `pydantic` is the sole required dep, re-resolve `uv.lock`, split CI into the
two-environment shape (a base-isolation job with ladybug ABSENT + a full both-backend job with
`[ladybug]`), and execute the [BLOCKING] decision-grade CLAUDE.md prose reversal. This is the
packaging payoff that gives the D-02 import isolation teeth: CI proves the in-memory spine
works with ladybug genuinely uninstalled.

Purpose: align the dependency table with the Phase 1 `BackendPort` seam (ladybug is the
*reference* backend, not *the* substrate), and record the constraint reversal explicitly rather
than as a silent edit (sibling to the Phase 1 §2 3.14-floor decision).

Output: restructured `pyproject.toml`, re-resolved `uv.lock`, two-env `quality.yml` +
`[ladybug]`-aware `pr.yml`, and the reversed CLAUDE.md prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-CONTEXT.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md

@pyproject.toml
@.github/workflows/quality.yml
@.github/workflows/pr.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Option B pyproject restructure + uv.lock re-resolve</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml:1-36 (current [project.dependencies] with ladybug on line 12; [dependency-groups])
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md (pyproject.toml MODIFY section — exact before/after)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-CONTEXT.md (D-03 install surfaces + extras shape)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md (Runtime State Inventory — uv.lock re-resolve is required; prek uv-lock hook enforces it)
  </read_first>
  <action>
    In `pyproject.toml`, change `[project.dependencies]` to `["pydantic>=2.11,<3"]` ONLY (remove
    the `ladybug>=0.17,<0.18` line). Add a new `[project.optional-dependencies]` table:
    `ladybug = ["ladybug>=0.17,<0.18"]`; `all = ["doxastica[ladybug]"]`; and a comment noting
    future `neo4j`/`surrealdb` extras slot in here (D-03). Leave `[dependency-groups]`,
    `[tool.pyright]`, `[tool.ruff]`, `[tool.pytest.ini_options]` unchanged.

    Re-resolve the lock: run `uv lock` (note user memory: `UV_NO_SYNC=1` may be needed; the
    sandbox-panic workaround is environment-level). ladybug should still appear in uv.lock (it is
    a transitive of the dev sync via the extra) but reclassified — the point is the lock is
    consistent with the new pyproject. Then verify both install surfaces resolve:
    `uv sync --no-default-groups` (base — ladybug should NOT be in the project deps) and
    `uv sync --dev --extra ladybug` (full). The prek `uv-lock` hook will reject a stale lock, so
    commit the updated uv.lock.
  </action>
  <verify>
    <automated>uv lock --check && uv sync --dev --extra ladybug && uv run pytest -q</automated>
  </verify>
  <done>
    pydantic is the only entry in [project.dependencies]; ladybug + all extras exist; uv.lock is
    consistent (`uv lock --check` passes); the full dev+extra sync installs and the suite is green.
  </done>
  <acceptance_criteria>
    - `grep -A2 '\[project.dependencies\]\|^dependencies' pyproject.toml` shows pydantic only (no ladybug in required deps).
    - `[project.optional-dependencies]` defines `ladybug` and `all`.
    - `uv lock --check` exits 0 (lock matches pyproject).
    - `uv sync --dev --extra ladybug && uv run pytest -q` is green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Two-environment CI + [BLOCKING] CLAUDE.md reversal</name>
  <files>.github/workflows/quality.yml, .github/workflows/pr.yml, CLAUDE.md</files>
  <read_first>
    - .github/workflows/quality.yml:9-44 (matrix + uv sync + pytest + cache-prune skeleton to reuse)
    - .github/workflows/pr.yml:14-44 (coverage job; needs --extra ladybug to exercise both backends)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md (quality.yml MODIFY — the two-job split; pr.yml note)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md (D-03 two-env CI; Job 1 isolation vs Job 2 full; State of the Art table for the exact prose lines to reverse)
    - CLAUDE.md (the "Runtime deps exactly ladybug + pydantic" line and the "Storage: pinned to LadybugDB / no storage abstraction" framing)
  </read_first>
  <action>
    In `.github/workflows/quality.yml`, split the single `quality` job into TWO jobs (or two
    matrix legs) reusing the existing step skeleton (checkout, setup-uv@v7 with enable-cache +
    cache-dependency-glob uv.lock, prek quality gates, cache-prune-on-always, ubuntu-latest,
    permissions contents:read, fail-fast:false, python 3.14):
    - **Job 1 (isolation)**: `uv sync --locked --no-default-groups --group dev` style WITHOUT the
      ladybug extra (ladybug ABSENT), then run ONLY the driver-free subset — at minimum
      `uv run pytest tests/test_import_purity.py tests/test_backend_memory.py` — proving D-02
      with ladybug truly gone. (If a dev-group dependency forces ladybug, install the dev tools
      but NOT the [ladybug] extra; the assertion is that the [ladybug] extra is not synced.)
    - **Job 2 (full)**: `uv sync --locked --dev --extra ladybug`, then prek + full
      `uv run pytest` over both backends.
    In `.github/workflows/pr.yml`, add `--extra ladybug` to the coverage `uv sync` so coverage
    exercises both backends.

    [BLOCKING] CLAUDE.md reversal (D-03 — a decision-grade edit, not a silent one). Update at
    minimum:
    - The runtime-dep line "Runtime deps **exactly `ladybug` + `pydantic`**" (and the matching
      Technology Stack / What NOT to Use / PKG framing) → "**pydantic** required; **ladybug** is
      the reference-backend extra (`doxastica[ladybug]`); future backends are extras."
    - The "**Storage: pinned to LadybugDB**" / "no storage abstraction" framing → note it is
      SUPERSEDED by the Phase 1 `BackendPort` decision; ladybug is the *reference* backend behind
      the port, not the only substrate. Keep the (still-true) ladybug API detail and the
      parameterized-Cypher discipline; only the dep-classification + pinned-storage framing
      reverses. Add a short dated note marking this as a Phase 2 D-03 decision-grade reversal
      (sibling to the 3.14-floor decision) so it reads as recorded, not silently changed.
  </action>
  <verify>
    <automated>uv run python -c "import yaml,sys; [yaml.safe_load(open(p)) for p in ('.github/workflows/quality.yml','.github/workflows/pr.yml')]; print('yaml-ok')" && grep -c "reference-backend extra" CLAUDE.md</automated>
  </verify>
  <done>
    quality.yml has a base-isolation job (ladybug absent) and a full [ladybug] job; pr.yml syncs
    the ladybug extra; CLAUDE.md's runtime-dep and pinned-storage prose is reversed to Option B
    and marked as a recorded decision.
  </done>
  <acceptance_criteria>
    - quality.yml defines two jobs/legs: one WITHOUT the ladybug extra running import-purity + in-memory, one WITH `--extra ladybug` running the full suite.
    - `grep -c "extra ladybug" .github/workflows/quality.yml` is >= 1 and the isolation job has no `--extra ladybug`.
    - `grep -c "extra ladybug" .github/workflows/pr.yml` >= 1.
    - Both workflow YAMLs parse (the yaml.safe_load check prints yaml-ok).
    - `grep -c "reference-backend extra" CLAUDE.md` >= 1 and the "exactly ladybug + pydantic" / "pinned to LadybugDB" framing no longer reads as an absolute current constraint.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → dependency install | CI installs declared deps/extras from the lock; the base job deliberately omits the ladybug extra to prove isolation. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PKG-01 | Tampering | stale uv.lock vs reclassified pyproject | mitigate | `uv lock --check` in CI/verify + the prek uv-lock hook reject a lock inconsistent with pyproject after the ladybug demotion. |
| T-02-PKG-02 | Spoofing | the ladybugdb slopsquat (wrong package name) | mitigate | The extra pins the verified `ladybug` (not `ladybugdb`, which 404s on PyPI — CLAUDE.md/What NOT to Use); the dep string and import name stay `ladybug`. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero NEW packages — `ladybug` is only RECLASSIFIED from a required dep to an extra; already installed and audited OK (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv lock --check` — lock matches the Option B pyproject.
- `uv sync --no-default-groups && uv run pytest tests/test_import_purity.py tests/test_backend_memory.py -q` — base-isolation subset green with ladybug absent from project deps.
- `uv sync --dev --extra ladybug && uv run pytest -q` — full both-backend suite green.
- YAML lint of both workflows parses; CLAUDE.md reversal prose present.
- `uv run ruff check` / `prek run --all-files` — clean (uv-lock hook passes).
</verification>

<success_criteria>
- pydantic is the sole required runtime dep; ladybug + all are extras (D-03, Option B).
- `pip install doxastica` yields a working in-memory AGM core without ladybug; `[ladybug]` adds the reference backend (D-03, D-05).
- uv.lock re-resolved and consistent (D-03).
- CI runs a base-isolation job (ladybug absent → import-purity + in-memory) and a full [ladybug] both-backend job (D-03, FORMAL-06).
- The [BLOCKING] CLAUDE.md runtime-dep + pinned-storage reversal is recorded as a decision-grade edit (D-03).
</success_criteria>

<output>
Create `.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-04-SUMMARY.md` when done
</output>
