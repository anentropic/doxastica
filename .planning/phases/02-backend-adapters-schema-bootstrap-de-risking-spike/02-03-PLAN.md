---
phase: 02-backend-adapters-schema-bootstrap-de-risking-spike
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/conftest.py
  - tests/test_backend_parity.py
  - tests/test_import_purity.py
autonomous: true
requirements: [BACK-03, FORMAL-06]
must_haves:
  truths:
    - "A parametrized `backend` fixture yields a fresh throwaway backend per example over [memory, ladybug] (FORMAL-06)"
    - "The ladybug fixture param is skipped (not errored) when the driver is absent (D-03 base CI)"
    - "Each example gets a fresh in-memory lb.Database — no state bleed or lock errors across tests (FORMAL-06)"
    - "Both backends return identical (reached, frontier) on diamond/cycle/over-bound graphs (D-05, BACK-03 oracle parity)"
    - "Both backends return identical match_nodes results on the same inserts (D-05)"
    - "doxastica, doxastica.core, doxastica.backends.memory import with ladybug genuinely absent, and MemoryCore.in_memory() works driver-free (D-02)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Parametrized backend fixture over both adapters; throwaway :memory: per example"
      contains: "params="
    - path: "tests/test_backend_parity.py"
      provides: "The oracle-parity suite — identical (reached, frontier) + match_nodes across backends"
      contains: "backend"
    - path: "tests/test_import_purity.py"
      provides: "Extended AST scan + runtime-absence assertion proving driver-blind core/memory"
      contains: "backends/memory"
  key_links:
    - from: "tests/conftest.py"
      to: "src/doxastica/backends/{memory,ladybug}.py"
      via: "constructs each backend; importorskip skips ladybug when absent"
      pattern: "importorskip"
    - from: "tests/test_backend_parity.py"
      to: "tests/conftest.py"
      via: "consumes the parametrized backend fixture"
      pattern: "def test_.*\\(backend"
    - from: "tests/test_import_purity.py"
      to: "src/doxastica/core.py"
      via: "AST scan asserts no ladybug import in core + backends.memory"
      pattern: "core"
---

<objective>
Lock the oracle-parity guarantee (D-05/BACK-03) from day one and extend the import-isolation
discipline (D-02) now that both backends exist. Build the parametrized `backend` fixture
(throwaway `:memory:` per example, ladybug param skipped when absent — FORMAL-06), the
parity suite asserting both backends return identical `(reached, frontier)` and `match_nodes`
on diamond/cycle/over-bound graphs (RESEARCH Open Question 1), and the extended
`test_import_purity.py` that proves `core` + `backends.memory` import driver-free.

Purpose: the parametrized conftest is the mechanism that keeps the ladybug adapter and the
in-memory oracle honest — without it, Phase 7's shadow-model comparison is built on sand. The
diamond-graph frontier edge case (which depth "wins") is asserted identical here, not deferred.

Output: `tests/conftest.py`, `tests/test_backend_parity.py`, and the extended
`tests/test_import_purity.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-CONTEXT.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md
@.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md

@src/doxastica/ports.py
@src/doxastica/backends/memory.py
@src/doxastica/backends/ladybug.py
@tests/test_import_purity.py
@tests/test_port_distinct.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parametrized throwaway-backend fixture + oracle-parity suite</name>
  <files>tests/conftest.py, tests/test_backend_parity.py</files>
  <read_first>
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md (conftest fixture template; the D-01a plain-list constraint; FORMAL-06 fresh-DB-per-call)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md (Validation Architecture conftest design; Open Question 1: diamond/cycle/over-bound parity; Pitfall 5: state bleed/locks)
    - src/doxastica/backends/memory.py (the oracle traverse/frontier semantics)
    - src/doxastica/backends/ladybug.py (the adapter to assert identical against)
    - tests/test_port_distinct.py:19-37 (assertion + docstring style)
  </read_first>
  <action>
    Fill `tests/conftest.py` with a `@pytest.fixture(params=["memory", "ladybug"])` named
    `backend` (a PLAIN params list, NOT a registry lookup — D-01a). For the "ladybug" param:
    `lb = pytest.importorskip("ladybug")` (skips, never errors, when the driver is absent — the
    D-03 base-CI job), construct `lb.Connection(lb.Database())` (None/`:memory:` path = a FRESH
    in-memory DB per fixture call — FORMAL-06, Pitfall 5), build
    `LadybugBackend(conn, namespace="dx", owns_conn=True)`, `yield`, then `close()`. For the
    "memory" param: yield a fresh `InMemoryBackend()`. Both via function-local imports so the
    fixture module loads even when ladybug is absent.

    Create `tests/test_backend_parity.py` consuming the `backend` fixture. Each test populates
    the SAME graph through the port primitives, then asserts results are consistent across the
    parametrized backends. Because the fixture is parametrized, comparing per-backend outputs to
    an expected literal already proves parity; ALSO add at least one test that builds BOTH
    backends in-process and asserts byte-identical sorted `(reached_ids, frontier)` and
    `match_nodes` from each. Graphs (RESEARCH Open Question 1):
    - **diamond**: A→B, A→C, B→D, C→D — assert reached and the depth at which D is found behave
      identically (the "which depth wins for the frontier test" edge case).
    - **cycle**: A→B→C→A — traverse terminates; reached is the de-duplicated set; identical both.
    - **over-bound chain**: a chain longer than a finite max_depth — assert identical boundary
      frontier; and max_depth=None → identical full closure with EMPTY frontier on both.
    - **match_nodes**: insert several BeliefState nodes with differing status; assert identical
      AND-match results (sorted by state_id, since the port leaves ordering to the core, BACK-04 §5).
    Normalize ids to `str` and sort before comparing (order is not guaranteed by the port).
  </action>
  <verify>
    <automated>uv run pytest -p no:randomly tests/test_backend_parity.py -q</automated>
  </verify>
  <done>
    The `backend` fixture yields a fresh throwaway backend per example for both params; ladybug
    is skipped when absent; the parity suite passes identically for memory and ladybug on
    diamond/cycle/over-bound/match_nodes with no state bleed.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_backend_parity.py --collect-only` shows each parity test parametrized over [memory, ladybug].
    - With ladybug installed, both params pass; the diamond/cycle/over-bound + match_nodes tests assert identical (reached, frontier)/results.
    - Running the parity file twice in one session shows no cross-test state bleed (fresh DB per example).
    - `grep -c "registry\|REGISTRY" tests/conftest.py` returns 0 (plain params list — D-01a).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend import-purity for driver-blind core + memory (D-02)</name>
  <files>tests/test_import_purity.py</files>
  <read_first>
    - tests/test_import_purity.py:1-35 (the existing parametrized AST scan to extend)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-PATTERNS.md (test_import_purity EXTEND section — both the static AST scan and the runtime-absence approach)
    - .planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-RESEARCH.md (§"simulating absence": ladybug IS installed in dev, so use a subprocess / sys.meta_path finder that blocks ladybug; AND rely on CI base-install Job 1 — recommend BOTH)
    - src/doxastica/core.py (must contain no top-level or TYPE_CHECKING ladybug import)
  </read_first>
  <action>
    Extend `tests/test_import_purity.py` two ways (D-02):
    1. STATIC AST scan — add `"core"` and `"backends/memory"` to the existing
       `@pytest.mark.parametrize("module", [...])` list (joining "protocol", "ports"). Adjust the
       path construction so a slashed module like `backends/memory` resolves to
       `src/doxastica/backends/memory.py`. The existing `ast.walk` offender check then catches a
       top-level OR TYPE_CHECKING-block ladybug import in either module. Update the module
       docstring to note core.py and backends/memory.py join the backend-blind contract (D-02),
       and that core.py's factories use function-local ladybug imports which a top-level scan must
       NOT see.
    2. RUNTIME ABSENCE — add a test that runs a subprocess (`sys.executable -c "..."`) whose code
       installs a `sys.meta_path` finder raising `ModuleNotFoundError` for any `ladybug` import,
       THEN does `import doxastica; import doxastica.core; import doxastica.backends.memory; from
       doxastica import MemoryCore; MemoryCore.in_memory()` and exits 0. Assert the subprocess
       returns 0 (ladybug genuinely blocked, the driver-free spine still imports and runs). This
       complements the CI base-install Job 1 (Plan 04) where ladybug is truly uninstalled.
    Keep the existing protocol/ports test green (no regression).
  </action>
  <verify>
    <automated>uv run pytest tests/test_import_purity.py -x -q</automated>
  </verify>
  <done>
    The AST scan covers protocol, ports, core, backends/memory with no ladybug offender; the
    subprocess test proves the driver-free spine imports and MemoryCore.in_memory() runs with
    ladybug blocked from the import system.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_import_purity.py --collect-only` lists core and backends/memory among the parametrized modules.
    - The subprocess absence test exits 0 (driver-free import + MemoryCore.in_memory() with ladybug blocked).
    - The original protocol/ports parametrizations still pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → both backends | Tests construct backends with fixed safe namespaces and synthetic graphs; no external input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PAR-01 | Tampering | oracle parity drift between backends | mitigate | The parametrized parity suite asserts identical (reached, frontier)/match_nodes across both backends from day one (D-05) — drift is caught here, not in Phase 7. |
| T-02-PAR-02 | Information Disclosure (test integrity) | hidden ladybug coupling in the "driver-free" spine | mitigate | The extended AST scan + subprocess-absence test prove core/backends.memory import with ladybug blocked (D-02). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; tests only. RESEARCH audit records zero new packages this phase. |
</threat_model>

<verification>
- `uv run pytest -p no:randomly tests/test_backend_parity.py -q` — parity green on both backends.
- `uv run pytest tests/test_import_purity.py -q` — extended scan + absence subprocess green.
- `uv run pytest -q` — full suite green (both backends present in dev env).
- `uv run ruff check tests/conftest.py tests/test_backend_parity.py tests/test_import_purity.py` — clean.
</verification>

<success_criteria>
- A parametrized `backend` fixture yields a fresh throwaway backend per example over both adapters; ladybug skipped when absent (FORMAL-06).
- Both backends return identical (reached, frontier) and match_nodes on diamond/cycle/over-bound graphs (D-05, BACK-03 oracle parity).
- No state bleed or lock errors across examples (FORMAL-06, Pitfall 5).
- core and backends.memory provably import with ladybug genuinely absent; MemoryCore.in_memory() works driver-free (D-02).
</success_criteria>

<output>
Create `.planning/phases/02-backend-adapters-schema-bootstrap-de-risking-spike/02-03-SUMMARY.md` when done
</output>
