---
phase: 06-structural-time-travel
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/test_scope_at.py
autonomous: true
requirements: [HIST-03]

must_haves:
  truths:
    - "A pure-Python operational-fold oracle replays the recorded revise/expand/contract op sequence and folds it to the active base as of any cut"
    - "get_scope_at(scope, cut) == fold(ops, cut) holds for every cut in the event pool, under Hypothesis, on BOTH backends (the D-07 SPEC)"
    - "The oracle is INDEPENDENT — it folds OPERATIONS, never calling get_scope_at/_current_tail (anti-tautology)"
    - "Same-millisecond and out-of-order UUID7 source_event_ids resolve correctly under the ordering contract (SC3)"
    - "Stepping as_of across the event ids reconstructs the same result as the fold replay (SC2); SC1 falls out at the maximal cut"
  artifacts:
    - path: "tests/test_scope_at.py"
      provides: "The operational-fold RuleBasedStateMachine + fold(ops, as_of) oracle + the get_scope_at==fold invariant + two .TestCase subclasses (memory + ladybug)"
      contains: "class .*ScopeAtMachine"
  key_links:
    - from: "tests/test_scope_at.py (fold oracle)"
      to: "recorded op sequence (own (source_event_id, append_seq) winner selection)"
      via: "independent operational fold — must NOT call get_scope_at/_current_tail"
      pattern: "def fold"
    - from: "tests/test_scope_at.py (@invariant)"
      to: "MemoryCore.get_scope_at"
      via: "assert {s.belief_id: s.value for s in get_scope_at(scope, cut)} == fold(scope, cut) for every cut"
      pattern: "get_scope_at"
---

<objective>
Land the operational-fold replay-equivalence property (D-07, LOCKED) — the correctness SPEC where the roadmap's "most complex single query" billing is actually cashed. Build a pure-Python operational-fold oracle that replays the recorded `revise`/`expand`/`contract` op sequence and folds it to the active base as of a cut, then assert `get_scope_at(scope, cut) == fold(ops, cut)` under a Hypothesis `RuleBasedStateMachine` on BOTH backends — with intra-millisecond-colliding and out-of-order `source_event_id`s (SC3) and `as_of` stepped across the event ids (SC2). SC1 (`get_scope_at(latest) == query_scope(current)`) falls out at the maximal cut.

Purpose: NVM *defines* `get_scope_at` as the fold-over-the-event-log; its cache-watermark reconstruction and replay-debugging rest on this equivalence. SC1/SC2/SC3 all collapse into this one property. The oracle is the spec; only an INDEPENDENT fold (over operations, not materialized states) makes the equivalence a real proof rather than a tautology.

Output: the operational-fold half of `tests/test_scope_at.py` — the `RuleBasedStateMachine`, the `fold(ops, as_of)` oracle, the `get_scope_at == fold` invariant, and the two `.TestCase` subclasses (memory + ladybug).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-structural-time-travel/06-CONTEXT.md
@.planning/phases/06-structural-time-travel/06-RESEARCH.md
@.planning/phases/06-structural-time-travel/06-PATTERNS.md
@.planning/phases/06-structural-time-travel/06-VALIDATION.md

# The direct stateful-machine + shadow-oracle template (the idiom to mirror)
@tests/test_invariants.py
@tests/conftest.py

# The example-test half this plan extends (created in 06-01)
@tests/test_scope_at.py
@src/doxastica/core.py
</context>

<artifacts_produced>
This plan adds to the existing `tests/test_scope_at.py` (created in 06-01):
- A fixed `_EVENT_POOL` (e.g. `tuple(uuid.uuid7() for _ in range(3))`) forcing intra-ms collisions, plus `_SCOPE_POOL` / `_BELIEF_POOL` / `_VALUE` sampled strategies (SC3 generator).
- An `Op`/`Entry` record type carrying `(op_kind, scope_id, belief_id, value, source_event_id_str, append_seq)`.
- A `_ScopeAtMachine(RuleBasedStateMachine)` with: `_make_backend` (bounded `Database(max_db_size=2**30)` for ladybug, `InMemoryBackend` for memory — copied from `test_invariants.py:132-148`); `@initialize` building `MemoryCore` + the shadow op-log; `revise`/`expand`/`contract` `@rule`s (contract `@precondition`-gated) that mirror each op into the op-log via `_record`; the `fold(scope_id, as_of) -> dict[belief_id, value]` oracle; the `scope_at_equals_fold_for_every_cut` `@invariant`; and `teardown` releasing the backend.
- `MemoryScopeAtMachine` / `LadybugScopeAtMachine` subclasses + their `.TestCase` entry points with `settings(max_examples=50, stateful_step_count=20, deadline=None)`.
- The `fold` oracle is a NEW test-only helper (pure Python over the op sequence), NOT production code.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Operational-fold oracle + Hypothesis stateful property (get_scope_at == fold, both backends)</name>
  <files>tests/test_scope_at.py</files>
  <read_first>
    - tests/test_invariants.py (the ENTIRE `_SpineMachine` idiom — the direct template): `_EVENT_POOL` collision pool (line 66) + sampled strategies (82-84); `_chain_tail` independent-recompute lesson (87-104 — the anti-tautology pattern: recompute via a DIFFERENT surface); `_make_backend` bounded-ladybug builder (132-148); `@initialize` `_setup` (150-158); `_record` op-log append + monotonic `_seq` (160-168); `_shadow_current` winner-by-`(source_event_id, seq)` + retracted→absent (170-183); the `revise`/`expand`/`contract` `@rule`s with `@precondition` (204-244); `teardown` (317-328); the two `.TestCase` subclasses + `_SETTINGS` (332-355); the Hypothesis imports (46-55)
    - tests/test_scope_at.py (the example-test half from 06-01 — reuse its preamble; do NOT duplicate `_event_id`/`_core`/`_ids`)
    - src/doxastica/core.py:86-96 (`_order_key` — the oracle's winner selection MUST mirror it as `(source_event_id_str, append_seq)`, where `append_seq` stands in for the monotonic `state_id` tiebreak)
    - .planning/phases/06-structural-time-travel/06-RESEARCH.md (Pattern 2 + Pitfall 6 — the independent-fold requirement; the fold/cut skeleton lines 205-227)
    - .planning/phases/06-structural-time-travel/06-PATTERNS.md (Half B, lines 152-241 — the verbatim-copy idioms with line citations)
  </read_first>
  <behavior>
    - fold(scope_id, as_of) is a PURE-PYTHON operational fold: keep recorded ops with `source_event_id_str <= as_of` (the SAME inclusive cut, str-vs-str), per (scope, belief) take the winner by `(source_event_id_str, append_seq)` (mirrors `_order_key`), and DROP a belief whose winning op is a contract (retracted-as-of, D-06). Returns `{belief_id: value}` for the scope. It MUST fold OPERATIONS — never call `get_scope_at`/`_current_tail`/`_current` (anti-tautology, Pitfall 6).
    - The `revise`/`expand` rules call the real `core.revise`/`core.expand` and record op_kind="revise" (active); `contract` (precondition-gated on a currently-active key) calls `core.contract` and records op_kind="contract". Each op draws its `source_event_id` from the fixed `_EVENT_POOL` (collisions + out-of-order guaranteed, SC3).
    - The `@invariant` scope_at_equals_fold_for_every_cut: for each scope in the pool, for each cut in (every pooled id + a maximal sentinel >= all written ids), assert `{s.belief_id: s.value for s in core.get_scope_at(scope, cut_uuid)} == fold(scope, str(cut))`. SC2 = stepping the cut; SC1 = the maximal-cut case agreeing with query_scope's "now".
    - The property runs as two auto-collected `.TestCase`s (memory + ladybug), bounded `max_examples=50, stateful_step_count=20`.
  </behavior>
  <action>
    Extend `tests/test_scope_at.py` with the operational-fold half, mirroring `tests/test_invariants.py`'s `_SpineMachine` idiom (copy the load-bearing scaffolding verbatim and rename for this phase). Add a fixed `_EVENT_POOL = tuple(uuid.uuid7() for _ in range(3))` plus `_SCOPE_POOL`/`_BELIEF_POOL`/value strategies and an `Op`/`Entry` record (`op_kind`, `scope_id`, `belief_id`, `value`, `source_event_id` str, `append_seq`). Build `_ScopeAtMachine(RuleBasedStateMachine)`: copy `_make_backend` verbatim (bounded `lb.Database(max_db_size=2**30)` + `importorskip` for ladybug; `InMemoryBackend` for memory — the cap is load-bearing because Hypothesis makes one DB per example); `@initialize` builds `MemoryCore` + the op-log + `_seq = 0`; a `_record` that bumps `_seq` and appends the op (op_kind carried so `fold` can drop a contract winner). Write `revise`/`expand` `@rule`s that call the real core op and record op_kind="revise"; a `contract` `@rule` `@precondition`-gated on a currently-active (scope, belief) that records op_kind="contract". Each rule samples `source_event_id` from `_EVENT_POOL`. Write the `fold(self, scope_id, as_of) -> dict[str, Any]` oracle EXACTLY per the behavior block — independent of the core (its own `(source_event_id_str, append_seq)` winner; never call `get_scope_at`/`_current_tail`, per the `_chain_tail` lesson). Add the `scope_at_equals_fold_for_every_cut` `@invariant`. Copy `teardown` verbatim. Define `MemoryScopeAtMachine`/`LadybugScopeAtMachine` subclasses (`backend_kind`) and their `.TestCase` entry points with `_SETTINGS = settings(max_examples=50, stateful_step_count=20, deadline=None)`, carrying the same `# pyright: ignore[...]` comments as `test_invariants.py:351-355`. Use a `_k`-named filter (e.g. `Fold`) on the class/test names so `-k Fold` selects them (per VALIDATION.md). Keep ruff/basedpyright-strict clean.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scope_at.py -k Fold -q</automated>
    <automated>uv run pytest tests/test_scope_at.py -q  # full file: example tests (06-01) + the fold property both green on both backends</automated>
    <automated>uv run pytest -q  # full suite incl. test_invariants/test_query_scope/test_import_purity — keystone + purity still green</automated>
  </verify>
  <done>The operational-fold `RuleBasedStateMachine` exists with the independent `fold` oracle and the `get_scope_at == fold` invariant; both `.TestCase` subclasses pass under Hypothesis (`-k Fold`) on memory and ladybug with colliding/out-of-order ids and cuts stepped across the pool. The full suite is green (keystone + purity unchanged). The oracle calls no `get_scope_at`/`_current_tail` (anti-tautology verified by inspection). basedpyright strict clean, ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → `MemoryCore.get_scope_at` | Test-only; the property drives the already-shipped read with typed `UUID` cuts drawn from a fixed pool. No new production surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering / Info-disclosure | test-only fold oracle + property | accept | This plan adds NO production code — only a test-only pure-Python oracle and a Hypothesis machine over the already-injection-proofed `get_scope_at`/`match_nodes` path. No new network/auth/input surface. V5 input-validation-by-construction continues to hold (the cut is a typed `UUID`). |
| T-06-02-SC | Tampering | package installs | accept | No package install in this phase (RESEARCH §Package Legitimacy Audit: not applicable — zero new deps; `pytest`/`hypothesis` already in the dev group). Phase-2 audit stands. |
</threat_model>

<verification>
- `uv run pytest tests/test_scope_at.py -k Fold -q` — the operational-fold property green on both backends.
- `uv run pytest tests/test_scope_at.py -q` — example tests + fold property together green.
- `uv run pytest -q` — full suite (keystone `test_invariants`, `test_query_scope`, `test_import_purity`) green.
- `uv run basedpyright` strict clean; `uv run ruff check` + `uv run ruff format --check` clean.
</verification>

<success_criteria>
- The D-07 SPEC holds: `get_scope_at(scope, cut) == fold(ops, cut)` for every cut, under Hypothesis, on BOTH backends — with intra-ms-colliding and out-of-order `source_event_id`s (SC3) and `as_of` stepped across the event ids (SC2). SC1 falls out at the maximal cut.
- The oracle is independent (folds operations, not materialized states; never calls `get_scope_at`/`_current_tail`) — the equivalence is a real cross-check, not a tautology.
- The full suite stays green; no production code is added in this plan.
</success_criteria>

<output>
Create `.planning/phases/06-structural-time-travel/06-02-SUMMARY.md` when done.
</output>
