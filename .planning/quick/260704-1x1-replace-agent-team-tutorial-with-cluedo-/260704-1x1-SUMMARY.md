---
phase: quick
plan: 260704-1x1
subsystem: docs
tags: [tutorial, docs, belief-revision, diataxis]
requires: []
provides: [DOC-CLUEDO-TUTORIAL]
affects:
  - docs/src/tutorials/cluedo-detective.md
  - docs/src/index.md
  - docs/src/tutorials/index.md
  - docs/src/explanation/scopes-and-world-scope.md
  - docs/src/how-to/trace-dependency-cascade.md
  - mkdocs.yml
tech-stack:
  added: []
  patterns: [diataxis-tutorial, runnable-verified-code-block]
key-files:
  created:
    - docs/src/tutorials/cluedo-detective.md
  modified:
    - mkdocs.yml
    - docs/src/index.md
    - docs/src/tutorials/index.md
    - docs/src/explanation/scopes-and-world-scope.md
    - docs/src/how-to/trace-dependency-cascade.md
  removed:
    - docs/src/tutorials/agent-team-memory.md
decisions: []
metrics:
  duration: 12min
  completed: 2026-07-04
---

# Phase quick Plan 260704-1x1: Cluedo Detective Tutorial Summary

Replaced the agent-team intermediate tutorial with a Cluedo-detective tutorial that centers
Story B (a held belief contradicted, superseded, and its dependents made stale), and repointed
every inbound reference; docs build clean under `--strict` and the closing script runs verified.

## What was done

- **Task 1** — Authored `docs/src/tutorials/cluedo-detective.md` (376 lines): a 12-step
  intermediate Diataxis tutorial. It frames the certain-vs-provisional split up front (certain
  facts in the world scope, a provisional theory of the crime in a `"theory"` belief scope),
  carries the Cluedo→primitive mapping table, one mermaid flowchart, `!!! info`/`!!! tip`
  admonitions, the honest "What doxastica does not do" section, the world-scope contraction
  guard demo, and a single self-contained runnable closing script with assertions.
- **Task 2** — Removed `docs/src/tutorials/agent-team-memory.md` with `git rm` and repointed
  all five inbound references (mkdocs.yml nav, index.md landing pitch, tutorials/index.md
  listing, scopes-and-world-scope.md two cross-refs, trace-dependency-cascade.md related-guides)
  to the new tutorial, re-pitched around the belief-revision story rather than agent teams.
- **Task 3** — End-to-end verification: extracted the largest python block and ran it under
  `uv run python` (exit 0, prints "All checks passed", only the deliberately-caught
  `WorldScopeContractionError` is raised); `uv run mkdocs build --strict` exits 0 with no
  broken-link warnings; the reference sweep is empty.

## API verification against source

Before writing code I verified the public surface against `src/doxastica/core.py`,
`models.py`, and `__init__.py`. Reality matched the plan's API notes exactly:

- Import surface `BeliefFilter, EdgeType, InMemoryBackend, MemoryCore, WORLD_SCOPE_ID, WorldScopeContractionError` all present in `__init__.__all__`.
- `revise/expand` return a `BeliefState`; `contract` returns `None`, is a vacuous no-op on an absent belief, and raises `WorldScopeContractionError` on `WORLD_SCOPE_ID` before any write.
- `add_edge(from_state_id, to_state_id, edge_type)` — dependent→basis convention confirmed.
- `get_impact(belief_state_id, depth=None)` returns `ImpactResult(reached, frontier, truncated)`, `reached` excludes the start node, and it must be called on the *basis* state to find dependents (walks `DERIVED_FROM` inbound).
- `get_scope_at(scope_id, as_of_event_id)` is an inclusive rewinding cut; `get_revision_chain(belief_id)` is cross-scope, oldest-first.
- `BeliefFilter(belief_ids={...})` accepts a set (pydantic coerces to `frozenset`); `include_retracted=True` surfaces the retracted tail.

No deviations from the plan's API assumptions were needed.

## Deviations from Plan

None affecting behavior. Two presentational choices worth noting:

- **Tutorial title.** The plan did not fix a published title; I used "The Detective's Notebook:
  Revising a Theory Under Contradiction" (the mkdocs nav entry is quoted because the title
  contains a colon). Link text across all inbound references was updated to match.
- **Step-3 initial weapon guess.** Step 3 seeds `weapon="Candlestick"` (initial theory) so that
  Step 4's `weapon="Wrench"` (derived from the assumption) is a genuine supersession, and the
  three first-theory writes share the captured `accusation_event` so `get_scope_at` reconstructs
  the full theory at that cut. This is within the plan's step description (revise culprit/weapon/room
  in step 3, re-derive weapon with provenance in step 4).

Task 3 required no code fixes — the closing script ran clean on the first attempt — so it
produced no separate commit.

## Verification results

- Closing script: `uv run python <tmp>` → exit 0, stdout "All checks passed.", empty stderr.
- `DISABLE_MKDOCS_2_WARNING=true uv run mkdocs build --strict` → exit 0; the only "warning"
  string in the log is the third-party Material nag banner, not a mkdocs broken-link warning
  (`--strict` would exit non-zero on a real one).
- `grep -rniE "agent-team-memory|agent-team|agent team|shared-world" docs/ mkdocs.yml` → empty.
- No GSD/process references, decision IDs, or phase numbers in the published tutorial.

## Commits

- `afa1836` docs(quick-260704-1x1): add Cluedo detective belief-revision tutorial
- `c22f7f7` docs(quick-260704-1x1): remove agent-team tutorial, repoint refs to Cluedo

## Known Stubs

None. Every code block in the published tutorial uses the real API; the consolidated closing
script is runnable and asserted.

## Self-Check: PASSED

- FOUND: docs/src/tutorials/cluedo-detective.md
- FOUND: commit afa1836
- FOUND: commit c22f7f7
- CONFIRMED GONE: docs/src/tutorials/agent-team-memory.md
</content>
