---
milestone: v0.2.0
audited: 2026-07-05T11:04:19Z
status: passed
scores:
  requirements: 8/8
  phases: 2/2
  integration: 8/8
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 10-stance-formal-proof-docs
    items:
      - "WR-01: `_FORMAL_03_CONFORMANCE_SET` unchecked string registry in test_invariants.py / test_scope_at.py — pre-existing drift risk, all four names currently resolve, not introduced by this milestone (advisory)."
      - "WR-02: imprecise `WORLD_SCOPE_ID` doc anchor in tutorial — link resolves, mkdocs --strict green, cosmetic (advisory)."
nyquist:
  compliant_phases: [10]
  partial_phases: [9]
  missing_phases: []
  overall: partial
---

# Milestone v0.2.0 — Stance (R21) — Audit Report

**Audited:** 2026-07-05
**Status:** ✅ **PASSED**
**Definition of done:** a green dual-backend conformance/property suite that carries
`stance` non-vacuously, plus the ordinal `Stance` field/order/comparison landed on the
frozen value layer and persisted byte-stable on both backends — closing the ratified
NVM R21 gap. All met.

## Scope

| Phase | Name | Plans | VERIFICATION | Score |
|-------|------|-------|--------------|-------|
| 9 | Stance Value Layer, Write & Persistence | 2/2 | passed | 5/5 |
| 10 | Stance Formal Proof & Docs | 4/4 | passed | 5/5 |

## Requirements Coverage (3-source cross-reference)

Every REQ-ID cross-checked against phase VERIFICATION.md status, SUMMARY.md
`requirements-completed` frontmatter, and the REQUIREMENTS.md traceability table.

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|--------|-------|--------------|---------------------|--------------|-------|
| STANCE-01 | 9 | SATISFIED | 09-01 | `[x]` | **satisfied** |
| STANCE-02 | 9 | SATISFIED | 09-01 | `[x]` | **satisfied** |
| STANCE-03 | 9 | SATISFIED | 09-01, 09-02 | `[x]` | **satisfied** |
| STANCE-04 | 9 | SATISFIED | 09-01, 09-02 | `[x]` | **satisfied** |
| STANCE-05 | 9 | SATISFIED | 09-02 | `[x]` | **satisfied** |
| STANCE-06 | 9 | SATISFIED | 09-01 | `[x]` | **satisfied** |
| STANCE-07 | 10 | SATISFIED | 10-01, 10-02, 10-03, 10-04 | `[x]` | **satisfied** |
| DOCS-01 | 10 | SATISFIED | 10-04 | `[x]` | **satisfied** |

**Score: 8/8 satisfied.** No `unsatisfied`, `partial`, or `orphaned` requirements —
every traceability-table REQ-ID appears in a phase VERIFICATION.md, and every phase
VERIFICATION requirement maps back to a traceability entry. FAIL gate not triggered.

## Cross-Phase Integration

Verified by `gsd-integration-checker` (full pipeline trace + `uv run pytest`):

- **8/8 cross-phase connections wired, 0 orphaned, 0 missing.**
- End-to-end pipeline **PASS** — the `Stance` write→serialize(`.name`)→store→hydrate
  (`Stance[token]` name-lookup)→compare path is unbroken on **both** the in-memory and
  ladybug backends.
- Phase 10 genuinely consumes Phase 9's `stance` field: the stateful oracle's base key
  is widened to `(value, stance)`, so K*6 Extensionality (`revise ≡ expand`) fails on a
  stance mismatch rather than passing vacuously — proven by
  `test_widened_key_discriminates_stance` (the non-vacuity guard) firing on both backends.
- Public surface: `from doxastica import Stance` resolves; the Cluedo tutorial's
  `revise(..., stance=...)` calls and `culprit.stance >= Stance.believed` comparison match
  the real Phase 9 API.

**E2E test run:** 574 passed, 1 xfailed (pre-existing AGM-recovery drift guard, unrelated
to stance), 0 failures.

## Non-Vacuity (the load-bearing milestone constraint)

The milestone's explicit goal was a suite that is *proven non-vacuous*, not merely green.
Phase 10 VERIFICATION documents mutation/revert probes for each success criterion:

- **SC1** — reverting `_base_of` to `{belief_id: value}` collapses the discrimination test
  to `assert {'b1':'v'} != {'b1':'v'}` (fails); `event()` label fires at 21% (memory) /
  26% (ladybug), confirming the discriminating path is actually generated.
- **SC2** — trichotomy expressed as primitive `b < a` (not `@total_ordering`-derived
  `a > b`); a broken `__lt__` yields 24 failures instead of a vacuous pass.
- **SC3** — exhaustive `@parametrize("stance", list(Stance))` with `is`-identity asserts;
  a `doubted`-only hydrate bug raises exactly 4 cases while 20 pass.
- **SC4** — without the ladybug extra: 467 passed, 88 **skipped** (not passed), 1 xfailed,
  exit 0 — SKIP-not-fail confirmed.

## Nyquist Coverage (discovery only — non-blocking)

| Phase | VALIDATION.md | `nyquist_compliant` | Status | Note |
|-------|---------------|---------------------|--------|------|
| 9 | exists (`status: draft`) | false | PARTIAL | VALIDATION artifact left in draft; phase itself passed VERIFICATION 5/5 with substantive dual-backend tests (test_stance.py, test_models_frozen.py, test_stance_persistence.py all green). Stale artifact, not a coverage gap. |
| 10 | exists (`status: approved`) | true | COMPLIANT | — |

Optional retroactive close: `/gsd-validate-phase 9` (not required for milestone completion).

## Tech Debt (non-blocking, from 10-REVIEW.md advisories)

- **WR-01** — `_FORMAL_03_CONFORMANCE_SET` unchecked string registry in `test_invariants.py`
  / `test_scope_at.py`. Pre-existing drift risk; all four names currently resolve; not
  introduced by this milestone. (10-01 added a mechanical conformance-registry check per the
  recent commit `bb486be`.)
- **WR-02** — imprecise `WORLD_SCOPE_ID` doc anchor in the tutorial. Link resolves,
  `mkdocs build --strict` green; cosmetic.

## Verdict

All 8 requirements satisfied, both phases verified 5/5, cross-phase integration fully wired,
E2E pipeline green on both backends, docs strict-clean, and the property suite proven
non-vacuous. No critical gaps. Tech debt is minimal and advisory. **Cleared to complete.**

---

_Audited: 2026-07-05 · gsd-audit-milestone_
